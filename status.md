# 진행 상태 (status.md)

> 매 작업 세션 시작 시 이 파일을 먼저 읽는다. 종료(또는 큰 단계마다) 갱신한다.
> 상세 작업 목록은 `task.md`.

## 현재 마일스톤
- **M0~M7 완료** + **실데이터 반영 착수**. 사용자 실제 파일(templates/)을 하나씩 반영 중.

### 2026-07-24 실데이터 반영 (1) 제품 목록 CSV
- 입력 파일: `templates/TM-NO_List_VMS Part.CSV`(2,675품목), `TM-NO_List_TM Part.CSV`(84품목)
  - 인코딩 CP949, wide 형식. 컬럼: TM-NO·품명·중량·[성형·소결·정형·가공·압입·밴딩·후처리]
  - 파트=파일명, 공정 칸에 값(코드) 있으면 라우팅 포함(좌→우 순서)
- **실제 공정 7종**: 성형·소결·정형·가공·압입·밴딩·후처리 (성형=COPQ 제외 기본)
- 반영:
  - db: product.weight, product_route.op_code, process.ord 컬럼 추가(+마이그레이션),
    db.ensure_processes()/CANON_PROCESSES(7공정)
  - ingest.ingest_product_csv(cp949 wide 파서)
  - 화면 신설: **제품 마스터**(/admin/products: CSV 가져오기·검색·페이징·등록/수정/삭제),
    **공정 관리**(/admin/processes), 메뉴 추가(관리 그룹)
  - calc.process_breakdown → 공정 순서 DB(ord) 기반 동적화
  - 검증: 실제 CSV 2,759품목 import, 화면 렌더(라우팅/코드/중량/COPQ) 정상
- **미확정(다음 파일에서)**: 공정별 **기준단가**(현재 0, Scrap Cost 계산에 필요) —
  공정 칸 코드가 단가인지/작업번호인지 확인 필요. xlsx/xlsm/scrap_data.db 설명 대기.

### 2026-07-24 실데이터 반영 (2) 폐기불량 DB / 외주소재불량 xlsm — 분석·규칙 확정 (구현 전)
- **scrap_data.db** (폐기불량): 테이블 scrap_data 2,593건(2/25~7/24). part=1Part/2Part,
  컬럼 date·process·tmno·scrap_reason·quantity·defect_category('해당'만 363)·defect_process.
  → **defect_category='해당' 만 공정불량율에 반영**. 지정 폴더 자동수집(2차) 예정.
- **00_sintering_defect.xlsm** (외주소재불량): Sheet1(609)+월별시트, 28컬럼.
  컬럼 반입예정일자·업체명·TM-NO·품명·Part·성형불량합계·소결불량합계·기타합계·합계·세부19종.
  → **성형불량합계→성형, 소결불량합계→소결** 공정불량율. **사람이 특정 폴더에 수동 업로드**.
- 확정 규칙(plan §0/실데이터 소스 매핑에 기록):
  · 1Part=VMS PART, 2Part=TM PART
  · 불량율 집계 공정 = **성형·소결·정형·가공 4개만** (양 파트 공통)
  · 폐기·외주소재불량 모두 공정불량(공정불량율)에 합산
- **다음 확인 필요**: xlsm '기타합계' 처리 / 정형·가공 공정불량 데이터 출처 / 공정별 기준단가 출처
- 아직 **구현 안 함**(사용자 "먼저 참고" 요청). 다음 단계에서 ingest(scrap_data.db, xlsm) 설계.

### 2026-07-24 TM-NO 정규화 규칙
- 접미 알파벳 제거해 base로 합쳐 집계 (598-10A→598-10). calc.base_tmno() 추가.
  전 파일 조인/집계 키=base. VMS 96그룹 합쳐짐. 제품마스터=base로 합침 확정(변형행 유지 안함). ingest·저장 시 base 정규화 적용.

### 2026-07-24 불량유형 마스터 누락유형 추가 (양식 파일 수정)
- 외주 xlsm/폐기 '해당'에 있으나 마스터에 없던 유형을 마스터 양식에 추가(TOP 표시용).
- 동의어 통일맵: 크랙→CRACK, 녹→녹불량, 산화→산화불량, 찍힘.눌림→찍힘 (ingest 자동치환 예정).
- DATA_1PART +9, DATA_2PART +11 (배분기준=입력공정 100%). 폐기 '해당'은 소결로_산화.이물/기타_불량 위주.
- 외주/폐기 집계는 공정 단위(성형/소결/기타, 해당+공정)이고, 세부 불량유형은 TOP 표시용으로 마스터 보유.

### 2026-07-24 불량유형 마스터 개정 분석 (구현 전, md만 갱신)
- `templates/불량유형마스터_양식.xlsx` 개정: 시트 **DATA_1PART / DATA_2PART / 작성안내**.
  → 불량유형이 **파트별로 다름** (1PART 33종, 2PART 33종).
- 새 배분기준 **형식 D "입력공정 100%"**: 배분을 마스터가 고정하지 않고,
  **사내 직접입력/Excel 입력 시 사용자가 선택한 공정에 100%** 반영.
  · 셋팅불량 전부 + 일부 공정불량(낙물·설비Error·초기가동 등)
  · 이런 불량은 **사내 입력으로만** 유입(외주·폐기 자동소스엔 없음) — 사용자 확인 완료
- 배분기준에 **"기타" 공정** 실사용 (예: `소결,정형,가공,기타`, 이종혼입 `성형,소결,정형,가공,기타`)
- 발생공정 빈칸 행 다수 → 배분기준으로만 공정 결정. 2PART 불량명에 개행 포함(윈도우\nGO 등)→파싱 시 개행 제거.
- 확정(사용자): ① 입력공정100%는 사내입력 전용 ② 불량유형 마스터 파트별 관리(part 컬럼 추가)
  ③ 업로드 화면도 2시트(DATA_1PART/2PART) 처리
- **구현 대기(다음)**: defect_type에 part 컬럼, 배분기준 형식D 파서, 마스터 업로드 2시트 처리,
  '기타' 공정 신설, 사내 공정/셋팅불량 직접입력 화면(공정 선택→100%).

### 2026-07-24 코딩 시작 — stage①③ 구현·검증 완료
- **확정 모델**: 집계공정=성형·소결·정형·가공·**기타**(=후처리=압입+밴딩+선별). 물리공정→기타 버킷.
  불량유형 공정귀속: 발생공정 **지정**=그 공정 고정 / **공란**=입력 시트의 공정.
- **stage① 마스터 개편(커밋 414b982)**:
  · db: CANON_PROCESSES=집계공정 5종, PHYS_TO_BUCKET/bucket_of(), defect_type part 컬럼+UNIQUE(part,kind,name) 재생성 마이그레이션
  · ingest_defect_master: DATA_1PART/2PART 2시트, 발생공정 공란 허용, calc.parse_alloc_rule이 '입력공정 100%'=빈배분
  · 검증: 실제 마스터 85건(VMS42/TM43) 오류0
- **stage③ 사내불량 폴더 적재**:
  · db: defect_entry에 process·kind·batch_key 컬럼(마이그레이션)
  · calc: Masters.resolve() — 저장 공정/구분 우선, 없으면 배분 폴백. compute_daily·process_breakdown이 이를 사용
  · ingest_daily_defect_file/_folder: 파일명 파싱(PART_공정_구분_YYYYMMDD), wide→long,
    발생공정 지정 유형은 그 공정·공란은 시트 공정, batch_key로 삭제후재적재(멱등)
  · 검증: 찍힘(공란)→성형, 산화불량(발생공정=소결)→소결, 눌림(압입)→기타, 셋팅 구분,
    재적재 멱등(4건 유지). proc_ppm 220·set_ppm 30·성형10/소결5/기타7 정상
- **stage④ 생산 xlsx(커밋 9e3771b)**: 파일명{1part|2part}_{시작}_{종료}, C규격=TM/I입고수량/J입고금액(원→천원),
  TOTAL·빈규격·음수 스킵, 변형 base 합산, production.part. 검증: 2part 입고수량합=파일 TOTAL 일치.
- **stage⑤ 폐기 scrap_data.db(커밋 f4bc6d5)**: '해당'만, part→VMS/TM, defect_process→집계버킷(비표준=기타),
  수량0/무게만 스킵, **TM-NO '-' 제품미지정은 part 단위 집계**(defect_entry.part 추가). 검증: 319건(44 무게만 스킵).
  · ⚠ 열린 확인: 비표준 defect_process(공정QC 5·증기 1·협력사)→기타 기본. 협력사는 외주 중복 가능성 확인 필요.
- **stage⑥ 외주 xlsm(커밋 f5721fe)**: 파일계산 성형/소결/기타 합계컬럼 그대로 반영(VBA 확인: raw기타 50%→소결 분할 있어 합계가 정답),
  kind='공정', TM-NO 없어도 파트집계, idempotent. 검증: 공정합계(성형4145·소결14481·기타2607) 파일 원장 정확 일치.
  · ⚠ 열린 확인: 100EA 검토룰(합계기준이라 원래 '단일유형' 취지와 달라 기본 미적용). 외주 TOP은 유형별 불가(합계 기반).
- **stage⑦ 공정별 단가 + 화면(완료)**: `제품별 단가 Master_Rev23(FY25).xlsx`
  · 파일구조: '단가산출' 시트에 성형(B/D)·소결(F/H)·정형(J/L)·완제품(N/P) 4블록, 4행부터 데이터.
    **누적단가 확인**(단조증가 6527행, 완제품=성형×2.0 중앙값). 정형 'O' 859개 = 정형공정 없음
    (정형 적용리스트 대조로 확증). base TM 2,630 고유, 제품마스터 2,579중 2,358(91%) 매칭.
  · **확정 규칙(사용자)**: ① 가공·기타 = **완제품 단가** ② 정형 'O' → 정형·가공·기타 모두 완제품 단가
  · db: **product_price(tm_no, process, unit_price)** 신설(집계공정 5종 키)
  · calc: Masters.price가 product_route를 버킷 정규화 + **product_price 우선 적용**
  · ingest_price_master: 4블록 파싱→5공정 단가 산출. 검증 2,626품목/13,122행, 정형없음 399건
  · **화면 신설 `/admin/prices`**(메뉴 '💲 공정별 단가'): TM-NO행 정렬 표(TM-NO·품명·파트·공정순서·5공정단가),
    파트/미등록 필터·검색·페이징, Excel 가져오기, 등록/수정/삭제(price_form.html)
    · 목록은 **제품 ∪ 단가보유 TM** 합집합(단가만 있는 품목도 표시). 총 2,847건
  · 검증: 전 라우트 200, 014-00(17.5/21/28/35/35), 정형'O' 1262-00(정형·가공·기타=26), 저장/빈칸삭제/삭제 정상
### 2026-07-27 서버 공유 폴더 구조 확정 + 재귀 스캔
- 루트 `\\carp130001\TheEyesHaveIt\QC_Data\Quality_Data\` (plan.md에 트리 기록)
  · `01_사내불량\{YYYY-MM}\{1PART|2PART}\{공정}\` (년월/파트/공정 3단), `02_생산량\{YYYY-MM}\`
  · `03_외주소재`,`04_폐기불량`,`05_단가마스터` = 고정 파일 덮어쓰기, `09_양식`, `99_오류`
- **원칙**: 폴더는 사람용, 판단은 **파일명**으로. → ingest_daily_defect_folder를 **os.walk 재귀**로 변경,
  Excel 임시파일(`~$…`)·규칙 불일치 파일 스킵, 상세는 상대경로로 반환.
- 검증: 년월/파트/공정 중첩 배치 18파일 인식(임시·무관 2개 제외), 데이터 3파일 적재 4건
  (산화불량은 성형폴더에 있어도 발생공정 규칙대로 소결, 눌림→압입), 재스캔 멱등 유지.
### 2026-07-27 대시보드 개선 (막대그래프·목표선·KPI 클릭 전환·값 라벨)
- 요청 4가지: ① 그래프를 **막대**로 ② **목표는 선**으로 ③ **KPI 지표 클릭 시 아래 그래프가 그 지표로 전환**
  ④ 그래프에 **실적 숫자 표시**.
- 월별 추이 차트를 area → **bar**로 변경(주별 차트는 원래 bar). 목표선은 기존 `drawTarget()`이
  FY 계단식 점선으로 그려주므로 그대로 유지(막대 위에 겹쳐 표시).
- `build_dashboard`에 **metrics 배열** 추가: 8개 지표(Scrap Cost·Scrap Quantity·COPQ·Incident·
  Warranty·공정불량율·셋팅불량율·생산수량) 각각의 7개월 실적/목표 시리즈를 미리 계산해 내려줌.
  KPI 카드에 `data-metric` 부여 → 클릭 시 JS가 차트 data-* 를 교체하고 `window.renderChart()` 재호출.
  선택 카드는 테두리+아래 화살표로 표시, 선택 지표는 localStorage에 저장(새로고침해도 유지).
- `charts.js`: 렌더 함수를 `window.renderChart`로 노출(지표 전환 시 재렌더용).
- **구현 중 발견해 함께 고친 문제 3건**:
  1. Jinja에서 `{% set c %}`는 content 블록 전용이라 scripts 블록에서 `c` 미정의 → 500 오류.
     `d.charts.metrics`로 직접 참조.
  2. metrics dict의 `values` 키가 **Jinja에서 dict.values 메서드로 해석**돼 시리즈가 통째로 깨짐
     (예전 리포트에서 `vseries`로 바꿨던 것과 동일한 함정) → `vseries`로 개명.
  3. `_join()`의 `%g` 포맷이 큰 수를 **지수표기**(1.77769e+07)로 내보내 JS가 못 읽음 → 고정소수 변환.
- **값 라벨 겹침 해결**: 7개월 × 큰 수(생산수량 1,778만EA)면 라벨이 슬롯 폭을 넘겨 겹침.
  · 1e7↑는 `만`, 1e5↑는 `천` 단위로 축약, 한글/숫자 글자폭을 구분해 실제 폭 추정,
    슬롯보다 넓으면 **앞 라벨과 세로로 겹칠 때만 위로 한 칸 올려** 어긋나게 배치.
  · 막대 최대치 여백 1.18배 → 1.30배로 확대(라벨 자리 확보).
  · Playwright 실측으로 겹침 0건 확인(라벨 폭 42.2px, 인접 라벨 y가 서로 다름).
- 검증(Playwright): 8개 지표 전환 모두 정상(막대 7개·선택 표시·라벨 단위 정확),
  리포트 화면 회귀 없음(KPI 6차트·공정별 4차트 렌더), JS 오류 0(favicon 404만).
- ⚠ 발견한 **데이터 이슈**(코드 아님, 미수정): 목표관리의 FY27 통합 COPQ 목표가 **310**으로 저장돼 있음
  (다른 파트는 0.28/0.355). 0.310 입력 의도로 보이며, 차트 목표선이 크게 벗어나 보임 → 사용자 확인 필요.

### 2026-07-27 외주소재불량·폐기불량 100EA 검토 화면 부활 + 재스캔 보존
- 요청: 예전 "단일 TM×단일 불량유형 ≥100EA 격리" 검토 화면(승인/수정/삭제)을 **현재의 폴더반영
  파이프라인(외주소재불량 xlsm·폐기 scrap_data.db)에 다시 연결**.
- **핵심 이슈**: 두 소스는 재스캔 시 `DELETE 전체 후 재삽입`(idempotent) 방식이라, 사람이 검토화면에서
  내린 결정(승인/수정/삭제)이 다음 재스캔에서 **그대로 사라지는 구조적 충돌**이 있었음 — 해결 필요.
- **해결**: `defect_entry.reviewed`(0/1) 컬럼 신설. 재스캔 시 `reviewed=0`인 행만 삭제하고,
  `reviewed=1`(사람이 처리 완료)인 행은 **보존**, 같은 (일자,TM-NO,불량명) 키로 새로 들어오는 행은
  건너뛴다(`ingest._reingest_preserve_reviewed()` 공통 헬퍼).
  · **삭제는 하드 DELETE 대신 `status='rejected'`로 소프트 처리**(계산은 `status='confirmed'`만 보므로
    자동 제외되고, `reviewed=1`이라 재스캔에도 다시 나타나지 않음).
  · `ingest_outsource_xlsm(quarantine_100=True)`(기본 재활성화), `ingest_scrap_db(quarantine_100=True)`
    (신규 지원 — 이전엔 파라미터 자체가 없었음).
- 화면(`review.html`/`/input/outsource-review`): **출처 배지**(외주소재불량/폐기불량) 추가,
  버튼을 "승인/반려" → **"반영(수정 가능)/삭제"**로 명확화. 메뉴명 "외주소재불량 검토"→**"불량 검토(100EA↑)"**
  (두 소스 공통 처리 반영).
- 검증: 재적재 시 외주 30건·폐기 16건 총 **46건** 검토대기로 격리. 승인·수정·삭제 3종 처리 후
  재스캔해도 상태·수량이 그대로 보존됨(재등장 없음) 확인. 승인 시 화면에서 즉시 사라짐 확인. 전 화면 200.
- qms.db 반영(백업 `qms_before_review.db`): 실제 46건이 검토대기로 전환(그만큼 일시적으로 계산에서
  제외 — 검토·반영 전까지는 KPI에 안 잡힘. 예: 2026-07 VMS 공정불량 1235ppm/ScrapCost 8,039천원으로
  일시 하락, 검토 후 반영하면 정상화).

### 2026-07-27 사내불량시트 처리 규칙 재정립 (성형 시트 제외 + 마스터 배분 우선)
- **확정 규칙 3가지(사용자)**:
  ① 사내불량시트는 성형·정형·가공·후처리·선별 등 여러 공정이 각자 작성한다.
  ② **성형 공정이 작성한 시트(공정+셋팅 모두)는 불량 D/B에 전혀 반영하지 않는다.**
  ③ 나머지 시트의 데이터는 **시트를 쓴 공정이 아니라 불량유형별 마스터 할당 공정**으로 집계한다.
     여러 공정 배분은 **동일비율 정수배분**(소수점 없음), 마스터 공란(=입력공정100%)만 시트 공정 그대로.
  + **COPQ/Scrap Cost "성형 제외" 규칙 폐기** — 성형 시트 자체가 D/B에 없으니 불필요.
    단, 다른 시트에서 배분기준에 따라 성형으로 배분된 몫은 COPQ에 정상 포함(제외 안 함).
  + **공정별 불량현황은 성형·소결·정형·가공·기타후공정 5개만 표시**.
- **발견한 기존 버그 2건(구현 중 확인)**:
  1. `Masters.resolve()`가 저장된 공정(sheet_proc)이 있으면 **무조건 100%로 확정**하고 마스터의
     발생공정/배분기준을 아예 참조하지 않고 있었음(퍼센트 배분 무시). 예: `산화불량`이 성형 시트에
     적혀도 마스터상 발생공정은 소결인데 성형으로 집계되던 문제.
  2. `Masters.defect` 딕셔너리가 **불량명으로만 키잉**돼 파트 구분이 없어, 같은 이름이 1PART/2PART에서
     다른 발생공정·배분기준으로 등록된 22건(예: CRACK, 녹불량)에서 나중에 로드된 파트 값이 다른
     파트까지 덮어씀. 지금까지는 resolve()가 이 dict를 안 써서 무증상이었으나, 버그①을 고치면서 활성화될
     상황이라 함께 수정.
- **코드 변경**:
  · `ingest_daily_defect_file`: sheet_proc=='성형'이면 즉시 스킵(batch_key 기존분 삭제 후 0건 반환).
    나머지는 마스터 발생공정 사전조회(dmap) 제거하고 **시트 공정을 그대로 저장**(최종 귀속은 calc가 결정).
  · `ingest_history_defect_xlsx`: `_hist_process_of()`가 "성형"이면 해당 행 자체를 skip.
  · `calc.Masters`: `self.defect`를 `(part,name)`으로 키잉. `allocate(part,...)`가 배분기준 타겟을
    **집계버킷(bucket_route)** 기준으로 필터링하도록 수정(기존엔 물리공정 route와 잘못 비교).
    `resolve(part,...)`: 마스터에 발생공정/배분기준이 있으면 그 규칙대로(allocate 호출),
    **둘 다 공란일 때만** 시트 저장 공정을 100% 사용.
  · `compute_daily`/`process_breakdown`/`top5_defect`: `part`를 resolve/kind_of에 전달하도록 호출부 수정.
    `scrap_cost_copq` 제외 로직 삭제(= scrap_cost와 항상 동일).
  · `process_breakdown`의 표시 공정 목록을 `process` 테이블 조회 대신 **`db.AGG_PROCESSES` 5종 고정**으로
    변경 — 향후 테이블에 잔여 항목이 생겨도 리포트가 영향받지 않음.
  · `db.py`: `CANON_PROCESSES` 성형 copq_exclude 1→0. `_prune_noncanonical_processes()` 신설
    (init_db에서 실행) — process 테이블의 비집계공정 잔여행 자동 정리 + copq_exclude 재동기화.
  · `report_defect.html`: COPQ 제외 배지/컬럼 삭제. `PROC_LABEL={"기타":"기타후공정"}` 도입해
    공정별 불량현황 라벨을 "기타"→"기타후공정"으로 표시.
  · `processes.html`: 사실과 다른 COPQ 제외 안내문구 수정.
- **qms.db 반영**(백업 `qms_before_rule_fix.db`):
  · `process` 테이블의 잔여 "선별"(ord=0) 삭제, 성형 copq_exclude 1→0.
  · 성형 태깅 사내입력(`source='direct' AND process='성형'`) **6,511행 삭제**(수량합 52,869).
    defect_entry 17,288 → 10,777.
- **검증**: 산화불량(비성형 시트, 마스터=소결)→소결 100% 재배분 확인, 찍힘(4공정균등)
  103건→35/34/34(합계 일치), CRACK(정형70:성형30)→45/19, 낙물(공란)→시트 공정 그대로.
  COPQ=ScrapCost 전체(제외 없음) 확인. 공정별 불량현황 5개 행 고정 확인, "선별" 완전 제거.
  Low_data 재적재해도 성형 재등장 0건, 사내불량 폴더 성형 파일 재스캔 시 0건 반환 확인.
  전 화면 200. 롤링 KPI: 2026-01~07 공정불량 796→945→1028→1133→1177→1304→1642ppm(자연스러운 추세).

### 2026-07-27 Low_data/Low_Inventory 7월분 추가 D/B화
- 브랜치에 7월 파일 6개(Low_data 4 + Low_Inventory 2) 추가 업로드됨 → 동일 로직으로 적재.
- **기존 7/23 단일일 생산 샘플과 충돌 발견·정리**: qms.db에 이미 있던 2026-07-23 하루치
  생산(1PART 704,043·2PART 35,180, 초기 시스템 구축 시 넣었던 테스트성 단일일 파일)이
  이번 Low_Inventory 7월 **월간 전체** 총량(1PART 18,486,667·2PART 645,285)과 중복 집계될 상황이라
  **기존 07-23 118행을 삭제하고 월간 총량으로 교체**함(같은 원칙: 월별 집계만 사용).
  1PART는 파일 TOTAL과 48,732 차이 나는데, 음수 조정행 21건 합(-48,732)과 정확히 일치 —
  stage④와 동일하게 양수만 반영하는 기존 정책 그대로(정상, 버그 아님).
- Low_data 7월 4개 파일: 2,140건 적재, 신규 불량유형 0(1~6월에서 이미 다 등록됨).
- 검증: 재적재 멱등 확인. **7월 KPI가 1~6월 흐름에 자연스럽게 이어짐**
  (1PART 공정불량 815→962→1048→1229→1393→1454→**1849**ppm, ScrapQty% 0.12→…→**0.22**) —
  이전엔 7월 생산량이 하루치뿐이라 10,910ppm으로 튀었던 문제가 월간 전체 생산량 반영으로 해소됨.
  전 화면 200. 백업 `qms_before_july.db` 보관.

### 2026-07-27 Low_data/Low_Inventory 과거(2026 1~6월) 원천 데이터 D/B화
- **Low_data**(24파일): 2026년 1~6월 **월단위** 사내 공정/셋팅 불량 실적, 파일명
  `{1part|2part}_{공정|셋팅}불량_26년 N월.xlsx`. 구조가 일일양식과 달라 **행(품명 B열)이 발생공정을 결정**
  (열은 여러 공정 유형이 섞인 마스터 전체 목록). 확인된 규칙(사용자):
  ① 품명에 성형/정형/압입/**가공** 포함 시 그 공정, 미포함 시 **후처리**(=집계상 기타)
  ② `폐기` 컬럼은 불량유형이 아니므로 **무시**
  ③ 마스터에 없는 불량명은 **공백차이만 있으면 기존명에 흡수**(nospace 비교), 진짜 새 이름만 마스터에 추가
  ④ 날짜는 그 달 **말일**로 통일, 월별 집계만 사용
- `ingest.ingest_history_defect_xlsx()` 신설: 우선순위 키워드 매칭(겹침 없음, 검증완료)으로 공정 결정,
  batch_key=`hist|{part}|{kind}|{ym}`로 재실행 안전(멱등, 검증: 재적재해도 총건수 불변).
  신규 불량유형 9종 자동 추가(설비문제(자동화)·편가공(척파손)·금형파손·초기가동·쪽떨어짐·품질문제·
  PIN파손·소재불량·셋팅(순수포함)), process=''·alloc_rule=''로 등록(행에서 이미 공정 확정하므로 무관).
  ⚠ 이 신규 유형들은 **DB에만 추가**됨 — `templates/불량유형마스터_양식.xlsx`는 안 건드림.
  나중에 마스터를 파일에서 재적재(`ingest_defect_master`, DELETE 후 재삽입)하면 사라지니,
  영구 반영하려면 마스터 xlsx도 갱신 필요.
- **Low_Inventory**(12파일): 월별 입고수량, 파일명 `{1part|2part}_{N}월_입고수량.xlsx`.
  기존 `ingest_production_xlsx`와 컬럼 100% 동일(C=규격,I=입고수량,J=입고금액) →
  `ingest.ingest_history_inventory_xlsx()` 신설(같은 파싱, 날짜만 월말로 대체).
  → **1~6월 생산수량 공백을 메워, 그동안 계산 불가능했던 그 기간 불량율(ppm)이 이제 원천 데이터로 산출됨.**
- **kpi_actual 정리**(사용자 확정): 과거데이터.xlsx는 **작년(2025) 데이터만 유지**, 2026년(1~6월) 실적행은
  **삭제**(130→80건) → 2026년은 이제 원천 데이터(Low_data+scrap_data.db+00_sintering_defect.xlsm) 기반
  **계산값**으로 표시(month_kpi의 "실적 우선" 규칙에서 2026년은 더 이상 override 없음).
- **검증**: 드라이런 후 실제 qms.db 반영(백업 `qms_before_lowdata.db`). defect_entry 1,197→15,148
  (사내 13,951 신규), production 118→3,669. 재적재 멱등 확인. KPI 계산 정상 렌더링
  (1PART Scrap Qty% 1~7월: 0.12,0.13,0.14,0.16,0.18,0.19,1.09 — 화면 차트와 일치).
  전 화면 200(대시보드 양파트·리포트 공정/셋팅 탭·제품마스터·목표관리·SVP·폴더반영).
- **참고 관찰**(버그 아님): 2PART 2026-04 공정불량이 계산값 5,816ppm으로 기존 공식실적(2,006ppm)보다 높음
  → TM `1987-00`에 외주소재불량 3,360개가 4월에 집중된 실제 이벤트(기존 적재된 xlsm 원본 확인, 중복 아님).
  공식 집계 방식과 시스템 계산 방식의 산정 범위 차이로 보이며, 사용자가 "이제 원천 데이터 기준"으로
  전환을 확정했으므로 그대로 반영.

### 2026-07-27 Scrap Cost 확인(이미 공정+셋팅 합산) + SVP 입력 UX 개선
- **Scrap Cost**: 요청("Scrap Cost도 Scrap Quantity처럼 공정+셋팅 합산")을 검토한 결과
  `compute_daily`의 비용 누적 로직이 애초에 `kind`로 필터링하지 않아 **이미 공정+셋팅 전체 합산**이었음.
  코드 변경 없음. 검증: 단가 있는 TM에 셋팅불량 100개(단가450) 추가 → Scrap Cost가
  정확히 +45천원 증가(438→483)해 비용에 포함됨을 확인.
- **SVP 입력 UX**: 스핀 화살표 제거(`type="number"`→`type="text" inputmode="numeric"`),
  **천단위 콤마 자동 표시**(입력 시 JS로 실시간 포맷, 서버는 기존처럼 콤마 제거 후 파싱).
  검증: 콤마 포함 제출(`80,000,000`)이 DB에 `80000000.0`으로 정확히 저장, 표시도 콤마 포맷.

### 2026-07-27 Scrap Quantity 산식 명시 (공정불량+셋팅불량 ÷ 입고수량)
- `month_kpi`에서 Scrap Quantity를 **공정불량수량 + 셋팅불량수량**으로 명시 계산하고,
  분모는 **입고수량(생산수량)** 사용. plan §5.4 산식도 갱신.
- **셋팅불량율(ppm) 지표는 그대로 유지**(사용자 확정) — 대시보드 카드·KPI 차트·목표관리 모두 변경 없음.
  (처음에 지표 제거로 진행했다가 사용자 정정으로 원복함)
- 검증: 2026-07 1PART 공정 7,681 + 셋팅 0 = 7,681 ÷ 704,043 = 1.09%,
  셋팅 300건 추가 시 7,981(1.13%)로 합산되고 셋팅불량율 426ppm 별도 산출 정상.
- 참고: 기존에도 scrap_qty는 모든 불량 합이라 수치는 동일 — 산식이 코드에 명시된 것이 변경점.

### 2026-07-27 과거 KPI 실적(과거데이터.xlsx) 적재
- 파일 구조: 1행 FY 라벨(FY26/FY27 병합), 2행 월(4월~3월), 3행~ 데이터.
  A열=KPI · B열=부문(1PART/2PART/합계) · C열=단위, 4열~ 월별 값. **원천 데이터가 아니라 집계 완료된 KPI 실적**.
  · 지표 6종: COPQ%, Customer Incident 건, Warranty 천원, 공정불량 ppm, 셋팅불량 ppm, Scrap Quantity%
  · 기간 2025-04 ~ 2026-06 (FY26 전체 + FY27 4~6월)
- **kpi_actual 테이블 신설**(ym, part, kpi, value, unit / UNIQUE(ym,part,kpi)).
  `ingest_history_xlsx()`: FY 라벨을 오른쪽으로 전파해 열→년월 매핑, 부문/지표명 정규화(부분일치 허용).
- `calc.month_kpi()`가 **입력된 실적을 계산값보다 우선** 사용(`from_actual` 키로 출처 표시).
  과거 월은 원천 데이터가 없고 보고된 공식 수치가 기준이기 때문. 실적 없는 달(2026-07)은 계산값 유지.
- init_data STEPS에 '과거 KPI 실적' 추가 → 초기 구축에 포함.
- 검증: 130건 적재(인식못한 행 0), 엑셀 값과 일치(2025-04 COPQ 0.25/0.214/0.236, 2026-06 공정불량 1381/3521),
  KPI 차트에 반영(COPQ 0.259·2.413·0.137…, 공정불량 1204·1044·826…), 전 화면 200.
- qms.db 반영 완료(백업 후). 사용자가 서버에서 입력한 svp 45건·target 77건은 **그대로 보존**.
- 참고: 이 파일은 폴더 스캔 소스에는 없음 — 월 추가 시 `templates/과거데이터.xlsx` 갱신 후 재적재 필요.
  (필요하면 `06_과거KPI실적` 폴더 소스로 추가 가능)

### 2026-07-27 SVP 입력단위 원 + 합계 직접입력
- **입력단위 천원 → 원**: DB(svp.amount)에 **원 단위로 저장**하고, `calc.svp_of()` 가
  천원으로 환산(÷1000)해 반환 → 나머지 계산(ScrapCost%·COPQ%)은 그대로 천원 기준 유지.
- **합계 자동합산 → 직접입력**: 표에 **합계 행(part='통합')** 추가, 12개월 모두 입력 가능.
  · `svp_of()`: 통합 조회(파트 2개 이상)일 때 **입력된 합계값 우선**, 없으면 파트 합으로 폴백.
  · 맨 오른쪽 열은 `FY 누계`(그 행 12개월 합, 참고용)로 이름 변경. 하단 자동합산 행은 제거.
- 검증: 원 입력(1PART 80,000,000 / 2PART 65,000,000 / 합계 150,000,000) →
  DB에 원 저장, KPI 분모 1PART 80,000천원·2PART 65,000천원·**통합 150,000천원**(파트합 145,000과
  다른 별도 입력값 사용 확인), 합계 비우면 145,000천원으로 폴백. 전 화면 200.

### 2026-07-27 SVP 입력: Excel 업로드 → FY 가로 표 직접입력
- Excel 업로드 제거, **FY별 가로 표**로 직접 입력하도록 전용 화면 신설(`svp.html`).
  · FY 탭(입력이력+당해/전년 FY 자동 구성) · 행=1PART/2PART · 열=**4월~익년 3월 12개월**
  · 행 합계·열 합계·총합 자동 표시. 빈칸 저장 = 해당 월 삭제(그 달은 생산금액 추정으로 폴백).
- 라우트: `GET /input/svp`(전용), `POST /input/svp/save`. INPUT_PAGES에서 svp 제거,
  `_recent_rows`·`input_upload`·`input.html`의 svp 분기도 정리(업로드 경로 완전 제거).
- 검증: 업로드 UI 없음, 12개월 컬럼 정상, 저장 후 합계(1PART 230,066 / 2PART 192,040 / 총 422,106),
  빈칸 저장 시 삭제 반영, 입력값이 KPI 분모로 확정 반영(2026-07 1PART 80,000천원 '확정',
  미입력 6월은 '추정' 폴백), 전 화면 200.

### 2026-07-27 qms.db 데모 데이터 삭제 + 삭제 기능 추가
- 사용자가 브랜치에 올린 `qms.db`에서 **개발용 seed 데이터 제거**(백업 후 작업).
  · 데모 품목 10건(1545-01 VVT 스프로킷 등) + 실적: product 10·route 40·production 3,420·defect_entry 1,247
  · seed 샘플 테이블: svp 30·claim 192·incident 16·target 38
    (목표 38건이 **전부 seed 기본값과 일치**함을 대조 확인 후 삭제 — 사용자 입력분 없음)
  · 남은 실데이터: 제품 2,660 · 단가 13,117 · 생산 118 · 불량 1,197(외주 878·폐기 319). 계정 3개 유지.
  · VACUUM으로 1.9MB → 1.4MB. 데모 잔존 0, 고아 실적 0 확인.
  · 삭제 후 KPI: 2026-07 1PART 10,910ppm / 2PART 109,778ppm (2PART는 생산 35,180 대비 외주불량이 커서 높음)
- **기능 추가**:
  · 제품 마스터 목록에 **행별 삭제 버튼**(제품·라우팅·단가 함께 삭제)
  · 폴더 반영 화면에 **'🧹 데모 데이터 삭제' 버튼** + `init_data.purge_demo()`
    — 제품만 지우면 defect_entry가 part로 계속 집계돼 KPI가 왜곡되므로 **생산·불량까지 함께** 삭제
  · 검증: 데모+실데이터 혼재(제품 2,670)에서 purge 후 실데이터 2,660 유지, KPI 10,941ppm 복원
- ⚠ 주의: `python -m app.seed` 는 제품 마스터를 **전체 교체**하므로 실운영 DB에서 실행 금지.

### 2026-07-27 제품 수정폼: 원본코드 → 공정별 단가 칸으로 교체
- 요청: 수정 화면의 **원본코드 칸을 없애고 그 자리에 공정별 단가**를 넣음.
- 문제: 라우팅은 물리공정 7종, 단가는 집계공정 5종 → 그대로 합치면 압입·밴딩·후처리 3행이
  '기타' 단가 하나를 공유(입력칸 3개가 같은 값)하게 됨.
  → **표를 집계공정 5종으로 통일**: `사용 | 공정(순서) | COPQ | 기준단가(원, 누적)` 한 표.
  · '기타' 행에 `(압입·밴딩·선별)` 부기. COPQ 제외(성형) 배지 표시. 별도 단가 패널 제거.
- 라우팅 표시/저장도 집계공정 기준(수정 시 후처리→'기타'로 체크되고 그대로 저장).
  기존 op_code는 삭제하지 않고 공정별로 보존(`codes` 매핑) — CSV 재적재 시 물리공정명 복원됨.
- main.py: `_copq_excluded()` 추가, product_new/edit/save에서 phys/bucket/code_* 제거.
- 검증: 수정폼 표 5행(원본코드 칸 없음), 기존 단가 로드, 정형 해제+기타단가 저장 → 목록
  `성형→소결→가공→기타 / 300·360·480·600·600` 반영 및 재편집 유지, 신규등록 `8888-7`→`8888-07`
  (새 TM-NO 규칙 동작), 삭제 정상, 전 화면 200.

### 2026-07-27 TM-NO 정규화 규칙 확장 (0채움·접미부 없음)
- 추가 규칙(사용자): ② `588-5` = `588-05` (접미부 2자리 0채움) ③ `1632` = `1632-0` = `1632-00` (접미부 없으면 -00)
  · 기존 ① 접미 알파벳 제거는 유지. `calc.base_tmno()` 재작성.
  · **안전장치**: 접미부가 숫자가 아니면(외주 xlsm의 날짜깨짐 `1990-01-01 00:00:00` 등) 원본 유지.
- **db._normalize_tmno()** 신설(init_db에서 실행): 규칙 변경 시 기존 행을 재정규화
  (product/product_route/product_price/production/defect_entry). UNIQUE 충돌 시 정규형 유지, 옛 행 삭제.
- **영향 검증**: 제품 마스터 2,759건 중 **키 변경 0건**, 신규 충돌 0건(기존 9건과 동일), 고유키 2,660 유지
  → 기존 집계에 영향 없음. 실 DB에서 `637`→`637-00` 1건 정리됨(단가 2626→2625는 이 통합 결과, 정상).
- 단위테스트 15케이스 통과. 전체 파이프라인 재실행 후 KPI 동일(10,941ppm·성형251·소결813·기타6639), 비정규 키 0.
- 현 데이터에서 매칭 개선은 0건(생산의 `5002`·`5003`은 마스터에 없는 상품 행) — 향후 수기 입력 대비 예방 규칙.

### 2026-07-27 제품 마스터 + 공정별 단가 화면 통합
- **중량 관리 제거**: 목록·수정폼에서 중량 삭제(제품 CSV의 중량은 DB에 남지만 화면·저장에서 미사용).
- **화면 통합**: '공정별 단가' 화면을 **제품 마스터에 병합**. 메뉴 '📦 제품 마스터 (단가 포함)' 하나로.
  · 목록: TM-NO · 품명 · 파트 · 공정 라우팅 · **성형/소결/정형/가공/기타 단가**, 파트·단가미등록 필터·검색
  · 수정폼 3단: ① 제품정보(TM-NO·품명·파트) ② 공정 라우팅(**물리공정 7종** 체크+원본코드, 집계공정 표시)
    ③ 공정별 단가(**집계공정 5종**, 누적) — **기존 값이 채워진 상태로 열리고 저장 시 반영**
  · 저장은 product / product_route / product_price를 한 트랜잭션에서 처리. 단가 빈칸=삭제.
    TM-NO 변경 시 옛 레코드 3테이블 정리, 제품 삭제 시 단가도 함께 삭제.
  · db.PHYS_PROCESSES(라우팅 7종) 추가 — 라우팅은 물리공정, 단가·집계는 5종으로 명확히 분리.
  · `/admin/prices`는 `/admin/products`로 **리다이렉트**(옛 링크 호환), prices.html·price_form.html 삭제.
- 검증: 목록에 단가 표시(014-00 17.5/21/28/35/35), 수정폼 기존값 로드(라우팅·코드·단가),
  저장 반영(품명·라우팅 가공 추가·성형 999·기타 비움=삭제) 후 재편집 유지, 신규등록 base 정규화(9999-01A→9999-01),
  삭제·필터·리다이렉트 정상. 전 화면 200. 메뉴 12개.
  · 주의: 폼 필드명에 한글 사용(use_성형 등) — 테스트 시 요청을 **UTF-8로 인코딩**해야 함(브라우저는 정상).

### 2026-07-27 UI 디테일 수정 3건
1. **파트 용어 통일**: 화면 표기를 **1PART/2PART**로 통일. DB 값은 'VMS PART'/'TM PART' 유지
   (데이터 마이그레이션 없음). main.py에 `pl` 필터 + `PARTS` 전역 추가, 템플릿 9개 적용.
   · 대시보드 TOP5 제목의 `top_part.split(' ')[0]`("VMS")도 `|pl`로 교체. 표시 잔존 0 확인.
2. **파일 업로드 UI 제거**: 제품 마스터·공정별 단가·마스터 화면의 상단 가져오기 폼 삭제
   (폴더 반영/초기구축으로 일원화). '마스터 업로드' 메뉴명 → **'마스터 조회'**.
   · 업로드 **라우트는 유지**(화면만 제거) — 되돌리기 쉬움.
   · ⚠ 열린 이슈: 불량유형 마스터는 폴더 소스에 없어, 개정 시 반영 경로가 초기구축(--force)뿐.
     필요하면 `06_불량유형마스터` 폴더 소스를 추가하면 됨.
3. **공정/셋팅 불량율 월별 목표**: target에 `mon` 컬럼(0=연간, 1~12=월별) + UNIQUE(fy,part,kpi,mon).
   마이그레이션은 기존 목표를 mon=0으로 이관(검증 완료).
   · `target_val(..., mon)`: 월별 목표 우선, 없으면 연간 폴백. 대시보드 카드·KPI 차트·공정별 차트 적용.
   · 목표관리 화면에 **월별 목표 표(4월~3월, 2지표×12칸)** 추가. 빈칸 저장 시 해당 목표 삭제.
   · 부수 수정: 불량율 목표 파트가 'VMS PART' 하드코딩 → 선택 파트 사용(통합일 때만 VMS).
   · 검증: 7월=3000·6월=4000 저장 → 차트 목표선 `,,,5000,5000,4000,3000`(연간 5000 폴백),
     대시보드 카드 목표 3,000/50 표시. 전 화면 200.

### 2026-07-27 초기 데이터 구축(templates) + 메뉴 정리
- **운영 흐름 확정(사용자)**: ① 최초 1회 `templates/` 실데이터로 DB 구축
  → ② 이후 신규는 공유 폴더에서 '폴더 반영'으로 수집. templates는 이후 보지 않음.
  겹치는 기간은 **폴더가 최신으로 덮어씀**(외주·폐기 전체교체, 나머지 upsert).
- **app/init_data.py 신설**: 8단계 순서 적재(불량유형→제품VMS/TM→단가→생산1/2part→외주→폐기).
  `python -m app.init_data`(데이터 있으면 중단) / `--force`. 화면 버튼도 추가(/admin/scan/init).
  검증: 85건/2675/84/2626/100/18/878/319 전부 정상, 재실행 방지 동작.
  **초기구축 후 폴더반영 연속 실행해도 중복 0** (제품2,660·외주878·폐기319·생산118 유지).
- **메뉴 정리**: 폴더 반영으로 대체된 업로드 화면 5개 제거
  (불량 입력·외주소재불량 가져오기·폐기불량 가져오기·생산실적 입력 / 외주검토는 **pending>0일 때만** 표시).
  INPUT_PAGES는 수기 입력 소스(SVP·Claim·Incident)만 유지. 제거된 경로는 대시보드로 303.
  → 메뉴 18개 → 13개. 전 화면 200 확인.
- DEPLOY.md 1-1절을 2단계 운영 흐름(초기구축 → 폴더반영)으로 개정 + 폴더 구조/권한 명시.

### 2026-07-27 stage⑧ 폴더 반영 화면 (수동 버튼) — 완료
- **결정(사용자)**: 우선 **수동 버튼**으로 시험 → 나중에 자동 주기로 변경.
  → 이를 위해 스캔 로직을 **app/scan.py 모듈로 분리**(화면과 향후 스케줄러가 같은 `scan_all()` 호출).
- db: **setting(k,v)** 테이블 + get_setting/set_setting. 루트 경로·last_scan_at 저장.
  기본 루트 = `\\carp130001\TheEyesHaveIt\QC_Data\Quality_Data` (env QMS_DATA_ROOT로도 지정 가능)
- app/scan.py: SOURCES 5종(사내불량/생산량/외주소재/폐기불량/단가마스터),
  scan_one()·scan_all(), upload_log에 `scan:{key}` 이력 기록. 폴더는 **읽기 전용**(수정·이동·삭제 없음).
  · 외주/폐기/단가는 폴더 내 **최신 수정 파일** 1개 사용
- **화면 신설 `/admin/scan`**(메뉴 '🔄 폴더 반영', 관리자): 루트 경로 편집·연결상태 배지,
  소스별 파일 수·폴더 유무, **전체 반영 / 소스별 반영 버튼**, 반영 결과표(오류 상세 최대 20건), 최근 이력 20건
- **검증(실데이터 전체 파이프라인)**: 사내불량 18파일4건 · 생산 2파일118품목 · 외주 878건 ·
  폐기 319건(무게만 44 제외) · 단가 2,626품목 → **전부 정상**, 재스캔 멱등(불량 1,201건 유지).
  집계: 2026-07 VMS 공정불량 10,941ppm/셋팅 4ppm/생산 704,043/ScrapCost 438천원, 공정별 성형251·소결813·기타6639.
  오류 케이스(잘못된 경로)도 크래시 없이 '확인 필요' 표시. 기존 화면 전부 200.
- **다음**: 자동 주기 스캔 전환(스케줄러) — 요청 시. `scan_all()` 그대로 호출하면 됨.
- 참고: 단가파일이 FY25 기준 — FY별 단가 관리 필요 여부 미확인(현재 단일 단가 테이블).

### 2026-07-24 사내 불량 일일 입력 양식 생성 (양식만, 적재코드 별도)
- 요구: 사내 공정/셋팅 불량을 **하루 단위 별도 파일**로 입력.
  · 파일 = 파트 × 공정 × 구분(공정/셋팅) × 날짜. 파일명 `1PART_성형_공정불량_20260724.xlsx`.
  · 행=TM-NO(입력자 직접 기입), 열=불량유형, 셀=수량 (wide).
  · 열(불량유형)은 **파트·구분 전체 목록, 모든 공정 시트 공통** (1PART 공정34/셋팅8, 2PART 공정35/셋팅8).
- 공정별 구분: 성형·정형·가공=공정+셋팅 / 소결·압입·밴딩·선별공정=공정만.
  · 1PART 공정: 성형·소결·정형·가공·압입·밴딩·선별공정 (7)
  · 2PART 공정: 성형·소결·정형·가공·선별공정 (5)
- 산출물:
  · `scripts/gen_daily_defect_templates.py` — 마스터(DATA_1PART/2PART) 읽어 양식 자동생성.
    사용: `python scripts/gen_daily_defect_templates.py [YYYYMMDD] [출력폴더]`
  · `templates/사내불량_양식/` 에 표준 하루치 **18개** 양식(공통 서식·합계 SUM·틀고정) 생성.
- **적재 방식 확정(사용자)**: 입력자가 작성한 양식을 **지정 폴더에 규칙적 파일명으로 저장** →
  시스템이 그 폴더 파일 전체를 스캔해 DB화(폴더 스캔 배치). 파일명이 곧 (파트·공정·구분·일자) 키.
  · **중복 방지**: 같은 (파트·공정·구분·일자) 재스캔 시 기존 사내입력분 삭제 후 재적재(idempotent).
  · 미확정: 폴더 경로, 스캔 트리거(수동 버튼 vs 주기 스케줄).
- **다음(구현 대기)**: 이 wide 양식 → defect_entry 적재 ingest(파일명 파싱→wide→long melt,
  공정=파일의 공정 100% 반영, 개행 제거·동의어 통일, 폴더 스캔·idempotent 재적재). 아직 코딩 안 함.

### 2026-07-24 불량율 계산 규칙 확정 (5건, 구현 전·md만)
- ① **분모 공통**: 공정불량율·셋팅불량율 모두 분모 = 그 파트의 기간 총 생산수량(입고). 공정마다 분모 다르게 안 함.
- ② **외주소재불량율 별도 계산 안 함**: 외주 불량은 불량유형별로 **해당 공정의 공정불량 수량에 합산만**.
- ③ **폐기도 동일**: '해당' 건을 해당 공정의 공정불량 수량에 **합산만** (별도 폐기불량율 없음).
- ④ **비집계 공정(압입·밴딩·후처리) 발생 불량도 버리지 않음**: 불량유형→해당 집계공정(성형·소결·정형·가공·기타)에 수량 포함.
  → 핵심: **집계 공정은 "불량유형→해당 공정" 매핑으로 결정**, 발생공정이 집계공정을 정하지 않음.
- ⑤ **날짜 기준**: 불량수량=**발생(등록)일**, 생산수량=**입고일**. 각자 자기 날짜로 기간 집계.
- 반영 위치: plan §0 결정표(집계 공정/외주·폐기 원칙 행), 실데이터 소스 매핑, §5.4 KPI 산식.
- 코딩 영향(다음 구현 시): process_breakdown/month_kpi 분모는 현행(공통 생산수량) 유지 OK.
  외주·폐기 ingest는 **불량율 계산 없이 defect_entry 수량으로만** 적재(공정=유형매핑/ defect_process).

### 2026-07-24 실데이터 반영 (3) 생산수량 xlsx + 추가 규칙 확정 (구현 전)
- **생산수량 파일** `구분_시작일_종료일.xlsx` (예: 1part_20260723_20260723.xlsx):
  ERP '제품창고재고금액조회' 내보내기. 헤더 2행, 3행 TOTAL(스킵), 4행~ 데이터.
  · **C열=규격=TM-NO, I열=입고수량=생산수량, J열=입고금액(원)→÷1000=생산금액(천원)**
  · 파일명: 구분(1part/2part)=파트, 시작~종료=생산일자
  · 규격 빈 행/소모품·상품·수량0 행 스킵. production에 파트도 저장(마스터 미등록 규격 대비)
- 추가 확정:
  1. xlsm **기타합계 → 신설 '기타' 공정** 에 배분 (불량율 집계 공정: 성형·소결·정형·가공+기타)
  2. **정형·가공(사내) 공정불량** = 직접입력 또는 Excel 업로드로 별도 입력
  3. **공정별 기준단가** = 별도 초기세팅 파일(일괄) + 제품 등록 폼 공정별 단가 입력(폼 구현됨)
- 남은 소소한 확인: 생산 xlsx 기간(시작≠종료) 처리 / 기준단가 초기세팅 파일 양식
- 다음 단계(구현 예정): ingest_production_xlsx(C/I/J·파일명 파트/일자), ingest_scrap_db('해당' 필터),
  ingest_outsource_xlsm(성형/소결/기타 배분), 단가 초기세팅 import, '기타' 공정 추가,
  불량율 집계공정 플래그(성형·소결·정형·가공·기타)
- 주의: 데모 seed(gen_sample.py)는 아직 옛 4공정 기준 — 실운영은 실제 CSV import 사용.

### M7 배포 (2026-07-22 완료)
- deploy/: setup.bat, run_server.bat, _service.bat, register_tasks.bat(작업 스케줄러),
  backup_db.py/.bat(온라인 백업·30개 보관), .env.example, DEPLOY.md(전체 가이드)
- app/setpw.py: 비밀번호 변경·계정 추가 CLI
- 검증: setpw(변경/추가/목록), backup_db(온라인 백업·복원), python -m app.db/uvicorn 동작
- .gitignore: qms.db, backups/, venv/, __pycache__

## 최근 로그

### 2026-07-30 (37) — [월마감 3/8] 반별목표제 서술칸을 DB 자동집계로 대체
- 확정: PDF에서 사람이 손으로 쓰던 서술칸(발생일자/품명/발생수량/발생원인/개선대책)을
  **DB 집계로 자동 생성**한다 — 공정별 집계 불량 중 수량 상위 TM-NO를
  발생일자(기간)·TM-NO·발생수량·불량내용으로 표시. **개선대책은 데이터로 뽑을 수 없어 제외.**
- `app/report_kpi.py`: `ban_top_items(conn, m, y, mth, part, proc, top=3)` 신설.
  공정 판정은 같은 화면의 반별 ppm과 동일하게 **귀책(원인)공정 기준**(`Masters.resolve()`, §5 7-2),
  **셋팅불량 제외 공정불량만**. 반환: tm_no / name / qty / dates("6/10, 6/17" 형식, 6일 초과 시
  "외 N일") / day_count / defects(상위 3개 "불량명 수량").
- 검증(2026-06 기준, PDF p7·p8·p10·p11과 대조):
  - **2P 정형: 2206-00 / 720EA / 6/30 → PDF와 수량·날짜 정확히 일치** ✅
  - 2P 소결: 6017-00 / 975EA / 6/10·6/17·6/24·6/25 (PDF는 862EA / 6/10·6/17·6/24)
    → PDF의 862는 TOP5표(p9)상 "브레이징 불량 862"만 센 값이고, 우리 975는 그 TM-NO의
      소결귀책 **전량**이라 정의 차이. 계산 오류 아님.
  - 1P/2P 성형·정형·가공 모두 정상 산출, 2P 가공만 6월 해당 건 없음(정상).
- ⚠️ **원천 데이터 한계 2건(보고서 문구가 PDF보다 뭉뚱그려짐)**:
  1. 불량명이 원본에 `기타_불량`(폐기), `외주 소결불량`/`외주 성형불량`(외주소재)으로 뭉쳐 저장돼
     있어, PDF의 "동심도 초과"·"브레이징 불량" 같은 **구체적 불량명이 나오지 않는다**.
     세부 불량명이 필요하면 원천 시트/불량유형 마스터에 세분화가 선행돼야 함.
  2. 1PART 소결 1위가 **TM-NO `(미지정)` 1,357EA**(폐기의 공정단위 불량 `소결로_산화.이물`).
     품목을 특정할 수 없는 불량이라 "품명(품번)" 칸에 넣기 애매함 — 보고서에서 제외할지 확인 필요.

### 2026-07-30 (36) — [월마감 2/8 완료] FY26 연간실적 저장소 + XLSX 적재 로직
- 확정: FY26은 **일회성 시드**이고 FY27 이후는 시스템이 원천 데이터로 계산한다("너가 편한방식으로
  저장" 위임). → `target` 테이블과 대칭되는 단순 구조로 결정.
- `app/db.py`: 테이블 2개 신설
  - `fy_actual(fy, part, kpi, value, unit)` UNIQUE(fy,part,kpi) — FY 연간 실적(스칼라)
  - `fy_claim(fy, part, customer, amount)` UNIQUE(fy,part,customer) — 업체별 FY 클레임(만원)
- `app/ingest.py`: `ingest_fy_actual_xlsx(conn, path, fy=26)` 신설. `FY26_실적입력.xlsx`의 5시트를
  읽어 **실적→`fy_actual`, 목표→`target`(fy=26, mon=0), 업체별 클레임→`fy_claim`** 으로 나눠 적재.
  시트명·A열 라벨로 값을 찾고, 전부 UPSERT라 **재실행해도 중복 없음(멱등 확인)**.
  kpi 이름: `proc_ppm` / `ban_ppm_{공정}` / `copq_defect_amt` / `copq_claim_amt` / `copq_pct` /
  `incident_official` / `incident_unofficial`. Q-COST는 공정불량+클레임이라 저장하지 않고 계산.
- `app/init_data.py`: STEPS 마지막에 "FY26 연간실적" 단계 추가 → 초기 구축 재실행 시 자동 적재.
- **DB(qms.db)**: PDF 기준값으로 40건 적재(실적 20 · 목표 12 · 업체별클레임 8).
  기존 FY26 목표(copq 0.22/0.173, incident 3/2)는 값이 동일해 덮어써도 변화 없음을 커밋본과
  대조해 확인했고, `proc_ppm` FY26 연간목표(1P 1011 / 2P 1458)는 이번에 새로 생긴 값이다.
- ⚠️ **발견한 데이터 불일치(사용자 확인 필요)**: 보고서 p14의 업체별 클레임 금액을 더하면
  **73,107만원**인데 같은 표의 '합계' 칸은 **72,944만원**으로 163만원 차이가 난다.
  업체별 값 중 하나가 다르거나 합계에 빠진 항목이 있는 것으로 보임.
- ⚠️ `fy_claim`의 part는 현재 전부 **미지정** — XLSX의 '파트' 열이 비어 있어서다(사용자 입력 대기).

### 2026-07-30 (35) — [월마감 2/8] FY26 입력 양식 XLSX + 고객사 마스터 신설
- 목표: (a) FY26 실적을 사용자가 채울 수 있는 XLSX 양식을 templates/에 제공,
  (b) 고객 클레임의 1PART/2PART 구분 근거 확보, (c) 고객사명 표기를 통일하는 마스터 화면 신설.
- **파트 구분 근거 확인**: 생산량 Low data(`1part_*.xlsx`/`2part_*.xlsx`)의 **E열 = 주거래처**가
  고객사명이고, 파일 자체가 파트별로 분리돼 있어 고객사→파트가 결정된다. 두 파일의 고객사명
  **교집합이 0건**임을 확인(1P 37개사 / 2P 6개사) → 고객사명만으로 파트가 유일하게 특정된다.
  단 **보고서는 약칭**(HTS, GMK, MBC…)을, 생산량 파일은 **정식명**(현대트랜시스 지곡, 한국지엠 보령,
  JEB…)을 쓰므로 마스터에 두 이름을 함께 둔다.
- `app/db.py`: `customer(id, name UNIQUE, short_name, part, active, memo)` 테이블 신설.
  name=정식명(주거래처와 동일하게 유지), short_name=보고서 표기명(약칭).
  `PERM_MENU_KEYS`에 `customers` 추가. **`_ensure_default_permissions()`를 "테이블이 비었을 때만
  전체 시딩"에서 "없는 (role,menu_key) 조합만 채우기"로 변경** — 앞으로 메뉴가 추가돼도 그 메뉴만
  기본 권한이 들어가고 관리자가 조정해 둔 기존 권한은 보존된다(이번에 customers가 여기 해당).
- `app/main.py`: `import sqlite3` 추가(UNIQUE 위반 처리용). `/admin/customers` 목록(검색·파트필터),
  `/admin/customers/save`(추가·수정, 중복 이름은 에러 메시지), `/admin/customers/{id}/delete` 신설.
  `PERM_MENUS`에 고객사 마스터 추가.
- `app/templates/customers.html` 신설, `base.html` 관리 그룹에 "🏢 고객사 마스터" 메뉴 추가.
- **DB(qms.db)**: 생산량 파일에서 고객사 43건 시딩(1P 37 / 2P 6). 보고서 표기명은 PDF와 확실히
  대응되는 26건만 미리 채웠고(만도익산·HTS·GMK·ZF·BorgWarner·MBC(JEB) 등), 나머지는 공란 —
  화면에서 채우면 된다.
- **`templates/FY26_실적입력.xlsx` 신설**: 6시트(안내 / 1_공정불량율 / 2_반별목표제 / 3_COPQ /
  4_고객품질ISSUE / 5_클레임_업체별). 노란칸=입력, 회색칸=자동계산(Q-COST·합계는 수식).
  **PDF에서 읽어낸 FY26 값을 미리 채워둠** — 사용자는 확인하고 틀린 것만 고치면 된다.
  5_클레임_업체별의 '파트' 열만 비워둠(PDF에 파트 구분이 없어 사용자 입력 필요).
- 검증: Playwright로 사이드바 메뉴 노출 → 목록 43건 → 2PART 필터 6건(정식명·표기명·파트 정상)
  → 검색 'HTS'로 현대트랜시스 2곳 조회 → 추가 → 같은 이름 재등록 시 "이미 등록된 고객사명입니다"
  에러 → 수정(표기명 변경 반영) → 삭제 전 과정 확인. 테스트 데이터 잔여 0건. JS 에러 없음.
- 다음: 사용자가 `FY26_실적입력.xlsx`를 채워 올려주면 그 값을 적재할 저장소(FY 연간 실적 테이블)와
  적재 로직을 만든다. **저장 방식은 `fy_actual(fy, part, kpi, value)` 신설을 제안했고 아직 확정 전.**

### 2026-07-30 (34) — [월마감 리포트 1/8] 목표관리에 반별목표제(공정별 ppm 목표) 추가
- 배경: `templates/26년 7월 품질경영 회의록_6월마감.pdf`(16장)를 분석해 월마감 보고서 화면을
  만들기로 결정. 사용자가 8개 항목을 확정했고, 그 중 **1번(반별목표제 목표항목 추가)** 부터 착수.
  나머지 결정사항은 아래 "월마감 리포트 구현 계획" 참고.
- 확정: 반별목표제 목표는 **공정별(성형/소결/정형/가공) ppm**, **파트별로만**(통합 없음),
  **월별 변동 없이 FY 연간 목표 하나**만 쓴다.
- `app/main.py`: `BAN_PROCESSES`(성형/소결/정형/가공), `ban_kpi(proc)`→`"ban_ppm_"+proc`,
  `BAN_KPIS`, `PART_ONLY_KPIS`(=MONTHLY_KPIS ∪ BAN_KPIS) 추가. `admin_target()`의 지표 목록에
  반별목표제 4행을 붙이고 각 항목에 `part_only` 플래그를 넣어 통합 탭에서는 숨김.
  `admin_target_save()`의 units에 반별 4종(ppm) 추가, 통합 스킵 조건을 `MONTHLY_KPIS` →
  `PART_ONLY_KPIS`로 일반화. target 테이블 스키마 변경 없음(kpi 문자열만 늘어남).
- `app/templates/target.html`: 하드코딩됐던 `it.kpi in ['proc_ppm','set_ppm']` 조건을
  `it.part_only`로 일반화(앞으로 파트전용 지표가 늘어도 템플릿 수정 불필요).
- **DB(qms.db)**: PDF 기준 FY27 반별목표 8건 시딩 — 1PART 성형900/소결320/정형370/가공80,
  2PART 성형1200/소결910/정형50/가공230.
- 검증: FY27·1PART 화면에 반별목표제 4행이 PDF와 동일한 값으로 표시됨 확인, 통합 탭에서는
  0건(숨김) 확인. Playwright로 실제 폼에서 가공 80→85 수정·저장 후 재조회 시 85로 반영됨 확인
  후 80으로 원복. (curl로 한글 필드명을 POST했을 때는 저장이 안 됐는데, 브라우저 실제 폼에서는
  정상 동작 — 셸에서 한글 필드명이 다른 유니코드 정규화형으로 전달된 테스트 스크립트 문제이지
  애플리케이션 버그가 아님.)

### 월마감 리포트 구현 계획 (2026-07-30 사용자 확정, 8개 항목)
1. ✅ 반별목표제 → 목표관리에 항목 추가(월별 변동 없음) — (34)에서 완료
2. ⬜ FY26 실적: 필요한 데이터 표를 Claude가 정리해 주고, 사용자가 숫자를 채워서 그 값을 사용
3. ⬜ 공정별 화면의 서술내용은 사람이 쓰지 않고 **DB에서 자동 생성** — 공정별 집계 불량 중
   수량 상위 TM-NO를 발생일자(기간)·TM-NO·발생수량·불량내용으로 표시
4. ✅ 고객품질ISSUE: FY27은 등록이 없어 전부 0인 게 정상. Official=공식체크, Unofficial=미체크
5. ⬜ incident 입력폼에 발생위치(고객명)·불량사진·발생수량·발생원인·개선대책 추가 +
   별도 세부자료 파일 업로드 보관(추후 TM-NO별 품질이슈 통계/분석에 활용)
6. ✅ Scrap Cost 공정 구분은 기존 5버킷 그대로(압입·밴딩·후처리는 '기타'로 묶음) — 변경 없음
7. ⬜ 목차에서 (2) KPI 현황, (4) 협력업체 품질실적 제외
8. ⬜ (8) 주요 업무 진행 현황: 텍스트 입력·편집 화면 신설
- 공통 요구: 리포트 메뉴에 "월마감" 소메뉴 → 년월 클릭 → 새창에서 한 장씩(상하 스크롤 없음,
  이전/다음 버튼), 저장되면 수정 불가(마감 시점 스냅샷), 추후 PDF 다운로드 가능.

### 2026-07-29 (33) — 불량 검토(100EA↑) 화면에서 집계공정도 수정 가능하게
- 목표: 검토 대기 목록(외주소재불량·폐기불량 100EA 이상)에서 수량뿐 아니라 **집계공정(불량귀책
  공정)**도 고쳐서 반영할 수 있게 해달라는 요청. "원본데이터는 수정 안 하고 시스템 반영 시
  수정된 공정으로 집계"라는 요구는 기존 수량 수정과 동일한 구조 — 이 화면은 원래 raw 파일
  (외주소재 xlsm·폐기 scrap_data.db)을 건드리지 않고 `defect_entry`(DB 사본)만 수정하므로,
  집계공정도 같은 방식으로 추가하면 요구사항이 자동으로 충족됨.
  검토 대기 큐는 외주소재·폐기 두 소스만 대상이라(`SOURCE_LABEL` 확인, 사내불량은 이 큐를
  거치지 않음), `defect_entry.process`에 이미 집계공정 5종(성형/소결/정형/가공/기타,
  `db.AGG_PROCESSES`) 값이 직접 저장돼 있어 물리공정→버킷 변환 없이 그대로 드롭다운으로 쓰면 됨.
- `app/main.py`: `outsource_review()`가 `agg_processes=db.AGG_PROCESSES`를 템플릿에 전달.
  `review_action()`에 `process` 폼 필드 추가 — approve 시 수량·공정 둘 다/하나만/둘 다 없음
  조합을 모두 처리해 `defect_entry.qty`·`process`를 갱신(집계공정이 `AGG_PROCESSES`에 없는
  값이면 무시해 잘못된 값이 못 들어가게 방어).
- `app/templates/review.html`: "처리" 열의 반영 폼에 집계공정 select(현재 값 기본 선택) 추가,
  수량 입력과 나란히 배치.
- 검증: 테스트용 pending 행(외주소재불량, process='성형') 하나를 DB에 직접 삽입 → Playwright로
  화면에서 select를 '소결'로 바꾸고 반영 클릭 → DB에서 `process='소결', status='confirmed',
  reviewed=1`로 정확히 바뀐 것을 확인. 테스트 행은 삭제해 원상복구.

### 2026-07-29 (32) — 데이터점검 미등록 경고 뱃지를 입력자에게도 노출
- 목표: 사이드바 "데이터 점검" 메뉴의 "⚠ N건" 경고 뱃지가 관리자에게만 보이던 것을 입력자(editor)도
  보이게 해달라는 요청. "100개 이상도"(불량 검토(100EA↑) 뱃지)는 코드 확인 결과 애초에 역할
  구분 없이 `pending_count()`가 전역으로 계산되고 `{% if pending %}`도 역할 체크가 없어 이미
  입력자에게 정상 노출되고 있었음(현재 대기 건수가 0이라 안 보였을 뿐, 코드상 문제 없음) —
  별도 수정 없음.
- `app/main.py`: `render()`의 `unreg_alert` 계산 조건을 `user["role"]=="admin"` 하드코딩에서
  `has_perm(user["role"], "data_check", "view")`로 변경. 데이터 점검 메뉴를 볼 수 있는 역할이면
  (기본값: 관리자+입력자+조회자 전부 보기 가능) 경고 뱃지도 함께 보이도록 일반화했다 — 권한
  매트릭스에서 나중에 조회자의 데이터점검 보기 권한을 끄면 그 역할은 뱃지도 자동으로 안 보이게
  된다(뱃지 표시 여부가 페이지 접근권한과 항상 일치).
- 검증: editor 계정으로 로그인해 대시보드 사이드바에서 "🩺 데이터 점검⚠ 14" 뱃지가 정상 노출됨을
  curl로 확인.

### 2026-07-29 (31) — 권한 없음 접근 시 대시보드로 조용히 넘어가지 말고 팝업 표시
- 목표: (30)번에서 권한 없는 메뉴 클릭 시 그냥 대시보드로 리다이렉트되던 게 어색하다는 피드백.
  "권한이 없습니다. 품질관리팀에 문의하세요." 팝업을 띄우도록 수정.
- `app/main.py`: `FORBIDDEN_REDIRECT = "/?perm_denied=1"` 상수 신설, 권한 거부 시 사용하던
  `RedirectResponse("/", status_code=303)` 6곳(`_guard`의 `_forbidden` 케이스, `_perm_guard`,
  사용자관리·권한매트릭스 저장의 관리자 전용 체크 4곳)을 전부 이 상수로 교체.
- `app/templates/base.html`: 모든 화면이 상속하는 스크립트 블록에 JS 추가 — URL에
  `?perm_denied=1`이 있으면 `alert('권한이 없습니다. 품질관리팀에 문의하세요.')`를 띄우고
  `history.replaceState`로 쿼리스트링을 지워 새로고침해도 다시 뜨지 않게 처리.
- 검증: 관리자로 viewer의 "제품 마스터" 보기 권한을 끈 뒤, viewer 계정으로 로그인해 사이드바에서
  해당 메뉴를 클릭 → 실제로 alert 다이얼로그("권한이 없습니다. 품질관리팀에 문의하세요.")가
  뜨고 대시보드로 이동함을 Playwright로 확인. 권한 원복 완료.

### 2026-07-29 (30) — 세부지표현황 ITEM/불량유형 개편, 관리자 라벨 제거, 사용자관리 CRUD·권한 매트릭스
- 목표: ① 세부지표현황-불량유형별의 "item별"/"유형별" 이름을 "ITEM"/"불량유형"으로 변경
  ② ITEM 탭에도 유형 탭과 동일하게 일/주/월 단위 선택을 추가 ③ 월/주 선택 시 그래프를 막대그래프로
  ④ 사이드바의 "관리자" 표기 텍스트 제거 ⑤ admin 비밀번호를 qazwsx0793으로 변경, 이 화면에서
  관리자만 editor·viewer 계정을 추가/수정/삭제 ⑥ 관리자는 전기능, editor·viewer는 메뉴별 보기/편집을
  체크로 허용하는 권한 매트릭스. 사전에 사용자에게 매트릭스 적용범위(실제 작동 여부·미허용 메뉴
  처리방식·editor/viewer 기본값)를 질문해 확인 후 진행: 실제로 접근을 막고, 메뉴는 사이드바에 계속
  보이되 클릭 시 차단, 기본값은 viewer=전체 보기전용·editor=데이터입력 4개 메뉴만 편집(기존 하드코딩
  동작과 동일)으로 확정.
- `app/report_kpi.py`: `_period_setup(cy, cm, unit, date_from, date_to)` 신설(일/주/월 라벨과
  날짜→인덱스 매핑을 공통화, 기존에 `defect_trend_types`에만 있던 로직을 재사용 가능하게 추출).
  `defect_trend()`(ITEM)에 `unit` 파라미터 추가(일/주/월 모두 지원, "days"→"labels" 키 변경).
  `defect_trend_types()`(불량유형)는 `_period_setup` 재사용하도록 리팩터.
- `app/main.py`: `report_detail()`에 `d_unit` 파라미터 추가(ITEM 탭 전용, 기본 "일"). `TABS`는
  변경 없음(라벨은 템플릿에서만 변경). 사용자 관리: `PERM_MENUS`(메뉴키·라벨 13개, 사용자관리
  자체는 제외 — 관리자 전용 고정), `load_permissions()`, `has_perm(role, menu_key, level)`,
  `_perm_guard(request, menu_key, level)` 신설. `has_perm`은 dash(대시보드)를 항상 허용(로그인
  직후 갈 곳이 없어지는 걸 방지), admin은 항상 허용, 나머지는 permission 테이블 조회(없으면
  보기만 허용하는 안전한 기본값). 기존에 흩어져 있던 ~25곳의 `u["role"] != "admin"` /
  `u["role"] in ("editor","admin")` 하드코딩 체크를 전부 `has_perm()`/`_perm_guard()` 기반으로
  교체(대시보드·세부지표현황·SVP·Claim·Incident·불량검토·데이터점검·제품마스터·공정관리·
  불량유형마스터·폴더반영·마스터조회·목표관리 13개 화면 × 보기/편집). `/admin/users/save`,
  `/admin/users/{username}/delete`, `/admin/permissions/save` 신설(모두 관리자 전용, 사용자
  관리 자체는 매트릭스 대상이 아님 — admin 계정 자체도 이 화면에서 만들거나 수정 불가).
- `app/db.py`: `permission(role, menu_key, can_view, can_edit)` 테이블 신설 +
  `_ensure_default_permissions()`(테이블이 비어있을 때만 시딩 — 기존 하드코딩 동작과 동일한
  기본값으로 viewer=전체보기, editor=svp/claim/incident/oreview만 편집).
- `app/templates/base.html`: `<span class="role">관리자</span>` 뱃지 전부 제거.
- `app/templates/report_detail.html`: 서브탭 링크 라벨 "item별"→"ITEM", "유형별"→"불량유형".
  ITEM 탭 폼에 단위(일/주/월) select 추가(불량유형 탭과 동일한 UX, 일 선택시만 시작일/종료일
  노출). 두 탭 모두 그래프 `data-type`을 `d_unit`/`t_unit`이 '일'이면 "multi"(꺾은선), 아니면
  "multibar"(막대)로 전환.
- `app/static/charts.js`: `renderMultiBar(cfg)` 신설(다중 시리즈 막대 — renderMulti와 동일한
  cfg.sets/labels/names 데이터 모양을 슬롯 안에 나란히 배치하는 막대로 그림, 색상은 기존 MULTI
  팔레트 재사용). `draw()`의 타입 분기와 툴팁 바인딩(`g-mdot` hover/click)에 "multibar" 추가.
- `app/templates/users.html`: 전면 재작성 — 사용자 목록(관리자에게만 수정/삭제 버튼 노출, admin
  행은 버튼 없음), 계정 추가/수정 폼(아이디·이름·권한·비밀번호, 비밀번호는 수정 시 빈칸이면
  기존 값 유지), 메뉴별 권한 매트릭스 표(editor·viewer × 보기/편집 체크박스, 대시보드는 "항상
  허용" 고정표시, 세부지표현황은 편집 개념이 없어 편집 칸 없음).
- **DB 변경(qms.db, 커밋 포함)**: admin 비밀번호를 qazwsx0793으로 변경(기존 값은 되돌릴 수
  없으므로 별도 백업 없이 직접 UPDATE, 요청에 따른 의도된 변경). `permission` 테이블 신설 +
  기본값 26행(editor·viewer × 13메뉴) 시딩.
- 검증: 새 비밀번호로 로그인 성공·구 비밀번호 거부 확인. ITEM 탭에서 단위를 "월"로 바꾸면
  차트 `data-type`이 "multibar"로 바뀜을 확인, 불량유형 탭도 동일 확인. 사이드바 HTML에서
  `class="role">관리자` 문자열 0건 확인. 사용자 관리에서 editor 계정 추가→목록에 노출→삭제까지
  왕복 확인. 권한 매트릭스에서 editor의 SVP 편집 체크 해제 후 저장 → 실제로 editor 계정으로
  로그인해 SVP 화면은 보이지만(200 응답) 저장 버튼이 사라짐(뷰 전용) 확인. viewer의 제품마스터
  "보기" 체크 해제 후 저장 → viewer 계정으로 `/admin/products` 접근 시 "/"로 리다이렉트됨(접근
  자체가 차단됨, "메뉴는 보이되 클릭 시 차단" 요구사항과 일치) 확인. 두 토글 모두 원복 후 저장해
  기본값 상태로 복구. Playwright 콘솔 JS 에러 없음(파비콘 404 1건 무관).

### 2026-07-29 (29) — 데이터점검 경고아이콘/숨기기, 공정별단가 자동입력, 메뉴 재배치, 대시보드 주별차트·TOP5 수정
- 목표: 사용자 요청 6가지 — ① 데이터점검 메뉴에 미등록 TM-NO 있으면 경고아이콘 ② 공정별단가에
  "기준단가(최종가격)" 입력 칸 추가, 해당공정 선택 시 자동으로 배분비율대로 단가 입력 ③ 세부지표현황
  메뉴를 개요-대시보드 아래로 이동, "집계/리포트"를 "리포트"로 개명(내용은 추후 결정) ④ 데이터점검
  미등록 리스트에 "숨기기" 버튼(누르면 목록·경고아이콘에서 제외) + "숨긴 항목 보기" 체크박스로 다시
  노출 ⑤ 대시보드 KPI 카드 클릭 시 월별 차트는 바뀌는데 주별 차트가 안 바뀌던 버그 수정 ⑥ 대시보드
  "공정불량 TOP5"에 1PART/2PART 선택바를 제목 옆에 추가해 그 선택에 따라 표가 바뀌도록.
- `app/db.py`: `data_check_hidden(tm_no PK, hidden_at)` 테이블 신설(숨김 처리 저장).
- `app/main.py`:
  - `_hidden_tmnos()`/`unreg_alert_count()` 신설, `render()`에서 관리자 로그인 시 항상
    `unreg_alert`를 계산해 컨텍스트에 넣음(모든 페이지 메뉴에서 뱃지 표시 가능하도록 16개 호출부를
    다 고치지 않고 `render()` 한 곳에서 처리).
  - `_data_check()`가 각 미등록 행에 `hidden` 플래그 포함. `data_check_page()`에 `show_hidden`
    쿼리파라미터 추가(기본은 숨김 항목 제외). `/admin/data-check/hide`, `/admin/data-check/unhide`
    POST 엔드포인트 신설.
  - `build_weekly()` 전면 확장: 기존엔 COPQ 주별 비율만 계산했는데, scrap_cost/scrap_qty/proc_ppm/
    set_ppm/incident/warranty/prod_qty까지 전 지표를 주 단위로 계산하도록 확장(SVP·Warranty·claim은
    월 단위 값이라 그 주의 생산금액 비중으로 비례배분하는 기존 COPQ 방식을 그대로 확장 적용,
    incident는 날짜 범위로 직접 카운트). `build_dashboard()`의 `metrics` 리스트 각 항목에
    `wk_values`/`wk_target`을 추가.
  - TOP5: 페이지 전체 파트필터와 무관하게 VMS PART·TM PART 양쪽을 항상 계산해 `top5` 딕셔너리로
    반환(`top_part_default`는 현재 파트필터가 TM PART면 2PART, 아니면 1PART).
- `app/templates/base.html`: 세부지표현황을 개요 그룹으로 이동, "집계 / 리포트" 캡션을 "리포트"로
  변경(현재 하위 메뉴 없음, 추후 항목 추가 예정). 데이터 점검 메뉴에 `unreg_alert` 값이 있으면
  "⚠ N" 뱃지 표시.
- `app/static/qms.css`: `.nav .warn-ic` 스타일(빨간 배지) 추가.
- `app/templates/data_check.html`: 미등록 목록에 "숨기기"/"보이기" 버튼(폼 POST), 상단에 "숨긴 항목
  보기" 체크박스(체크 시 `?show_hidden=1`로 이동) 추가.
- `app/templates/product_form.html`: "기준단가(최종가격, 원)" 입력 칸 추가(폼 미제출, 순수 JS
  보조용). 입력하거나 "사용" 체크박스를 토글하면 `price_calc.py`의 배분비율(성형/소결/정형=
  기준단가×비율, 가공/기타=기준단가 그대로, 파트·정형유무별 비율은 파이썬 규칙과 동일하게 JS로
  이식)을 그대로 필드에 채운다. 채운 뒤에도 직접 수정 가능. 기존 "최근 3개월 평균단가로 채우기"
  버튼(생산실적 기반 제안)은 그대로 유지, 별개 기능으로 공존.
- `app/templates/dashboard.html`: 주별 차트에 id 부여(`wChart`/`wTitle`/`wDot`/`wVal`) 하고 KPI 카드
  클릭 JS(`select()`)에서 월별 차트와 동일하게 주별 차트도 함께 갱신하도록 수정. TOP5 영역에
  1PART/2PART 선택바(`#topPartSeg`) 추가, 클릭 시 두 `data-topgrid` 블록을 보이기/숨기기로 전환.
- 검증: 서버 기동 후 Playwright로 로그인 → 대시보드에서 "공정불량율" 카드 클릭 시 주별 차트 라벨·
  값·제목이 COPQ에서 "당월 주별 공정불량율"로 실제 변경됨 확인. TOP5에서 2PART 클릭 시 VMS 블록
  숨김/TM 블록 노출 확인. 데이터점검 메뉴에 "⚠ 14" 뱃지 노출 확인(당시 미등록 15건 중 1건 숨김
  처리 후). 숨기기 클릭 → 목록 15→14건, "숨긴 항목 보기" 체크 → 15건(숨김 표시 회색 처리)로 복귀,
  보이기 클릭 → 정상 해제 확인. 제품 수정 화면에서 기준단가 1000원 입력 + 전체 공정 체크 시
  성형500/소결600/정형800/기타1000(정형있음 규칙 0.5/0.6/0.8/1.0)로 정확히 자동 입력됨 확인.
  메뉴 순서(대시보드→세부지표현황→...→리포트(빈 캡션)→관리) 렌더 결과로 재확인. 콘솔 JS 에러
  없음(파비콘 404 1건은 무관). `python3 -c "from app import main"` 및 4개 템플릿 Jinja 컴파일
  확인 통과.

### 2026-07-29 (28) — 세부지표현황: 누계 열 + Warranty/Incident 월별화 + 불량유형별 item/유형 서브탭
- 목표: (27)번 이후 사용자가 5가지를 추가 요청 — ① Warranty/Incident에 발생·누적·목표 행 추가
  ② Scrap Cost/Quantity/COPQ/Incident/Warranty/공정불량에 4월 왼쪽 "누계" 열 추가
  ③ 불량유형별 TM-NO 자동완성(입력 시 유사 품번, 선택 시 그 품번의 불량유형만) ④ 불량유형별을
  item별/유형별 서브탭으로 분리(유형별=불량유형 복수선택+일/주/월) ⑤ 그래프 hover/click 시
  (일자,수량) 툴팁. 구현 전 질문 5개를 먼저 하고 답변 받은 뒤 진행(누계로 대체되므로 별도 발생행
  불필요, 데이터 있는 달까지 누적비율, TM-NO 2글자 이상 앞자리, 불량유형 최대 3개 선택바 3개,
  주/월 각각 최근 7개월/12개월, 라벨 "N월M주").
- `app/report_kpi.py`: `latest_actual_month()`/`_months_so_far()` 신설(생산량 데이터 있는
  마지막 달 기준). `_block_cost`/`_block_copq`/`_block_incident`/`_block_warranty`/
  `process_table()` 각각에 `"cum"` 딕셔너리 추가 — %·ppm류는 누적 분자/누적 분모로 재계산(단순
  평균 아님), 공정불량 누계 목표(ppm)만 예외로 월별 목표 평균 사용. `_block_warranty`/
  `_block_incident`는 월별 열을 FY누적 running sum에서 **그 달 발생분만**으로 변경(누계 열이
  누적 역할을 대신). `tmno_search()`(2글자 이상 접두 검색, defect_entry 기준, Part 필터 지원),
  `defect_names_for_tm()`, `defect_trend_types()`(유형별 탭: 불량유형 최대 3개, 일/주/월 단위,
  주=최근 7개월 내 실제 주차를 "N월M주"로, 월=최근 12개월) 신설.
- `app/main.py`: `report_detail()`에 `sub`/`t1`/`t2`/`t3`/`t_unit` 파라미터 추가, item별/유형별
  분기. `/report/tmno-search`, `/report/tmno-defects` JSON 엔드포인트 신설.
- `app/templates/report_detail.html`: `block_table`/`row` 매크로에 누계 열 추가, 6개 탭 전체
  반영. Incident 탭의 옛 `b.actual`(FY누적) 행 제거, Warranty 라벨 "FY누적"→월간으로 수정.
  불량유형별 탭에 item별/유형별 링크 서브탭 추가, TM-NO 입력창에 자동완성 드롭다운(JS, 2글자
  이상 debounce fetch) + 선택 시 불량유형 드롭다운 재구성, 유형별 탭 폼(불량유형 select 3개 +
  단위 select, 일 단위일 때만 시작일/종료일 노출).
- `app/static/charts.js`: `renderMulti()`에 시리즈명 표시(names), 각 점에 투명 히트서클(반경 7)
  + data-lbl/data-nm/data-val/data-col 부여. `draw()`에 멀티차트 전용 툴팁 div(mousemove/click로
  표시, mouseleave로 숨김) 바인딩 — innerHTML 재설정으로 매번 떨어져나가는 tooltip div를 다시
  붙이도록 처리.
- `docs/ARCHITECTURE.md`: 규칙 7-3(누계 열 계산방식) / 7-4(Warranty·Incident 월간화) /
  7-5(불량유형별 item/유형 서브탭·TM-NO 자동완성) 추가.
- 검증: `_block_copq`의 cum 합계를 직접 호출로 재검증(cum.total=85,205.49천원=1~4월 합, 오차
  없음). 서버 기동 후 로그인 세션으로 scrap_cost/scrap_qty/copq/incident/warranty/process1/
  process2/trend(item·type) 8개 URL 모두 200 응답, 본문에 traceback 없음 확인. 누계 열 수치가
  월별 합과 정확히 일치함을 렌더된 HTML에서 확인(예: Scrap Cost 1Part 공정불량소계 누계
  142,172=43,391+18,863+36,806+43,111). Playwright로 브라우저 구동해 TM-NO "59" 입력 시
  자동완성 8건 노출·클릭 시 입력창 채워짐과 불량유형 재구성 확인, 유형별 탭에서 불량유형 2개
  선택 시 범례 2색 분리 확인, 주 단위 라벨이 "1월1주"~"4월3주" 형태로 표시됨 확인, 차트 점에
  마우스오버 시 툴팁에 (기간, 값)이 표시됨을 확인. 콘솔 JS 에러 없음(파비콘 404 1건은 무관).
- **확인 필요(사용자 확정 아님, 판단으로 구현)**: 공정불량 탭 누계 "목표(ppm)"는 ppm 목표가
  자연스럽게 누적 합산되는 값이 아니라서 **데이터 있는 달들의 단순평균**을 썼음 — 다른 방식을
  원하면 알려주세요.

### 2026-07-29 (27) — 세부지표현황 5행 배분을 귀책(원인)공정 기준으로 수정
- 목표: (26)번에서 만든 세부지표현황의 성형/소결/정형/가공/기타 배분이 "발견공정"(예: 후처리
  시트에서 잡힌 불량은 무조건 '기타')이었는데, 사용자가 예시(598-01, 후처리 시트에서 깨짐 10개,
  성형200/소결300/정형400/후처리500원 단가)로 지적: **비용은 발견공정 단가(10×500원)로 계산하되,
  그 비용이 집계되는 행은 그 불량유형의 진짜 귀책공정(예: 성형)**이어야 한다는 것. 사전에 "이렇게
  하면 총 금액·총 수량이 바뀌냐"는 질문에 **바뀌지 않는다**(비율 배분이라 합계 보존)고 답변 후
  확정, 구현.
- `report_kpi.month_process()` 수정: 기존에는 `db.bucket_of(저장된 물리공정)`로 버킷을 단순
  매핑했는데, `Masters.resolve()`(불량유형 마스터 배분규칙 + 제품 라우팅 교집합, 기존에 이미
  있던 함수)로 얻은 `allocs=[(버킷,수량),...]`를 사용하도록 변경. 비용은 발견공정 단가로 계산한
  건별 총액(cost)을 그 수량 배분 비율대로(`cost * q/qty`) 나눠 버킷에 누적 — 각 버킷 단가를
  다시 조회하지 않으므로 총액이 정확히 보존됨(부동소수 오차 무시할 수준). raw(종합) 섹션은
  이 변경과 무관하게 발견 그대로 유지.
- `process_table()`(공정불량-1PART/2PART 탭): 기존에 1PART만 압입을 기타에서 분리해 별도
  ppm 행으로 보여주던 로직 제거 — 이제 압입 시트에서 발견된 불량도 귀책배분 결과에 따라 성형 등
  실제 원인 버킷으로 흩어질 수 있어 "압입 버킷"이라는 개념 자체가 성립하지 않음. 양쪽 파트 모두
  표준 5버킷(성형/소결/정형/가공/기타)으로 통일.
- 검증: `month_process()` 직접 호출로 raw 합계(qty=25024, cost=10,861.88천원)와 bucket 합계가
  정확히 일치함을 확인(수학적 보존 확인). Scrap Cost 탭 6월 1Part "합계"·"실적%"는 (25)번에서
  검증한 값(12,046천원/0.148%)과 동일하게 유지되면서, 성형/소결/정형/가공/기타 5행 값만 재분배됨
  확인(예: 6월 1Part 성형 6,083천원 — 이전엔 성형 294천원이었음, 정형/가공에 몰려있던 것이
  실제 귀책에 따라 성형·소결로 크게 이동). 공정불량-1PART 탭도 정상 렌더링. 서버 에러 없음.
- `docs/ARCHITECTURE.md`에 규칙 7-2로 기록(총액=7-1 발견공정 기준 유지, 5행 배분=귀책기준으로
  분리해서 명시 — 둘을 혼동하지 않도록).

### 2026-07-29 (26) — 세부지표현황을 사용자 제공 엑셀 양식(탭 구성)으로 전면 재구성
- 목표: 브랜치 templates에 올려주신 `세부지표현황.xlsx`(Scrap cost/Scrap quantity/COPQ/
  C_Incident/warranty/공정불량-1part/공정불량-2part/불량유형별 8개 탭, FY 4월~3월 월별 표)
  양식과 내용 그대로 시스템 화면에 tab 형식으로 보이게 재구성. 기존의 유연한 다축 분석기
  (KPI종합/파트별/공정별/불량유형별추이/비중/TM별/품명별 뷰 시스템)를 대체.
- 원칙: (24)(25)번에서 확정한 "Scrap Cost/COPQ는 불량을 **발견한 공정** 기준, 배분규칙 미적용"
  원칙을 이 리포트 전체에 일관 적용(엑셀 구조 자체가 외주소재불량·폐기불량을 별도 행으로 두는 등
  발견공정 기준을 전제로 하고 있어 자연스럽게 맞음).
- `app/report_kpi.py` 신규 모듈:
  - `month_process()`: 월+파트의 불량을 공정/셋팅으로 나눠 raw(입력 그대로: 성형/소결/정형/가공/
    압입/후처리/외주소재/폐기불량)·bucket(표준 5종: 성형/소결/정형/가공/기타, 외주소재는 전부
    기타로) 두 기준으로 한 번에 집계.
  - `scrap_cost_table`/`scrap_qty_table`/`copq_table`/`incident_table`/`warranty_table`:
    Total/1Part/2Part 3블록 × FY 12개월 데이터 생성. COPQ는 Scrap cost + Claim 6개 항목 +
    기타(나머지 use_agg 항목) 구조. Incident/Warranty는 이전에 확정한 대로 **FY 누적** 실적.
  - `process_table()`: 공정불량-1PART/2PART 탭 — "종합"(raw 8종/7종, EA, 집계기준) +
    "공정별"(발생기준 ppm, 1PART만 압입을 기타에서 분리해 별도 행으로 표시).
  - `defect_trend()`: 불량유형별 탭 — Part·TM-NO·불량유형·기간(시작일/종료일) 필터로 일 단위
    발생 추이(상위 8개 불량유형 라인차트) + TM-NO별 수량 순위(상위 20) 산출.
- `app/main.py`: `/report/detail` 라우트를 `tab`+`fy` 파라미터 기반으로 전면 교체(옛 `view` 기반
  다축 분석 라우트 로직 제거). `/report/kpi`, `/report/defect` 옛 링크는 각각
  `tab=scrap_cost`, `tab=process1|process2`로 리다이렉트하도록 호환 유지.
- `app/templates/report_detail.html` 전면 재작성: 상단 탭 네비게이션 + FY 선택, 탭별 표
  렌더링(Jinja 매크로로 3블록 반복 구조 공통화), 불량유형별 탭은 필터 폼 + 라인차트 + TM-NO 순위표.
- `app/static/charts.js`: `renderMulti()`가 라벨 개수만큼 전부 그리던 걸, **최대 12개만 골라
  표시**하도록 수정(일 단위 28개 이상 라벨이 겹쳐 보이던 문제 — 기존 월별 차트(≤12개)는 영향 없음).
- **버그 수정**: COPQ 탭 초기 구현에서 `b.items[it]`가 Jinja의 dict 속성 우선 해석 때문에
  `dict.items()` 내장 메서드로 잘못 resolve되어 값이 전부 비어 보이던 문제 발견 → 데이터 키를
  `items`→`copq_items`로 변경해 해결.
- 검증: 8개 탭 전부 200 응답, 서버 에러 없음. Scrap Cost 탭 1Part/2Part 6월 실적이 (24)(25)번에서
  검증한 값(12,046천원/0.148%, 25,944천원/0.451%)과 정확히 일치. COPQ 탭 Scrap cost 행이 Scrap
  Cost 탭 합계 행과 일치 확인. 불량유형별 탭에서 TM-NO 필터("1027-00" → 934건)·불량유형 필터
  ("깨짐" → 9,128건) 정상 동작 확인. Playwright로 시각 확인, 콘솔 에러 0(favicon 제외). 대시보드·
  데이터점검·제품마스터·Claim/Incident·폴더반영·불량유형마스터·목표관리·마스터조회 등 다른 화면
  전부 회귀 없음 확인.
- 참고: 기존 다축 분석 엔진(`calc.analyze()`, `calc.period_key()`, 분기/년 집계, TM-NO별·품명별
  비중 분석)은 더 이상 화면에서 호출되지 않지만 코드는 남겨둠(향후 재사용 가능성, 완성도 높은
  범용 로직이라 삭제하지 않음).

### 2026-07-29 (25) — 2PART 공정단가 배수·외주소재불량 배수 파트별 분리
- 목표: (24)번에서 남겨둔 2PART 잔여 차이(−16.8%) 처리. 사용자 확정: **2PART는 성형 0.5배,
  소결 0.8배, 외주소재불량 0.9배**로 고정(1PART는 기존 그대로: 정형있음 성형0.5/소결0.6/정형0.8,
  정형없음 성형0.6/소결0.8, 외주소재 0.8배).
- `price_calc.py`: `RATIO_TM_PART = {"성형":0.5,"소결":0.8,"정형":0.8}` 추가,
  `ratios_for(part, has_jeonghyeong)` 신설(파트별 배수 반환, 2PART는 정형유무 무관 고정값에서
  정형 있을 때만 정형 배수 포함). `compute_process_prices()`·`suggest_prices()`(제품마스터 화면
  단가제안) 둘 다 이 함수로 교체.
- `calc.py`: `OUTSOURCE_PRICE_RATIO`를 상수 0.8 → `{"VMS PART":0.8,"TM PART":0.9}` dict로 변경
  (`OUTSOURCE_PRICE_RATIO_DEFAULT=0.8` 폴백). `Masters.scrap_price()`에 `part` 인자 추가,
  `compute_daily()`/`process_breakdown()` 호출부도 함께 갱신.
- 기존 2PART 78개 TM-NO의 `product_price`(성형/소결, effective_from='2000-01-01' 기준값)를
  새 배수로 일괄 재계산해 반영(원래 그 공정이 없던 품목은 새로 만들지 않음). 1PART는 미변경 확인.
- 검증(6월): 2PART Scrap Cost 22,777 → **25,944천원(0.451%)**, 레거시 28,081천원 대비
  **−16.8% → −7.6%**로 개선. 잔여 −2,137천원 내역: 외주소재 −1,461천원(레거시 자체가 항목별로
  0.91~1.08배 섞여있어 0.9 고정으로는 완전히 못 맞춤), A8LF 가공 데이터 없음 −702천원(6월 데이터
  자체 누락), 정형 −211천원, 기타(후처리) +237천원. `/admin/products/6017-01/edit`에서 화면 단가
  제안 버튼도 새 배수(성형 8,140/소결 13,024, 기타 16,280원 기준)로 정확히 나오는 것을
  Playwright로 확인. 1PART는 −1.8%(변화 없음) 그대로. 서버 에러 없음. 작업 전 백업
  (`backups/qms_backup_..._before_tmpart_ratio_change.db`).

### 2026-07-29 (24) — Scrap Cost를 '발견공정 기준'으로 변경 + 외주소재불량 단가 0.8배
- 배경: 사용자가 기존 엑셀 방식으로 계산한 6월 실적(1PART 12,262천원/0.163%,
  2PART 28,081천원/0.494%)을 브랜치에 올려 시스템과 비교 요청. 비교 결과 시스템이
  1PART 8,418천원(−31%), 2PART 22,777천원(−19%)로 크게 낮게 나옴.
- **원인 규명**: 시스템 데이터를 그대로 두고 "입력공정 귀속" 방식으로만 재계산하니 1PART가
  12,014천원(레거시와 2% 차이)이 됨 → 차이의 94%가 **불량 배분규칙** 때문임을 확인.
  레거시는 후처리에서 발견된 불량 17,219건을 후처리 단가(1.0배)로 계산하는데, 시스템은 이를
  성형(0.5배)·소결(0.6배)로 재배분해 실패비용이 구조적으로 과소평가되고 있었음.
  추가로 단가 항목별 비교에서 레거시의 외주소재불량 단가가 완제품(기타)단가의 약 0.8배임을 확인.
- **사용자 확정 규칙(2026-07-29)**:
  1. 외주소재불량 Scrap Cost 단가 = **완제품(기타)단가 × 0.8**.
  2. 소결 배수는 **시스템 현행 유지**(정형 있으면 0.6 / 없으면 0.8) — 레거시(0.8~0.95)가 아님.
  3. Scrap Cost/COPQ는 **불량을 발견한 공정(입력공정)** 단가로 계산. 배분규칙은 비용에 쓰지 않음.
- `calc.py`:
  - `OUTSOURCE_PRICE_RATIO = 0.8` 상수 추가.
  - `Masters.scrap_price(tm_no, stored_process, source, d)` 신설 — source='outsource'면
    기타단가×0.8, 아니면 입력공정 버킷의 단가.
  - `Masters.kind_from()` 신설 — 배분 계산 없이 공정/셋팅 판정만 하는 경량 헬퍼.
  - `compute_daily()`: 비용을 allocs 순회 대신 `scrap_price()` 한 번으로 계산(발견공정 기준).
  - `process_breakdown()`: **수량은 배분규칙(원인공정) 유지, 비용만 발견공정 기준**으로 분리.
    `exclude_cost`(성형 셋팅) 처리도 이 함수에 함께 반영.
- 검증(6월): 1PART 8,418 → **12,046천원(0.148%)**, 레거시 12,262천원 대비 **−1.8%**까지 근접.
  2PART 22,777 → 23,350천원(0.406%), 레거시 대비 −16.8%로 아직 차이 있음.
- **2PART 잔여 −4,731천원 내역**(사용자 확인 대기):
  · 외주소재불량 배수 −4,055천원 — 1PART는 실제 0.794배로 0.8과 정확히 맞지만, 2PART는
    수량가중 0.95배(소결)/1.08배(성형외주)/0.91배(공정불분명)로 파트별로 다름.
    → "0.8 고정 유지" vs "파트별 차등(1PART 0.8 / 2PART 0.95)" 중 사용자 선택 대기.
  · A8LF 가공셋팅 38건·가공공정 41건이 6월 데이터에 아예 없음 −702천원
    (6월을 새 월별양식으로 다시 올리면 자동 해소).
  · 정형 단가 −211천원, 후처리 +237천원.
- **미결 사항**: 공정별 불량수량(PPM) 배분은 기존대로 원인공정 배분을 유지했다(비용만 바꾸라는
  지시였음). 그 결과 세부지표 공정별 표는 수량=원인공정, 비용=발견공정으로 열 기준이 다르다.
  수량도 발견공정으로 통일할지는 사용자 결정 필요.

### 2026-07-28 (23) — 데이터 점검 화면 신설 + 단가 규칙 자동제안 + Claim/Incident 수정 기능
- 목표(사용자 요청 3가지):
  1. 관리 메뉴에 **TM-NO 미등록 / 공정별 단가 누락**을 바로 확인하고 등록 화면으로 이동할 수 있는
     화면 신설.
  2. 제품마스터 단가 입력 시, 지난번 확정한 "정형공정 유무에 따른 적용규칙"(성형/소결/정형은
     나머지공정단가에 비율 적용, 기타는 그대로)을 적용해 성형/소결/정형/기타 값을 자동 제안.
  3. Claim/Incident 이력표에 **수정** 버튼 추가(기존엔 등록·삭제만 가능, 수정 불가했음).
- `price_calc.py`: `suggest_prices(conn, tm, months=3)` 신규 — 특정 TM-NO 하나의 최근 3개월
  production 실적으로 나머지공정단가를 추정하고, 정형 있음/없음 두 경우의 성형·소결·정형·기타
  제안값을 함께 반환(실제 라우팅 체크와 무관하게 둘 다 계산해서 화면에서 고르게 함).
- `main.py`: `_data_check()` 신규 — ①`defect_entry`/`production`에 있지만 `product`에 없는
  TM-NO(파트 추정·최근발생일·출처 포함), ②`product`에는 있지만 라우팅된 공정 중 단가가 없거나
  라우팅 자체가 없는 TM-NO를 각각 집계. `GET /admin/data-check`(관리자 전용) 신설.
  `product_new()`가 `?tm=` 쿼리파라미터로 TM-NO·파트(defect_entry/production에서 추정)를
  프리필하도록 확장. `product_new`/`product_edit` 둘 다 `price_calc.suggest_prices()` 결과를
  템플릿에 전달.
  Claim/Incident: `claim_page`/`incident_page`가 `?edit=id`로 기존 행을 불러와 폼에 채우고,
  `claim_save`/`incident_save`가 `orig_id`(hidden field) 유무로 INSERT/UPDATE 분기하도록 수정.
- `templates/data_check.html` 신규(미등록 TM-NO 표 + 단가누락 표, 각 행에 바로가기 "등록" 버튼).
  `templates/product_form.html`: 라우팅·단가 표 상단에 "최근 3개월 평균단가 추정 N원" +
  "정형 있음/없음 규칙으로 채우기" 버튼 2개 추가, JS로 클릭 시 4개 단가 입력칸과 정형 체크박스를
  한 번에 채움(수동 수정 가능, 강제 아님).
  `templates/claim.html`/`incident.html`: 등록 폼이 edit_row 있으면 "OO 수정" 제목+기존값 프리필+
  hidden orig_id, 이력표에 "수정" 링크(`?edit=id`) 추가.
  `templates/base.html`: 사이드바에 "🩺 데이터 점검"(관리자) 메뉴 추가.
- 검증: `/admin/data-check`에서 미등록 TM-NO 14건 정상 표시(파트 추정·최근발생일·출처 포함),
  단가누락 0건(현재 라우팅된 항목은 모두 단가 있음). `/admin/products/new?tm=1634-01` 접속 시
  TM-NO·파트(1PART) 프리필 확인. `/admin/products/044-04/edit`에서 "정형 있음 규칙으로 채우기"
  클릭(Playwright) → 성형 109/소결 131/정형 174/기타 218원 정확히 채워지고 정형 체크박스도
  자동 체크됨을 확인(추정 나머지단가 218원 × 0.5/0.6/0.8/1.0). Claim·Incident 각각 테스트 등록→
  수정(내용 변경)→행 수 그대로(중복 안 생김, UPDATE 확인)→삭제로 정리. 전체 페이지 200 응답,
  서버 에러 없음. 작업 전 백업(`backups/qms_backup_..._before_datacheck_edit_features.db`),
  테스트 전후 행수 대조로 실데이터 영향 없음 확인.

### 2026-07-28 (22) — (사용자가 서버에서 실행한) 폴더 반영 결과 DB 분석
- 사용자가 실제 서버 공유폴더에 7월 데이터를 올리고 "폴더 반영"을 실행한 뒤 그 결과 `qms.db`를
  브랜치에 업로드 → 이번 세션에서 만든 로직들이 실제 데이터로 올바르게 동작하는지 전수 점검.
- 확인 결과(전부 정상): 성형 공정불량 0건(미반영), 성형 셋팅불량 1,052건 전부 exclude_cost=1,
  2파트 가공은 신규 등록한 OP-70~100/설비Error/기타로, 1파트 가공은 기존 표준 불량명으로 정확히
  구분 반영, 외주소재불량 세부 불량명 정상(뭉친 이름 없음)+애매 5종 TM-NO별 정형/소결 정상 분배+
  TM-NO없는 행 0건, 폐기불량 기타_불량→remark 치환 정상, 생산량 토·일 없이 평일만 분포, freshness
  설정값도 파일명 종료일 기준으로 정상 갱신, 생산량 컷오프(7/27) 이후 폐기/외주 데이터는 지표에서
  자동 제외됨을 확인, 6월 이전 데이터는 전혀 안 건드려짐(과거월 잠금 정상).
- 부가 확인: 사용자가 4~6월 실제 Claim 데이터 9건을 입력해둔 것을 발견 — 지난번 지적된
  "COPQ=Scrap Cost" 문제가 실제로 해소된 것 확인(6월 VMS PART COPQ 17,415천원 vs Scrap Cost
  8,418천원으로 정상 차이).
- 참고(버그 아닌 데이터 품질 이슈로 안내): 7월 데이터 중 TM-NO 8개가 제품마스터에 미등록
  (6023-01, 2033-00, 1907-00, 1641-00, 670-02, 1634-01, 1949-00 등) — 이게 계기가 되어 바로 다음
  (23번, 데이터 점검 화면)으로 이어짐.

### 2026-07-28 (21) — 폴더 반영 시험을 위해 qms.db의 7월 데이터 전체 삭제
- 목표: 사용자가 서버 공유폴더(01_사내불량·02_생산량·03_외주소재·04_폐기불량)에 7월 실데이터를
  올려두고, 이번 세션에서 만든 신규 로직(월별 사내불량 양식, 생산량 평일분배, 외주/폐기 세부화,
  과거월 잠금 등)이 폴더 스캔으로 실제 잘 인식되는지 시험하려 함. DB는 이 저장소의 `qms.db`를
  직접 수정해 다운로드해서 서버에 쓰는 방식으로 진행(원격 서버·공유폴더는 이 세션에서 직접 접근
  불가하다는 점을 사용자에게 안내).
- 삭제 범위(사용자 확정, 4개 소스 전체): `defect_entry`의 `source='direct'`(사내불량)·
  `'outsource'`(외주소재불량)·`'discard'`(폐기불량) 중 `d BETWEEN '2026-07-01' AND '2026-07-31'`,
  그리고 `production`의 같은 기간. 생산량 freshness 캐시(`setting.prod_last_file_end`)도 함께
  리셋(재스캔 시 자동 갱신됨).
- 삭제 결과: 사내불량 2,241건, 생산량 590건, 외주소재불량 183건, 폐기불량 39건 — 총 defect_entry
  2,463건 + production 590건 삭제. 삭제 후 잔여 0건 확인, 6월 이전 데이터는 그대로(diff 검증 완료).
- 검증: 삭제 후 `/`, `/report/detail`, `/admin/scan` 전부 200 응답, 서버 에러 없음(7월 데이터가
  없어져도 이전 달 기준으로 정상 렌더링). 작업 전 백업
  (`backups/qms_backup_..._before_july_purge_for_folder_test.db`).
- **다음 단계(사용자 몫)**: 이 커밋의 `qms.db`를 서버 PC에 받아 교체 → `/admin/scan`에서
  "폴더 반영" 실행 → 7월 데이터가 새 로직대로 재적재되는지, 대시보드 수치가 정상 복원되는지 확인.

### 2026-07-28 (20) — 생산량 미등록 날짜의 불량은 지표에서 자동 제외
- 목표: 생산량 데이터가 아직 등록되지 않은 날짜에 입력된 불량이 지표(PPM·COPQ·Scrap Cost 등)에
  섞여 분모(생산량) 없이 반영되는 문제 방지 — **지표는 파트별로 생산량이 등록된 날짜까지만**
  반영되게 하고, 생산량이 나중에 등록되면 자동으로 반영되도록 확정.
- `calc.compute_daily()`: 함수 시작에서 파트별(`VMS PART`/`TM PART`) `production` 테이블의
  `MAX(d)`를 조회해 컷오프 날짜로 삼고, 불량 집계 루프에서 그 파트의 컷오프보다 이후 날짜의
  불량은 `continue`로 건너뛴다(생산량이 아예 없는 파트는 컷오프가 없어 전부 제외). 모든 KPI 카드가
  `compute_daily()` → `month_kpi()`를 거치므로 이 한 곳 수정으로 전체 지표에 일괄 적용됨.
- 검증: 사본 DB에서 VMS PART의 최신 생산량 행(7/31)을 삭제해 컷오프를 6월 말로 되돌린 뒤 재계산 —
  7월 VMS PART의 `scrap_qty` 합계가 정확히 0으로 떨어짐(모든 7월 불량이 제외됨)을 확인. 실제
  운영 DB는 두 파트 모두 생산량·불량 데이터가 7/31까지 동일하게 채워져 있어 **현재 지표 값에는
  영향 없음**(경계일 자체는 포함되는 것도 별도 확인). 서버 에러 없음.

### 2026-07-28 (19) — 데이터 최종입력일(freshness) 판단 기준 정리 — 생산량은 파일명 종료일
- 목표: 대시보드 상단 "데이터 최종입력일" 표시가 사내불량·생산량 각각 어떻게 판단되는지 확인.
  사내불량 월별시트는 1~31일 시트가 이미 다 만들어져 있어(빈 시트도 존재), 불량 0건인 날짜와
  미입력 날짜를 구분할 신호가 시트 안에 없음(참고로 시트의 `생산수량` 컬럼도 실제로는 전혀
  채워지지 않아 이걸로도 구분 불가함을 실데이터로 확인)을 사용자에게 설명하고 처리 방향 확인.
- 확정 결론:
  - **사내불량**: 기존 방식 그대로 유지 — 여러 시트 중 **불량이 1건이라도 있는 가장 최근 날짜**를
    최종입력일로 본다(토·일 등 불량 0건인 날이 있어도 문제없음, 사용자 확인).
  - **생산량**: DB에 실제로 적재된 날짜(`MAX(d) FROM production`) 대신, **파일명에 적힌 종료일
    자체**를 기준으로 삼는다. 평일만 분배하는 로직 때문에 종료일이 토·일이면 실제 적재된 마지막
    날짜가 그보다 앞당겨질 수 있어, 이 둘을 분리해야 함.
- `ingest.py`: `ingest_production_xlsx()`가 파일 처리 시마다 파일명의 종료일을
  `setting['prod_last_file_end']`(신규 키, `PROD_FRESHNESS_KEY`)에 기록(최댓값으로 갱신).
- `main.py`: `data_freshness()`의 생산량 항목이 이 setting 값을 우선 사용하고, 아직 값이 없으면
  (이 기능 이전 데이터) `MAX(d) FROM production`으로 폴백.
- 검증: 종료일이 일요일(예: 8/7(금)~8/9(일))인 합성 파일로 테스트 — 실제 적재된 생산 데이터는
  8/7(평일)까지만 반영되지만, freshness 설정값은 파일명 그대로 "2026-08-09"로 정확히 기록됨을
  확인. 테스트 데이터·설정값 정리 완료(기존 실데이터 영향 없음, 백업 대조로 확인). 서버 에러 없음.
  작업 전 백업(`backups/qms_backup_..._before_freshness_test.db`).

### 2026-07-28 (18) — 생산량 다일범위 파일을 평일 기준 균등 분배
- 목표: 생산량 파일(`1part_{시작}_{종료}.xlsx`)이 여러 날 범위로 만들어질 때, 기존 코드는
  **파일명의 시작일 하루로만** 전체 수량을 기록하고 있었음(종료일은 파일명 형식 검증에만 쓰이고
  실제로는 미사용)을 발견해 사용자에게 확인. 사용자 확정 규칙: **토·일요일을 제외하고 평일에만
  균등 분배**한다(예: 목~일 범위로 저장돼 있으면 목·금 2일로 분해, 하루짜리 파일이면 그대로 100%).
- `ingest.py`: `ingest_production_xlsx()`가 파일명에서 시작일뿐 아니라 **종료일도 파싱**해
  `_weekdays_in_range(d1,d2)`(신규)로 평일 목록을 구하고, 그 날짜들에 수량/금액을 분배.
  수량(정수)은 `calc.largest_remainder()`로 정수 배분(합계 보존), 금액(실수)은 균등 나눗셈.
  평일이 하나도 없는 범위(전부 토·일)는 데이터 유실 방지를 위해 전체 달력일로 폴백.
  하루짜리 파일(시작일=종료일)은 기존과 동일하게 100% 그대로.
- 검증: 목~일(4일) 범위 합성 파일로 실제 적재 테스트 — 토·일 제외 목·금 2일로만 반영되고,
  수량 10→5+5, 7→4+3(largest_remainder), 금액도 절반씩 정확히 분배됨을 확인. 테스트 데이터는
  적재 후 정리(백업 대조로 기존 실데이터 없었음을 먼저 확인 후 삭제). 서버 에러 없음. 작업 전
  백업(`backups/qms_backup_..._before_prod_split_test.db`).

### 2026-07-28 (17) — 주간 집계 기준을 월요일~일요일로 변경
- 목표: 대시보드 "당월 주별 COPQ" 등 주 단위 집계 기준을 확인해보니, 기존 `iso_week_of_month()`는
  요일과 무관하게 그 달 1일부터 **단순 7일씩** 끊는 방식이었음(월~일도 일~토도 아님). 사용자가
  **월요일~일요일**을 한 주로 하되, 월 경계를 넘지 않도록(주가 두 달에 걸치지 않도록) 첫 주·
  마지막 주는 달 안쪽으로 잘라내는 규칙으로 확정(예: 1일이 수요일이면 1주=수~일, 마지막날이
  화요일이면 마지막주=월~화, 다음 달은 새 1일부터 다시 시작).
- `calc.py`: `iso_week_of_month()` → `week_of_month()`로 교체. 그 달 1일이 속한 캘린더주의
  월요일(`first_monday`)과 대상 날짜가 속한 캘린더주의 월요일(`d_monday`) 사이 일수 차이를 7로
  나눠 주차를 계산(월 경계에서 자연히 잘림). 실제 날짜(2026-07: 1일=수요일)로 검증 — 1~5일(수~일)
  =1주, 6~12일(월~일)=2주, ..., 27~31일(월~금)=5주로 정확히 분리됨을 확인.
  다음 달(8월, 1일=토요일)도 독립적으로 새로 시작함을 확인.
- `main.py`: `build_weekly()`가 각 주차의 실제 시작/종료 날짜(`start`/`end`)도 함께 반환하도록
  수정(기존엔 주차 번호만 반환). "이번주 TOP5" 조회에 쓰이던 `wk_lo = (cur_week-1)*7+1` 식(고정
  7일 청크 가정, 새 주 계산과 안 맞음)을 제거하고 `weekly[-1]["start"]/["end"]`를 직접 사용하도록
  수정.
- 검증: 대시보드 렌더링에서 1~5주 라벨 정상 표시, 서버 에러 없음.

### 2026-07-28 (16) — 2파트 가공공정 불량유형 마스터 등록
- 목표: 2파트(TM PART) 가공공정 월별시트는 다른 공정과 컬럼 구성이 다름 — 셋팅불량 시트는
  `OP-70`~`OP-120`(공정 스테이션 번호), 공정불량 시트는 `치수·외관·편차·Tool마모·설비Error·
  자주검사·기타`. 이 불량유형들을 마스터에 등록하고, 셋팅/공정불량 모두 **가공공정에 100%
  반영**되도록 설정.
- 등록 전 충돌 확인: `설비Error`·`기타`는 이미 TM PART/공정불량 마스터에 "입력공정 100%"로
  등록돼 있었고, 실제 DB에는 **후처리 시트**에서 작성한 동명 데이터가 이미 들어와 있었음.
  가공 100%로 강제하면 기존 후처리분까지 가공으로 재배분되는 문제라 사용자에게 확인 →
  "입력공정 100%면 상관없다"(작성한 시트의 공정으로 자동 귀속되므로 가공 시트분은 가공으로,
  후처리 시트분은 후처리→기타로 각각 정확히 붙음. 명시적 '가공 100%' 강제가 불필요) →
  **`설비Error`·`기타`는 변경하지 않음.**
- 신규 등록 5종(공정불량) + 6종(셋팅불량), 전부 `alloc_rule=''`(입력공정 100%, 작성 시트=가공
  이므로 자동으로 가공공정에 100% 귀속):
  - 공정불량: 치수, 외관, 편차, Tool마모, 자주검사
  - 셋팅불량: OP-70, OP-80, OP-90, OP-100, OP-110, OP-120
- 검증: 등록 후 `/admin/defect-types` 화면에 정상 표시 확인, 서버 에러 없음. 작업 전 백업
  (`backups/qms_backup_..._before_2part_gagong_defect_types.db`).

### 2026-07-28 (15) — 사내불량 입력을 월별 양식(시트=일자)으로 전환
- 목표: 사내불량 입력 방식을 기존 "일별 개별 파일" 방식에서, 사용자가 실제 사용할 **월별 1파일
  (시트=해당월 일자 1~31 + 종합)** 양식으로 완전히 교체.
- 파일명 규칙(사용자 확정): `{YYMM}_{공정}_{공정불량|셋팅불량}_생산{1|2}파트.xlsx`
  (예: `2607_성형_공정불량_생산1파트.xlsx` = 2026년 7월, 성형 공정 작성, 공정불량, 생산1PART).
  실제 파일(`templates/7月 성형 공정불량_생산1파트.xlsx`, `..._셋팅불량_...`)을 openpyxl로 직접 열어
  구조 확인: 시트 `1`~`31`(일자)+`종합`, 4행=세부 불량유형 헤더(34종), 5행=TM NO./CODE(ERP)/품명 등
  기본정보 헤더, 6행부터 TM-NO별 데이터. 세부 불량유형명(폐기·소재불량·소계 제외 31종)을 기존
  `defect_type` 마스터와 전수 대조 — **전부 이름이 정확히 일치**함을 확인(단, xlsm 헤더에 셀 줄바꿈·
  공백이 섞여있어 공백을 전부 제거하고 비교해야 함. 예: "영점 및\n동심도수정" → "영점및동심도수정").
- **성형 작성 규칙(사용자 신규 확정, 기존 규칙보다 정교화)**:
  - 성형 공정 작성 + **공정불량** → D/B에 전혀 반영 안 함(파일 자체 skip). (기존 규칙과 동일)
  - 성형 공정 작성 + **셋팅불량** → D/B엔 반영해 **셋팅불량율에는 포함**하되, **Scrap Cost·COPQ
    계산에서는 제외**(신규 규칙 — 기존엔 성형 작성분은 공정/셋팅 구분 없이 전부 미반영이었음).
- **폴더 구조(사용자 확정)**: `01_사내불량\{연도:2026}\{1파트|2파트}\{월:7월}\` 아래에 공정별
  하위폴더 없이 파일들이 섞여서 놓임. 재귀 스캔.
- `db.py`: `defect_entry`에 `exclude_cost`(기본 0) 컬럼 추가(SCHEMA + `_add_col` 둘 다).
- `calc.py`: `compute_daily()`가 `exclude_cost=1`인 행은 `scrap_qty`/`proc_qty`/`set_qty`(불량율
  집계)는 그대로 누적하되, `scrap_cost`/`scrap_cost_copq` 누적은 건너뛰도록 수정.
- `ingest.py`: `parse_monthly_defect_filename()`, `ingest_monthly_defect_file()`(파일 1개, 성형
  규칙 적용, 일자별 batch_key로 idempotent 재적재), `ingest_monthly_defect_folder()`(재귀 스캔)
  신규 추가. 기존 일별 양식 함수(`ingest_daily_defect_file` 등)는 과거 데이터 호환용으로 코드는
  남겨두되 더 이상 스캔 파이프라인에서 호출하지 않음.
- `scan.py`: `scan_defect()`가 `ingest_monthly_defect_folder()`를 호출하도록 교체, 폴더 구조 설명
  주석 갱신.
- 검증: 실제 파일을 새 파일명 규칙으로 복사(`2607_성형_공정불량_생산1파트.xlsx`,
  `2607_성형_셋팅불량_생산1파트.xlsx`)해 실제 적재 테스트 — 공정불량 파일은 0건(미반영 확인),
  셋팅불량 파일은 988건 적재되고 전부 `exclude_cost=1`로 표시됨. "영점및동심도수정" 등 공백이
  섞인 헤더도 정상 매칭됨. `calc.compute_daily()`로 특정 행(TM 044-04, 치수조정, 성형, 단가 109)이
  실제로 비용에서 제외되는 것을 개별 계산으로 직접 검증(제외 안 했으면 1.308천원 발생했을 비용이
  0으로 처리됨). 대시보드/검토화면/제품·불량유형 마스터 전부 200 응답, 서버 에러 없음. 작업 전
  백업(`backups/qms_backup_..._before_monthly_defect_test.db`).
- **참고**: `templates/` 폴더의 실제 업로드 파일은 옛 이름(`7月 성형 공정불량_생산1파트.xlsx`)
  그대로 두었음(포맷 확인용 샘플). 실제 운영에서는 서버 공유폴더에 새 파일명 규칙으로 올려야
  자동 스캔이 인식한다.

### 2026-07-28 (14) — 폐기·외주소재불량 세부화 + 과거월 데이터 잠금
- 목표(사용자 질문 3가지에 대한 구현):
  1. 폐기(scrap_data.db)·외주소재(xlsm) 모두 **이번 달 이전(지난달까지) 데이터는 재스캔해도
     수정되지 않도록 잠금**(원본이 사후에 바뀌어도 DB 값 유지).
  2. 폐기에서 불량명이 `기타_불량`이면, `remark`(비고)에 적힌 실제 사유를 불량명으로 표시
     (접두어 없이 remark 내용만) — TOP5 등 집계에도 세부 사유 그대로 반영.
  3. 외주소재불량(xlsm)도 성형/소결/기타 3개 뭉친 합계 대신 **19개 세부 불량명 컬럼**을 그대로
     불량명으로 반영. `00_sintering_defect.xlsm`의 VBA(Module1)를 olevba로 직접 열어 그룹화 공식을
     확인한 뒤 반영: 깨짐·크랙·기포·뜯김·밸런스NG→성형, 변형(촥좌)·변형(Window)·변형(쏠림)·
     브레이징·조립불량·변형(편가공)·산화·용침불량·소결붙음→소결로 직접 매핑. 원래 VBA가 소결/기타에
     50%씩 임의로 나누던 애매한 5종(찍힘.눌림/이종혼입/녹/이물소착/소재돌출)은 **TM-NO의 제품
     라우팅에 정형이 있으면 정형, 없으면 소결**로 배분(사용자 확정 규칙). TM-NO 없는 행(제품
     미지정/날짜 오인식)은 라우팅 판단이 불가능하므로 **집계에서 제외**(사용자 확정).
  - 참고: 2026년 7월까지는 사용자가 `qms.db`를 직접 수정하고, 8월부터 시스템 매일 입력이 본격
    시작되므로, "당월 이전 잠금" 규칙은 8월 이후부터 자연스럽게 의미를 갖는 구조로 그대로 반영.
- `ingest.py`:
  - `_reingest_preserve_reviewed()`: 이번 달 1일 이전 날짜의 신규 행은 통째로 무시(DB값 유지),
    reviewed=0이면서 이번 달 이후인 기존 행만 삭제 후 재적재. 반환에 `locked`(잠금 건수) 추가
    (3-tuple로 변경, 호출부 2곳 갱신).
  - `ingest_scrap_db()`: `remark` 컬럼 SELECT 추가, `scrap_reason='기타_불량'`이고 remark가
    있으면 remark를 불량명으로 사용.
  - `ingest_outsource_xlsm()`: `_OUTSOURCE_SUMS`(3개 합계) 방식 제거, `_OUTSOURCE_DETAIL_DIRECT`
    (14개 직접매핑) + `_OUTSOURCE_DETAIL_AMBIGUOUS`(5개, `product_route`에 정형 존재 여부로 정형/
    소결 결정, TM별 캐시) 로 교체. TM-NO 없는 행은 스킵(`skip_no_tm` 카운트). 반환값에 스킵건수
    추가(4-tuple로 변경, `scan.py` 호출부 갱신).
- 검증: 실데이터(`templates/00_sintering_defect.xlsm`, `templates/scrap_data.db`)로 실제 재적재
  테스트. 7월 이전(1~6월) 기존 행은 예전 "외주 성형불량" 등 뭉친 이름 그대로 유지(잠금 확인),
  7월 이후 행은 "깨짐"·"변형(Window)"·"찍힘.눌림" 등 세부 불량명으로 정상 반영. 애매 5종 항목이
  TM-NO별로 정형/소결로 갈라지는 것 확인(예: 6011-00 이물소착→정형, 2076-00 이물소착→소결).
  폐기 쪽 `기타_불량` 26건 전수가 "사각홀부 버", "면취부 크랙", "동심도 불량" 등 remark 내용으로
  치환됨을 확인. 재실행해도 건수 그대로(idempotent) 확인. 대시보드/TOP5/검토화면(`/input/
  outsource-review`)/Claim/Incident 전부 200 응답, 서버 에러 없음. 작업 전 백업
  (`backups/qms_backup_..._before_detail_defect_migration.db`).

### 2026-07-28 (13) — Customer Incident에 TM-NO·품명·공식/비공식 체크박스 추가
- 목표: Incident 등록란에 고객명 옆에 TM-NO·품명 입력 추가, 공식/비공식 체크박스 추가,
  **공식(체크)만 KPI에 반영**, Claim처럼 삭제 기능도 확인(이미 존재).
- `incident` 테이블에 `tm_no`·`product_name`·`is_official`(기본 1) 컬럼 추가(`_add_col`).
- `calc.incident_count()`: `WHERE is_official=1` 조건 추가 — 비공식 건은 Customer Incident
  KPI 집계에서 자동 제외.
- `main.py`: `incident_save()`가 TM-NO(`calc.base_tmno()`로 정규화)·품명·공식여부를 저장,
  신규 라우트 `/input/incident/{id}/toggle-official` 추가(Claim의 `toggle-agg`와 동일 패턴,
  이력표에서 체크박스로 즉시 토글).
- `incident.html`: 등록폼에 TM-NO·품명 입력 행, "공식" 체크박스(기본 체크) 추가. 이력표에
  TM-NO·품명·공식(토글 체크박스) 컬럼 추가, 비공식 행은 회색으로 표시.
- 검증: TM-NO·품명·공식 체크 상태로 Incident 등록 → DB에 정확히 저장 확인. 공식 체크 해제
  (toggle 라우트 직접 호출) → `calc.incident_count()`가 1→0으로 즉시 반영되는 것 확인.
  삭제 라우트 정상 동작(등록→삭제 후 DB에서 사라짐 확인). `/`, `/report/detail`,
  `/input/incident`, `/input/claim` 전부 200 응답, 서버 로그에 에러 없음, Playwright로
  화면 렌더링·JS 콘솔 오류 0 확인. 테스트 데이터 정리 완료. 작업 전 백업
  (`backups/qms_backup_..._before_incident_tmno_migration.db`).

### 2026-07-28 (12) — Claim 이력표 컴팩트하게 (년월만, 금액 천원 단위)
- 목표: 컬럼이 많아져(TM-NO·품명 등) 이력표가 좁아 보임 → 날짜는 년월까지만,
  금액은 천원 단위로 표시해 폭을 줄인다.
- `claim.html` 이력표만 변경(등록 폼의 날짜picker·원단위 입력은 그대로): 날짜 `{{ r.d[:7] }}`로
  년월만 표시, 전표금액·Re-claim·집계금액을 ×1000 없이 그대로(DB가 이미 천원 저장) 표시,
  헤더도 "(원)"→"(천원)"으로 변경.
- 검증: 전표 500천원·Re-claim 50천원 등록 → 이력표에 "2026-07 / 500 / 50 / 450(집계금액)"으로
  표시됨을 확인, 원 단위 대비 자릿수가 줄어 표가 눈에 띄게 좁아짐. 서버 렌더링 정상.

### 2026-07-28 (11) — Claim에 TM-NO·품명 추가 + Warranty/Incident 카드를 FY 누적으로 변경
- 목표: ① Claim 입력에 TM-NO·품명 필드 추가. ② Claim에서 항목=Warranty로 등록한 건이
  Warranty 지표에 반영되는지 확인/보장. ③ Warranty·Incident의 "목표"는 FY 누적합계라는
  사용자 설명에 따라, 대시보드 카드 값도 월별 단일값이 아니라 **FY 시작(4월)부터 당월까지
  누적**으로 바꿔 목표와 실제 비교가 되게 한다.
- ① `claim` 테이블에 `tm_no`·`product_name` 컬럼 추가(`_add_col`), 등록폼·이력표에 노출.
  TM-NO는 다른 화면과 동일하게 `calc.base_tmno()`로 정규화해 저장.
- ② 확인 결과 **이미 자동으로 연결돼 있었음** — `calc.month_kpi()`가
  `claim_sum(..., ["Warranty"])`로 그 달 Warranty 항목 합계를 그대로 Warranty 카드 값에
  쓰고 있었다(새로 만들 필요 없이 기존 로직이 커버). 실제로 Warranty 클레임 50만원 등록 후
  카드가 0→500(천원)으로 바뀌는 것 확인.
- ③ `main.py build_dashboard()`: `calc.fy_months(cur_fy)`로 FY 전체 월 목록을 구해 당월까지의
  구간만 잘라, 그 구간의 `claim_sum(...,["Warranty"])`/`incident_count()`를 각각 합산해
  `cards.warranty.val`/`cards.incident.val`로 사용(기존 "전월대비"는 당월 단독 증감 그대로 유지
  — 누적이 이번 달에 얼마나 늘었는지를 보여주는 의미로 그대로 둠). 카드 라벨에 "FY 누적"
  배지 추가해 월별 값이 아님을 명시.
- 검증: Warranty 클레임 1건(50만원) 등록 → Warranty 카드 0→500(목표 1,000 대비), COPQ 카드도
  동반 상승(19,966→20,466천원) 확인. Customer Incident 카드에도 "FY 누적" 배지 정상 표시.
  TM-NO·품명 입력 후 이력표에 정확히 표시됨을 Playwright로 확인. 서버 렌더링·JS 오류 0.
  작업 전/후 백업(`backups/qms_backup_..._after_claim_tmno_migration.db`).

### 2026-07-28 (10) — Claim 입력 개선: 원 단위, 전표금액/Re-claim 분리, 집계포함 체크박스
- 목표: Claim 화면 후속 개선 4가지.
  1. 입력 단위를 천원→원으로.
  2. 전표금액·협력사 Re-claim을 각각 입력받고, 집계(COPQ 등)는 **전표금액−Re-claim**으로
     계산. Re-claim도 이력 보존.
  3. 이력표에 "집계포함" 체크박스 추가 — 체크된 건만 집계에 반영.
  4. 날짜 달력 선택(이미 `<input type="date">`라 브라우저 기본 달력 제공 중, 추가 조치 불요),
     "기타 항목명" 라벨의 "(항목=기타일 때)" 부연설명 삭제.
- `claim` 테이블에 `reclaim`(Re-claim 금액, 천원 저장)·`use_agg`(집계포함, 기본 1) 컬럼 추가
  (`_add_col`, 기존 빈 테이블이라 마이그레이션 위험 없음).
- `calc.claim_sum()`: `SUM(amount)` → `SUM(amount-reclaim)` + `WHERE use_agg=1` 조건 추가.
- 화면: 입력은 원 단위로 받아 저장 시 ÷1000(다른 COPQ 계산과 단위 일치), 이력표는 ×1000해서
  전표금액·Re-claim·집계금액(전표−Re-claim, 굵게 강조) 세 칸을 모두 원 단위로 표시.
  집계포함 체크박스는 `onchange`로 즉시 토글 저장(`/input/claim/{id}/toggle-agg`).
- 검증: 전표 1,000,000원·Re-claim 200,000원 입력 → DB 800(천원) 저장, `claim_sum()`도
  800(=80만원) 정확히 계산. 체크박스 해제(직접 라우트 호출로 확인) → `claim_sum()` 즉시 0으로
  반영 확인. 서버 렌더링·JS 오류 0. 작업 전/후 백업
  (`backups/qms_backup_..._after_claim_reclaim_migration.db`).

### 2026-07-28 (9) — Claim·Customer Incident를 엑셀 업로드 → 건별 직접 등록으로 전환
- 목표: Claim 입력화면의 엑셀 업로드를 없애고 직접 등록(년월일·파트·고객명·항목·금액·내용)
  + 하단 이력표로 바꾼다. 사용자 확인 후 Customer Incident도 동일하게 전환.
- **스키마 변경**: `claim`이 기존엔 `(년월,파트,항목)` 유니크의 **월별 집계** 테이블이라
  같은 달·같은 항목을 두 번 넣으면 서로 덮어썼음. `incident`와 같은 **건별 원장** 방식으로
  변경 — `ym`→`d`(전체 날짜), `customer`·`content` 컬럼 추가, UNIQUE 제약 제거(여러 건 누적
  가능). `db._migrate_claim()`: 기존 월별 데이터는 각 월 1일로, 고객명/내용은 빈칸으로 이관
  (실제로는 claim 데이터가 비어있어서 이관 대상 0건).
- `calc.claim_sum()`: `WHERE ym=?` → `WHERE d LIKE ?`(년-월 접두 매칭)로 변경, incident_count와
  동일한 패턴. COPQ 계산(claim_total, warranty)에 영향 없음을 검증.
- **항목(item) 드롭다운**: COPQ 계산에 실제 쓰이는 6개 고정 항목(Warranty·3rd Party
  Containment·Quality Special Freight·Customer Incident Cost·Unplanned Inspection &
  Sorting·Variance) + "기타"(선택 시 직접입력 칸 활성화, JS로 토글) — 오타로 COPQ 집계에서
  누락되는 걸 방지.
- 새 화면: `/input/claim`(`claim.html`), `/input/incident`(`incident.html`) — 상단 등록 폼 +
  하단 이력 표(삭제만 지원, 수정은 삭제 후 재등록). `/input/outsource-review`도 기존 범용
  `/input/{page}` 라우트에 얹혀 있었어서 전용 라우트로 분리.
- **정리**: 이제 쓸모없어진 범용 엑셀업로드 라우트(`/input/{page}`, `/input/{page}/upload`,
  `INPUT_PAGES`, `_recent_rows()`, `app/templates/input.html`)와 `ingest.ingest_claim()`
  전부 삭제(dead code 방지). `seed.py`는 Claim 시드를 건너뛰도록 수정(직접입력 전용이라
  샘플 엑셀 적재 대상에서 제외), `ingest_incident`는 seed.py용으로 유지.
- 검증: Playwright로 Claim 등록(일반 항목/기타 항목 직접입력 둘 다) → DB 값 정확히 일치,
  삭제 확인. Incident 등록도 확인. 대시보드 프레시바에 "Claim 2026-07-16" 정상 반영.
  COPQ/Warranty 카드 계산 크래시 없음 확인. 전 화면(대시보드·세부지표·SVP·불량검토·마스터조회)
  렌더 정상, JS 오류 0. 작업 전/후 백업
  (`backups/qms_backup_20260728_..._after_claim_schema_migration.db`, claim 테이블이
  원래 비어있어 마이그레이션으로 인한 데이터 손실 없음 확인).

### 2026-07-28 (8) — KPI 카드 전체에 전월대비 표시(목표 오른쪽, 간격)
- 목표: 지난 회차에서 Warranty·공정불량율·셋팅불량율 3개에만 붙였던 "전월대비"를
  **KPI 카드 8개 전부**에 붙이고, 위치를 **목표 값 오른쪽**(간격을 두고)으로 통일.
- `main.py`: `cards.incident`·`cards.prod_qty`에 delta(전월 대비 증감) 필드 추가
  (기존엔 이 둘만 delta가 없었음). Scrap Cost/Scrap Quantity/COPQ는 delta가 이미
  계산돼 있었는데 화면에 표시만 안 하고 있었음.
- `dashboard.html`: `mom(delta, dec)` Jinja 매크로로 "전월대비 ▲/▼ N" 렌더링을 통일.
  Warranty·공정불량율·셋팅불량율은 기존에 전월대비가 목표보다 **앞**에 있던 걸 목표
  **뒤**로 재배치. `.mom{margin-left:10px}` 로 목표와 전월대비 사이 간격 확보.
  증감이 0이면 숫자 없이 "-"만 표시(예: Customer Incident).
- 검증: 서버 기동 후 스크린샷으로 8개 카드 전부 "목표 X · 전월대비 ▲/▼/-" 순서·간격 확인,
  1PART/2PART 필터 전환 정상, JS 오류 0.

### 2026-07-28 (7) — 대시보드 화면 정리 (프레시바 1줄·KPI제목 서식·TOP5 PPM·전월대비 라벨)
- 목표: 대시보드 상단부 4가지 다듬기.
  1. 데이터 최신 반영일 바(freshbar) 글자를 줄여 한 줄로 표시.
  2. SCRAP COST 등 KPI 카드 제목을 굵게 + 첫 글자만 대문자로.
  3. 공정불량 TOP5에 불량율(PPM) 컬럼 추가.
  4. 공정불량율·셋팅불량율의 ▲▼ 숫자가 뭘 의미하는지 불명확 → 명확히 표시.
- ①: `.freshbar` 폰트 11.5px→7.5px, `white-space:nowrap`+`overflow-x:auto`로 한 줄 고정.
  "데이터 없음" 표기를 "-"로 줄여 공간 확보.
- ②: `.kpi .lb`의 `text-transform:uppercase` 제거하고 `font-weight` 600→700, 라벨 텍스트 자체를
  문장체로 수정(Scrap Cost→Scrap cost, Scrap Quantity→Scrap quantity, Customer Incident→
  Customer incident). COPQ는 약어라 그대로 둠.
- ③: `calc.top5_defect()`가 TM-NO별 ppm(불량/생산×100만)을 함께 반환하도록 수정,
  대시보드 TOP5 표에 "불량율(PPM)" 컬럼 추가(생산량 0인 경우 '-').
- ④: ▲▼ 숫자는 **전월 대비 증감**이었음(목표 대비가 아님) — `main.py`의
  `delta = cur[kpi] - prev[kpi]` 확인. 화면에 "목표 X ppm"과 나란히 있어 혼동 소지가 있었으므로,
  Warranty·공정불량율·셋팅불량율 카드에 **"전월대비"** 라벨을 화살표 앞에 추가해 구분.
- 검증: 서버 기동 후 대시보드 스크린샷으로 4가지 전부 육안 확인(프레시바 한 줄 완성,
  KPI 제목 문장체, TOP5 PPM 표시, 전월대비 라벨), JS 오류 0.

### 2026-07-28 (6) — 불량유형 마스터 관리 화면 신설 (수정·삭제·추가 + 배분기준 표준입력)
- 목표: 마스터조회에 읽기전용으로만 있던 불량유형 마스터를 CRUD 가능하게 만들고,
  배분기준을 자유 텍스트 대신 정해진 방식(체크박스+비율)으로만 입력하게 한다.
- 확정 방식(문답): 집계공정 5종(성형·소결·정형·가공·기타) **체크박스 + 비율(%) 입력**.
  비율을 전부 비우면 균등배분, 하나도 안 고르면 "입력공정 100%"(시트에 기록된 공정 그대로).
  체크+비율 방식이 기존 "발생공정" 단일필드를 완전히 포괄하므로(단일 체크=옛 발생공정과 동일)
  **발생공정 입력란은 화면에서 제거** — DB 컬럼 자체는 과거 데이터 호환을 위해 유지하고,
  기존에 발생공정만 있던 행은 수정 화면을 열면 해당 공정 1개가 자동으로 체크된 상태로
  보이도록 처리(수정 저장 시 새 형식(alloc_rule)으로 자동 이관됨).
- 신규 화면 `/admin/defect-types`(`app/templates/defect_types.html`) — 상단 추가/수정 폼 +
  하단 목록(파트·구분·불량명·배분기준·수정·삭제). `main.py`에 `_compose_alloc_rule()`
  (체크박스→문자열), `_decompose_alloc_rule()`(문자열→체크박스 미리채움) 헬퍼 추가.
  사이드바에 "⚠ 불량유형 마스터" 메뉴 추가, 마스터조회 화면의 미리보기 표는 이 페이지로
  연결하는 링크로 교체.
- 확인 사항: 사용자가 언급한 "화면에 불량명이 중복되는게 많다"는 점 — DB를 직접 확인한 결과
  **완전 중복(같은 파트+구분+불량명)은 0건**. 같은 불량명이 파트가 다르거나(1PART/2PART)
  구분(공정/셋팅)이 달라 별도 행으로 존재하는 것이며, ARCHITECTURE.md에 이미 기록된
  "파트별로 배분기준이 다를 수 있어 (파트,구분,불량명)으로 키잉" 규칙에 따른 정상 데이터.
  화면에서 파트/구분 컬럼이 눈에 잘 안 띄어 중복처럼 보였던 것으로 판단, 목록 정렬을
  파트→구분→불량명 순으로 바꿔 시각적으로 묶어서 표시.
- 검증: 추가(비율 배분·균등배분·입력공정 100% 세 케이스 전부)·수정(미리채움 정확성)·삭제를
  Playwright로 실제 폼 조작해 확인, `calc.parse_alloc_rule()` 파싱 결과까지 일치 확인.
  테스트로 만든 항목은 검증 후 정리. 서버 기동 후 대시보드·세부지표·마스터조회 렌더 정상,
  JS 오류 0.

### 2026-07-28 (5) — 라우팅(정형/압입/가공/성형/소결) 전체 재정비 규칙 적용
- 목표: 지난 회차에서 "단가로 라우팅을 역산하면 안 된다"고 정리했던 문제를, 사용자가 훨씬
  더 정밀하고 신뢰 가능한 규칙으로 다시 정의해서 전체 품목(2,690개)에 적용.
- **확정 규칙**(사용자 확정, 문답으로 세부조건까지 확인):
  1. **정형**: 정형단가가 0이거나 **기타단가와 같으면** 정형공정 없음 → 라우팅에서 제거
     (기타와 같아서 제거하는 경우 정형단가도 0으로 변경). 0도 아니고 기타와도 다른데
     라우팅에 없으면 → 추가.
     (이 규칙이 지난번 문제를 정확히 비켜간다: `ingest_price_master`가 정형없는 품목의
     정형칸에 완제품가=기타가를 그대로 채워넣는 방식이었는데, "정형==기타"로 걸러내면
     바로 그 폴백 케이스만 정확히 잡아낸다.)
  2. **압입**: 품명에 `ROD GUIDE·ROD GUIDE ASSY·R/GUIDE·R/GUIDE ASSY·S/YOKE·S/YOKE ASSY·
     SUPPORT YOKE·SUPPORT YOKE ASSY` 중 하나라도 포함(아포스트로피·공백 무시 매칭) →
     있으면 추가, 없으면 제거(완전 재동기화).
  3. **가공**: **1PART(VMS PART)** 이면서 품명에 "PISTON" 포함 → 있으면 추가, 없으면 제거.
     2PART는 이 규칙 대상이 아니라 건드리지 않음.
  4. **성형·소결**: 단가는 등록돼 있는데 라우팅에 없으면 → 추가만(제거 없음).
  5. 기타는 그대로 유지.
- 적용 전 미리보기로 건수 확인 후 실행, 실행 결과가 미리보기 숫자와 정확히 일치:
  **라우팅 변경 1,274개 TM-NO** — 추가(성형947·소결957·정형983·압입42·가공465),
  제거(정형52·압입1·가공0), **정형단가 0으로 변경 256건**.
  재실행 시 변경 0건으로 멱등성 확인(더 손댈 게 없을 때까지 정리됨).
- 검증: 1003-00(ROD GUIDE)→압입 추가, 009-00·1274-07(PISTON,1PART)→가공 추가,
  2852-01→정형 유지 등 샘플 육안 확인. 서버 기동 후 대시보드·세부지표·제품마스터 렌더
  정상, JS 오류 0.
- 작업 전 백업: `backups/qms_backup_20260728_035127_before_routing_rules.db`.

### 2026-07-28 (4) — 2852-01 단가 등록 + 정형 라우팅 36건 조정
- 목표: 마지막 단가 미등록 품목 2852-01(성형144·소결173·정형231·기타288) 등록, 그리고
  "등록된 단가를 기준으로 모든 아이템의 라우팅을 조정해달라"는 요청 처리.
- 작업 전 백업: `backups/qms_backup_20260728_033501_before_routing_adjust.db`.
- 2852-01 단가 등록(가공·기타 288 동일) → 단가 완전 미등록 품목 0건.
- **"단가 기준 전체 라우팅 조정"은 두 차례 검증 후 범위를 크게 좁혔다** —
  단가 존재 여부로 라우팅(정형/기타 유무)을 역산하려 했으나, 이 시스템의 두 주요 단가
  소스가 모두 신뢰할 수 없는 신호였음을 발견:
  1. `기타 단가 존재` → `price_calc.py`가 정형 유무와 무관하게 **모든 계산 대상에 기타단가를
     항상 채우므로** 신호가 아님(1,712건 오탐).
  2. `정형 단가 존재`(전체 스캔) → 기존 `ingest_price_master()`가 정형공정이 없는 품목도
     완제품단가를 정형칸에 채워 넣는 방식이라 역시 신뢰 불가(1,398건 오탐, 사용자 확인 후 폐기).
  → **사용자 확인**을 거쳐, 지난 회차(단가누락정리.xlsx)에서 이미 신뢰성 있게 확인했던
  **36건만** 정형 라우팅 조정(34건 정형 추가, 2건 정형 제거)으로 범위를 확정.
- 검증: 36건 전부 파일↔DB 정형 유무 일치 확인. 서버 기동 후 대시보드·세부지표·제품마스터
  렌더 정상, JS 오류 0.
- 교훈(기록): 단가 존재 여부로 라우팅을 역산하는 방식은 이 시스템에서 **일반적으로 쓰면
  안 됨**(위 두 가지 구조적 이유). 라우팅을 대량 정비하려면 원본 CSV/ERP 라우팅 데이터가
  따로 필요.

### 2026-07-28 (3) — 단가 누락 189개 TM-NO 일괄 보정 (templates/단가누락정리.xlsx)
- 목표: 사용자가 브랜치 templates 폴더에 올린 `단가누락정리.xlsx`(TM-NO·품명·PART·성형/소결/정형/기타단가)를
  단가마스터(`product_price`)에 반영.
- 사전 확인: 원격에서 `qms.db`가 새로 올라와 있어(브랜치에 delete→재업로드 커밋 2건) 먼저 내용을
  검증했다 — 우리가 `docs/누락_TMNO_목록.md`에 기록했던 32건 중 29건(미수금형 코드 2건·2PART
  1건 제외)이 사용자가 직접 제품마스터에 등록한 상태로 반영돼 있었다(로컬에만 있던 데이터 없음,
  순수 추가). `git pull`로 정상 반영.
- `단가누락정리.xlsx` 189행 전부 **제품마스터엔 있으나 단가가 아예 0건이던 TM-NO**와 정확히
  일치(교집합 100%) — 확인 후 진행.
- `ingest.ingest_missing_price_xlsx()` 신규: 정형단가가 0/공란이면 정형 행을 넣지 않고(정형공정
  없음으로 처리), 기타단가는 가공·기타 두 버킷에 동일하게 저장(기존 price_calc.py 규칙과 동일
  패턴), effective_from='2000-01-01'(기준값)로 저장. 189개 TM-NO 전부 반영, 파일↔DB 100% 일치
  확인. 이제 단가 완전 미등록 품목은 **2852-01(BODY) 1건**만 남음.
- 참고(별도 확인 필요, 조치 안 함): 파일의 "정형 있음/없음" 여부가 DB `product_route`의 실제
  라우팅과 다른 품목이 36건 있었음 — 대부분 DB 라우팅이 `{기타}` 하나만 있는(성형·소결도 없는)
  상태라, 그 품목들의 라우팅 데이터 자체가 부실한 것으로 보임. 가격 반영에는 영향 없어(파일의
  정형단가 값을 그대로 신뢰) 넘어갔지만, 라우팅 정비가 필요하면 별도로 요청 바람.
- 검증: 서버 기동 후 대시보드·세부지표·제품마스터(전체/미등록 필터) 렌더 정상. 작업 전 백업
  `backups/qms_backup_20260728_030551_before_missing_price_import.db`.

### 2026-07-28 (2) — 제품마스터 화면 라우팅·단가 표시를 성형/소결/정형/기타 4종으로 축소
- 목표: 제품마스터(등록/수정/목록) 화면의 공정 라우팅·단가를 5종이 아니라
  **성형·소결·정형·기타** 4종으로만 보여준다(가공·기타는 단가가 항상 같아 화면상 구분 불필요).
  집계 엔진(리포트·대시보드·불량배분규칙)은 계속 5종을 쓰는 것으로 사용자 확인(화면 전용 범위).
- `main.py`: `DISPLAY_PROCESSES = [성형,소결,정형,기타]` 신설. `_price_map_display()`로
  기타 단가(없으면 가공 단가로 폴백) 하나만 노출. 목록의 "공정 라우팅" 요약 텍스트도
  `_route_display()`로 가공→기타 치환 + 연속중복 제거해서 동일하게 4종으로 표시.
- 저장 로직(`/admin/products/save`): 화면 '기타' 단가는 내부적으로 가공·기타 둘 다 동일하게
  저장(원래 목적대로 항상 동기화). **라우팅은 주의해서 처리** — '기타' 체크 시 원래 그 제품에
  '가공' 버킷이 있었으면 가공+기타 둘 다 유지하고, 없었으면 기타만 저장한다(무조건 가공+기타를
  같이 넣으면 화면 저장만으로 없던 '가공' 버킷이 생겨, "찍힘"처럼 가공을 포함하는 불량배분규칙이
  조용히 새로 적용되는 부작용이 있어서 이렇게 막음).
- 검증: 가공 없던 제품(1987-01)·있던 제품(1018-10) 둘 다 **변경 없이 그냥 저장** 후
  bucket 집합이 저장 전후 동일함을 DB로 직접 확인(1987-01: {성형,소결,정형,기타} 유지,
  1018-10: {성형,소결,정형,가공,기타} 유지). 화면 스크린샷으로 4열 표시 확인, 서버 JS 오류 0.
- 작업 전 백업: `backups/qms_backup_20260728_(...)_before_display_process_merge.db`.

### 2026-07-28 (1) — 공정별 단가(성형/소결/정형/가공/기타) 산출 규칙 신설 + 미등록 TM-NO 기록
- 목표: ① 제품마스터 미등록 TM-NO 32건을 별도 MD로 기록. ② 공정단가 산출 규칙을 사용자가
  새로 정의(최근 3개월 입고금액/수량 기준 + 정형 유무별 비율)한 대로 구현하고, 매년 4월 자동
  재계산 시 과거 원가는 바뀌지 않게 한다.
- ① `docs/누락_TMNO_목록.md` 생성 — Low_Inventory엔 있으나 product 마스터엔 없는 32건
  (1762-02 GUIDE SLEEVE 등, 1~7월 합계·월별 상세 포함). 등록은 보류(사용자 확인 후 진행).
- ② 확정 규칙(`app/price_calc.py`, `docs/ARCHITECTURE.md` §5-8에 기록):
  - "나머지공정단가" = 최근 3개월 `production` 실적의 입고금액합÷입고수량합(자동으로 고객사별
    수량가중평균이 됨, E열/고객사 컬럼을 따로 볼 필요 없음 — production 테이블이 이미
    TM-NO 단위로 고객사 합산된 값이라서).
  - 정형 있음: 성형×0.5·소결×0.6·정형×0.8 / 정형 없음: 성형×0.6·소결×0.8. 가공·기타=나머지 그대로.
  - 정형 유무는 `product_route`로 판단(사용자 확인 완료). 최근 3개월 실적 없는 TM-NO는
    계산 대상에서 제외(기존 단가 유지, 사용자 확인 완료).
- **스키마 변경**: `product_price`에 `effective_from`(효력시작일) 추가,
  UNIQUE를 `(tm_no,process,effective_from)`으로 변경(`db._migrate_product_price`, 기존 값은
  전부 `2000-01-01`로 이관). `calc.Masters`가 (tm,proc)별 단가 **이력**을 들고 있다가
  `price_on(tm_no,proc,d)`로 그 발생일(d) 시점에 유효한(effective_from≤d 중 최신) 단가를 고르도록
  변경 — `compute_daily`·`process_breakdown`의 원가 계산 두 곳을 이걸로 교체.
  제품수정 화면 저장·단가마스터 엑셀 업로드는 계속 `effective_from='2000-01-01'`(기준값)로 저장,
  화면 표시용 `_price_map`은 "오늘 기준 유효한 값"만 골라 보여주게 수정.
- 지금 즉시 1회 적용: 최근 3개월(5·6·7월) 기준 806개 TM-NO·3,696건 계산해 기준값(2000-01-01)
  갱신. **매년 4월 1일 자동화**: `deploy/update_price.bat` + Windows 작업 스케줄러
  `QMS_PriceUpdate`(`register_tasks.bat`에 등록, 매년 4/1 03:00) → `python -m app.price_calc` →
  그 해 4월 1일부터만 적용되는 새 effective_from 행 추가.
- 검증: 미래 effective_from(2027-04-01) 시뮬레이션 삽입 후 `price_on()`이 과거 날짜엔 이전 단가,
  4/1 이후 날짜엔 새 단가를 정확히 반환 확인(테스트 행은 삭제). 서버 기동 후 대시보드·세부지표·
  제품수정(단가 표시) 렌더 정상, JS 오류 0. 작업 전 `qms.db` 백업
  (`backups/qms_backup_20260728_002737_before_price_rule.db`).

### 2026-07-27 (7) — 6월 2PART 생산수량 941천 vs 파일 825,217 오차 원인 규명·수정
- 목표: 사용자가 대시보드 6월 2PART 생산수량(941천)과 본인이 올린 `Low_Inventory/2part_6월_입고수량.xlsx`
  파일 값(825,217)이 다르다고 제기 → 원인 규명 후 조치.
- 작업 전 백업: `backups/qms_backup_20260727_234338_before_low_reload_and_tmno_rename.db`.
- **원인 3가지 복합**(백업 후 순서대로 조치):
  1. `Low_Inventory`(월별 입고수량)·`Low_data`(월별 사내불량) 폴더를 읽는 `ingest_history_inventory_xlsx`/
     `ingest_history_defect_xlsx` 함수가 **어떤 화면 버튼에도 연결 안 된 미사용(dead) 코드**였음.
     즉 지금 DB의 값은 과거 어느 시점에 이 파일들의 **이전 버전**을 스크립트로 1회성 적재한 결과였고,
     이후 사용자가 파일을 수정해도 반영할 방법이 없었음. → 1~7월 × 1PART/2PART, Low_Inventory 14개+
     Low_data 28개(공정/셋팅) 파일 **전부 재적재**.
  2. TM-NO **1987-01**이 처음엔 제품 마스터(`product`)에 미등록 상태였음. 조사 결과 1987-00과는
     생산량·단가가 계속 다른 **별개의 실제 변형 품번**으로 확인(사용자 확인 완료, 합치지 않음) →
     `product`+`product_route`를 1987-00 라우팅(성형·소결·정형·후처리) 그대로 복사해 신규 등록.
     기존 `product_price`(1987-01, 5개 공정 단가)는 이미 올바른 값이라 그대로 둠.
  3. TM-NO **1691-00**(SHAFT SPLINE)이 제품 마스터에 `TM PART`(2PART)로 잘못 등록돼 있었음.
     실제로는 1~7월 매달 `1part_입고수량.xlsx` 파일에만 나타남(2part 파일엔 한 번도 없음) →
     사용자 확인 후 `product.part`를 `VMS PART`로 정정.
  4. **버그 발견 및 수정**: `ingest_history_inventory_xlsx`/`ingest_production_xlsx`가
     `qty <= 0` 이면 스킵 → 반품 등 **마이너스 수량 조정 행이 통째로 누락**되어 파일 자체의
     TOTAL과도 어긋났음(1월 2PART: -400 조정행 누락으로 400 차이). `qty == 0`만 스킵하도록 수정.
- 검증: 재적재 후 2PART는 **1~7월 전 달이 파일 TOTAL과 정확히 일치**(1월만 미등록 데모코드
  '5003-00' 1EA 차이, 무시 가능). 서버 기동 후 대시보드(통합/1PART/2PART) 렌더 정상, JS 오류 0.
- **미해결(별도 확인 필요, 사용자에게 재질문)**: 같은 검증을 1PART에 했더니 매달
  **100만~190만EA** 규모로 파일보다 DB가 작게 나옴. 원인: `1part_입고수량.xlsx`에는 있는데
  제품 마스터에 아예 없는 TM-NO가 **16개**(1762-02·1945-01·637-01 GUIDE/SLEEVE 계열 등,
  6월 기준 합계 1,726,617EA). `TM-NO_List_VMS Part.CSV` 원본에도 이 코드들이 없어 초기
  제품 CSV 자체가 이 품목들을 놓친 것으로 보임. 라우팅·단가 정보가 없어 등록 방법을
  사용자에게 확인 후 진행 필요.

### 2026-07-27 (6) — 대시보드 최상단에 데이터 최신 반영일 표시
- 목표: 소스별로 DB에 실제 반영된 마지막 날짜를 대시보드 맨 위에 보여줘서 누락(지연) 여부를
  한눈에 알 수 있게 한다.
- 각 데이터의 적재 규칙 확인 후 소스별로 다르게 처리:
  - **일별 소스**(사내불량·외주소재불량·폐기불량·생산량, `defect_entry.d`/`production.d`) —
    서로 같은 날짜까지 채워져야 정상이므로, 4개 중 **가장 늦은 날짜(기준일)보다 뒤처진 소스만
    빨간 경고(⚠)** 로 표시. 사내불량=direct, 외주=outsource, 폐기=discard (status='rejected' 제외,
    검토대기 pending은 포함 — 실제 들어온 데이터이므로).
  - **월별 수기 입력**(SVP·Claim, `ym` 컬럼) — 참고용으로 최신 연월만 표시, 경고 없음
    (월 중에는 당연히 전월까지만 있을 수 있어서 지연 판정 대상 아님).
  - **비정기 입력**(Customer Incident, `d` 컬럼) — 발생 시에만 기록되므로 참고용 표시만.
  - `main.py`에 `data_freshness(conn)` 추가, `dashboard.html` 최상단(파트 선택 탭 위)에
    `.freshbar`로 렌더링.
- 검증: 실데이터 기준 사내불량·생산량 07-31, 외주·폐기 07-23으로 나와 있어 외주·폐기 두 건이
  빨간 경고로 정확히 표시됨(실제 지연 상태와 일치). Playwright로 대시보드 스크린샷·JS 오류 0 확인,
  파트 필터(1PART/2PART) 전환도 정상.

### 2026-07-27 (5) — 화면 설명글 정리 + 그래프 수치표시 개선
- 목표: ① 화면 곳곳의 기능/역할 설명 텍스트 전부 제거. ② 월별·주별 막대그래프 수치 라벨을
  당월/당주만 항상 보이게 하고, 이전 값은 마우스오버·클릭 시에만 보이게.
- ①: 로그인(샘플계정 안내), 사용자관리(권한 설명), 불량검토(대기 사유·처리 방법 안내),
  입력화면(TM-NO 처리방식·조회권한 안내), 공정관리(집계 규칙 안내), 제품등록(TM-NO 표기·단가 누적 설명),
  제품목록(빈 목록 안내문), 폴더반영(읽기전용·최초1회·데모삭제 안내 3건), SVP입력(FY기간·분모 설명),
  목표관리(FY정의 안내 + '비고' 열 전체), 세부지표현황(탭 부제목 7개 + 주별집계 미제공 안내),
  대시보드(KPI카드 클릭 안내) 제거. 데이터 건수·에러/성공 메시지 등 기능적 피드백은 유지.
- ②: `charts.js` renderBar — 막대마다 항상 보이던 수치 라벨을 `data-cur`(당월/당주 인덱스)
  하나만 기본 노출하고 나머지는 `.g-vlab-alt`(opacity:0)로 숨김. CSS `:hover`로 노출,
  터치기기 대응으로 클릭 토글 리스너 추가(`qms.css`, `charts.js`).
- 검증: 전체 화면 재기동 후 로그인 포함 14개 경로 200/오류 0. Playwright로 대시보드 JS 오류 0,
  hover 전 라벨 1개(당월)→과거 막대 hover 후 2개(당월+hover) 확인. 스크린샷으로 각 화면 육안 확인.

### 2026-07-27 (4) — 외주소재불량 xlsm 재업로드 반영(일부 TM-NO 오류 수정)
- 목표: 사용자가 `templates/00_sintering_defect.xlsm`(소재불량 현황)을 오류 수정 후 재업로드 →
  해당 데이터를 DB에 반영.
- 작업 전 안전조치: `qms.db` 백업(`backups/qms_backup_20260727_222859_before_sintering_reimport.db`).
- 적재 전 파일↔DB 차이 리포트로 먼저 확인(적재 없이 파싱만) — 사람이 검토 확정(`reviewed=1`)한
  행이 하나도 걸리지 않는 것 확인 후 진행:
  - 기존 오류: 12건이 TM-NO 공란으로 들어가 있었음(1990-01·1907-01 등) → 이번 파일에서 정정, 수량 동일.
  - 나머지 차이는 정정이 아니라 **07-03~07-23 신규 기간 데이터**(파일 706행, 기존 610행).
- `ingest.ingest_outsource_xlsm()` 재실행(source='outsource' 전체 교체, reviewed=1 보존) →
  **1,010건 적재, 신규 100EA+ 33건 검토 대기**(/input/outsource-review 에서 승인/수정/삭제 필요).
- 검증: 적재 후 DB(780키·23,557EA)가 파일 파싱 결과와 완전 일치, TM-NO 공란 0건.
  서버 기동 후 대시보드·세부지표·불량현황·불량검토 화면 모두 200/오류 0.
- 다음 할 일: 사용자가 /input/outsource-review 에서 신규 33건(100EA 이상) 검토 처리.

### 2026-07-27 (3) — TM-NO 없는 폐기불량은 지정 공정으로만 집계
- 목표: 불량검토(100EA 이상) 화면에 보이는 **TM-NO 없는 폐기불량**을 특정 TM-NO에 넣지 않고
  **지정된 공정의 불량으로만** 집계한다.
- 완료:
  - `calc.Masters.resolve()`: TM-NO가 비어 있고 입력공정(stored_process)이 있으면
    **그 공정 100%**로 확정하고 마스터 배분기준/품목 라우팅 배분을 건너뛴다.
    (TM-NO 없는 건은 라우팅이 없어 배분기준이 엉뚱한 공정·`?` 버킷으로 새던 경로를 차단)
  - 불량검토 화면(`review.html` + `main.outsource_review`): **집계공정 열 추가**,
    TM-NO 빈 값은 `TM-NO 없음`으로 표시, 안내문에 집계 규칙 명시.
- 검증: 서버 기동 후 admin 로그인 → /input/outsource-review 렌더 정상(오류 0),
  대시보드·세부지표·불량현황 200. TM-NO 없는 실데이터 5종 모두 지정 공정 100% 확인.
  마스터 배분기준이 다중공정인 유형(`찍힘`)으로 회귀 확인: TM 없음→소결 100%,
  TM 있음→기존 배분(소결/정형/기타) 유지. 2026-07 VMS 소결 2,676 중 646이 TM-NO 없는 건.
- 주의(미조치, 별건 제안): `ingest._reingest_preserve_reviewed()`의 보존 키가
  `(d, tm_no, defect_name)`이라 **TM-NO가 빈 행끼리 같은 날·같은 불량명이면 키가 충돌**한다.
  검토 완료(reviewed=1) 행이 생기면 재스캔 때 같은 키의 다른 행들이 함께 스킵될 수 있다.

### 2026-07-27 (2) — 문서 정리 + 시스템 구조도
- 목표: 대화 세션이 바뀌어도 문맥이 끊기지 않도록 문서를 정리한다.
- 완료:
  - **`docs/ARCHITECTURE.md` 신규** — 시스템 구조 전체를 한 문서에 정리.
    데이터 흐름도(mermaid) · 2단계 운영 흐름 · 모듈 지도 · 공정 2층 구조 ·
    배분 규칙 흐름도 · TM-NO 정규화 · DB 스키마(ERD) · KPI 계산식 · 화면 구조 ·
    차트 렌더러 · **알려진 함정 7가지** · 미해결 항목.
  - **`CLAUDE.md` §7 채움** — 비어 있던 프로젝트 메모(매 세션 자동 로드되는 곳)에
    스택·실행법·건드리면 안 되는 것을 기록. 특히 자주 혼동한 규칙 3가지를 명시:
    성형 "시트" 제외 ≠ 성형 "원인" 제외 / COPQ·Scrap 공정 제외 규칙 없음 /
    외주·폐기는 수량 합산만.
  - **`CLAUDE.md` §8 경로 정정** — `docs/worklog.md`(존재하지 않음) → `status.md` 로 수정하고,
    문서 5종의 역할 분담을 명시.
  - **`README.md` 갱신** — 폐기된 "COPQ 성형 제외" 규칙 삭제, 실행법을 `app.init_data` 기준으로 정정,
    모듈 목록에 scan/init_data 추가, 화면 목록·수집 2단계 표 추가.
- 다음 할 일: 아래 "열려있는 결정/확인 대기" 및 ARCHITECTURE.md §13 미해결 항목 참고.
- 주의: **코드 구조가 바뀌면 `docs/ARCHITECTURE.md` 를 같이 고친다.**

### 2026-07-27 — 세부지표현황 (KPI현황 + 공정별불량현황 통합)
- 목표: 두 리포트 메뉴를 하나로 합치고, 검색조건 + 집계단위(월/분기/년)로 여러 분석 그래프를 보게 한다.
- 완료:
  - **분석 엔진** `calc.analyze()` — 분류축 5종(part/process/defect_type/tm/product_name)
    × 집계단위 3종(월/분기/년) × 지표 2종(ppm/EA) × 상위 N(5/10/15/20).
    `period_key()`가 날짜를 `2026-07` / `FY27 Q1` / `FY27` 로 변환.
  - **화면** `/report/detail` — 검색조건 8개(파트·기간·집계단위·구분·공정·소스·지표·표시개수)
    + 분석유형 탭 7개 + 차트 + 상세표. nav는 `📈 세부지표현황` 하나로 통합.
  - 구 라우트 `/report/kpi`, `/report/defect` 는 리다이렉트로 유지(옛 링크 호환),
    `report_kpi.html` / `report_defect.html` 삭제.
  - **차트** `charts.js`에 `renderMulti()`(다중 꺾은선, y축 눈금 3단) + `renderHBar()`(가로 막대) 추가.
    패널용 넓은 캔버스(900×300)를 따로 둬서 글자가 확대되지 않게 함. 라벨 폭은 실측해 여백 산정.
  - **COPQ 목표 정정**: FY27 통합 `310.0` → `0.31` (입력 실수). 백업 `qms_before_detail.db`.
- 함께 고친 계산 버그:
  - 상위 N 합산 항목 이름이 `기타` 라서 **실제 불량유형 '기타'와 충돌**해 값이 이중 계상됨
    → `그 외 N개` 로 분리. (예: 불량유형 비중에서 기타 19,403 → 실제 그 외 10,357)
  - TM-NO/품명 축의 불량율 분모를 그룹 단위로 계산하도록 수정.
    합산 그룹도 포함된 품번들의 생산수량 합으로 분모를 잡아 ppm이 나오게 됨.
  - 제품 마스터에 없는 TM-NO(7건, 88EA)의 배분 공정이 **빈 문자열**로 남던 것 → `?`(화면 `(공정미상)`).
- 검증: Playwright로 7뷰 × 3집계단위 × 2지표 × 3파트 = **126조합, JS 오류 0 / 라벨 잘림 0**.
  대시보드 회귀 없음. 월별 Scrap 수량·비용(4~7월) 값 변동 없음.
- 다음 할 일:
  - `(공정미상)` 88EA의 원인인 TM-NO 7건(1634-01, 1641-00, 1752-00, 588-42, 798-01, 1987-01, 670-02)
    을 제품 마스터에 등록하면 정상 공정으로 분류됨.
  - 100EA 검토대기 46건 반영/수정/삭제 처리 필요(KPI에서 일시 제외 중).
  - 신규 불량유형 9종이 DB에만 있고 `templates/불량유형마스터_양식.xlsx` 미갱신.
- 주의: 주별 집계는 미제공. 사내불량·생산수량이 월 단위로 수집돼 월말 1일에 몰려 있어 불량율이 왜곡됨.
  일일 입력이 쌓이면 `PERIOD_UNITS`에 "주"를 추가하면 된다.

### 2026-07-22
- 목표: 시스템 실제 구현 (Excel 샘플 → DB → 계산 → 웹).
- 완료:
  - M0 골격 + 샘플 Excel 생성기 (scripts/gen_sample.py, sample_data/)
  - M1 DB 스키마(app/db.py) + Excel 적재/검증(app/ingest.py) + 로그인/3권한
  - M3 배분 계산엔진(app/calc.py) — 정수 배분(찍힘40→소결20/정형20 검증)
  - M4 KPI 집계 — FY 매핑, Scrap Cost/Qty%·COPQ%·불량율 ppm, 분모 SVP/추정
  - M5 대시보드 — KPI 카드·롤링7개월(FY계단목표)·주별·TOP5 (실데이터)
  - M6 리포트(KPI현황·공정별) + 목표관리 + 사용자관리 + 데이터입력 업로드
  - Playwright 검증: 로그인→전화면 200, 차트 실데이터 렌더, JS 오류 0
- 다음 할 일:
  - M7: 포트 5003 상시구동(Windows) 가이드 + DB 백업 스크립트
  - 입력 화면에 '직접입력 폼'(현재는 Excel 업로드 위주) 추가
  - 대시보드 지표 선택 토글(현재 COPQ 고정) 등 UX
- 주의:
  - 실행: python scripts/gen_sample.py → python -m app.seed → uvicorn app.main:app --port 5003
  - 샘플 COPQ가 다소 높음(Claim 샘플값 큼) — 실데이터로 조정됨. 디자인/로직은 정상.
  - 목표 FY는 2자리(26/27) 저장, calc.fy_of는 4자리(2027) → target_val에서 %100 정규화.

## 열려있는 항목 (다음 세션에서 이어서)

### 사용자 조치가 필요한 것
| # | 내용 | 조치 위치 |
|---|---|---|
| 1 | 100EA 검토대기 **46건**이 KPI에서 일시 제외 중 | `/input/outsource-review` 에서 반영/수정/삭제 |
| 2 | `(공정미상)` 88EA — 제품 마스터에 없는 TM-NO 7건<br/>(`1634-01`, `1641-00`, `1752-00`, `588-42`, `798-01`, `1987-01`, `670-02`) | `/admin/products` 에 등록하면 정상 분류 |

### 개발 대기
| # | 내용 | 비고 |
|---|---|---|
| 3 | 폴더 스캔 자동 주기 전환 | `scan.scan_all()` 을 그대로 호출하면 됨 (현재는 수동 버튼) |
| 4 | 신규 불량유형 9종이 DB에만 있고 양식 파일 미갱신 | `templates/불량유형마스터_양식.xlsx` |
| 5 | 주별 집계 | 일일 입력이 쌓이면 `calc.PERIOD_UNITS` 에 "주" 추가 |

### 확정된 가정 (2026-07 기준, 변경 없음)
- 마스터 기준단가 단위: **원** (집계 시 천원 환산)
- 가공·기타 공정 단가 = 완제품 단가. 정형 'O' 품목은 정형 이후 완제품 단가 적용.
- 목표 FY는 2자리(26/27) 저장, `calc.fy_of` 는 4자리(2027) → `target_val` 에서 `%100` 정규화.
