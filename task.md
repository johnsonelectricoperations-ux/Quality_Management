# 구현 작업 목록 (task.md)

> 근거: `plan.md` (요구사항·계산규칙·화면), `docs/design/screens-qms.html` (디자인/차트).
> 규칙: 한 번에 한 단계. 동작하는 상태가 되면 커밋. 각 단계 완료 시 `status.md` 갱신.
> 스택(확정): Python venv + FastAPI + SQLite + Jinja2, 포트 **5003**. 권한 3단계(조회자/입력자/관리자).
> 체크박스는 **실제 완료된 것만** 표시한다 (검증 통과 기준).

---

## 마일스톤

### M0. 프로젝트 골격 + 샘플 데이터
- [x] `task.md` / `status.md` 생성
- [x] 디렉터리 구조 (`app/`, `sample_data/`, `scripts/`)
- [x] `requirements.txt`, 실행 스크립트, README(설치·실행)
- [x] 샘플 Excel 생성기 (`scripts/gen_sample.py`) — 마스터 3종 + 트랜잭션, 시드 고정
- [x] 샘플 데이터 생성 실행 → `sample_data/*.xlsx`

### M1. DB 스키마 + 마스터 업로드 + 로그인/권한
- [x] SQLite 스키마 (`app/db.py`)
- [x] 마스터 파서/검증 (`app/ingest.py`) — 배분기준 파싱, TM-NO 문자 강제
- [x] 로그인 + 세션 + 권한 3단계 (`app/auth.py`)
- [x] 마스터 업로드/조회 화면
- [x] 검증: 샘플 마스터 업로드 → 조회, 잘못된 배분기준 오류 표시

### M2. 데이터 입력 화면 + 트랜잭션 업로드
- [x] 불량 입력(공정/셋팅 탭) 직접입력 + Excel 업로드
- [x] 외주소재불량/폐기불량 가져오기, 100EA 격리·검토
- [x] 생산실적(수량+금액) 직접입력 + Excel 업로드
- [x] SVP / Claim / Customer Incident 입력
- [x] 검증: 각 트랜잭션 CRUD·업로드 동작

### M3. 배분 계산 엔진
- [x] 배분기준 파싱 + 라우팅 교집합 + 정수 배분(최대잉여법) (`app/calc.py`)
- [x] 검증: 예시(찍힘 40 → 소결20/정형20) 재현 테스트

### M4. KPI 집계 (FY, 일/주/월, 분모 이원화, ppm)
- [x] FY 매핑, Scrap Cost/Qty(%)·COPQ(%)·불량율(ppm)·Warranty·Incident
- [x] COPQ 성형 제외, 분모 SVP/추정 이원화
- [x] 검증: 수기 검산 일치

### M5. 대시보드
- [x] KPI 카드(목표 병기) + 롤링 7개월 추이 + 당월 주별 (FY 계단 목표선)
- [x] 공정불량 TOP5 (당월/해당주, VMS/TM)
- [x] 라이트/다크 + 차트 렌더러 이식

### M6. 리포트 + 목표 관리
- [x] KPI 현황(월별 추이 2열) + COPQ 구성
- [x] 공정별 불량현황(공정 차트 2열 + 상세표)
- [x] 목표 관리(FY×파트×KPI) + 사용자 관리

### M7. 배포 준비
- [x] 포트 5003 상시 구동 가이드(Windows), DB 백업 스크립트
- [x] 최종 점검 + README 정리

---

## 현재 위치
→ **M0~M7 완료** (구현·배포 가이드 완료). 잔여(선택): 입력 화면 직접입력 폼, 대시보드 지표 선택 토글 (상세는 `status.md`)
