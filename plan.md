# 통합품질관리시스템 구축 계획 (plan.md)

> 근거 문서: `시스템 구성.txt` (2026-07-22)
> 디자인 기준: `docs/design/johnson-electric.md` (존슨 오렌지 #F26100, 엔터프라이즈 대시보드 톤)
> 상태: **계획 확정 단계** — 핵심 4개 질문 답변 반영 완료(§0). 남은 확인 사항은 §8.

---

## 0. 확정된 결정 사항 (2026-07-22 답변 반영)

| 항목 | 결정 |
|---|---|
| Scrap Cost 단가 | 제품 MASTER의 공정별 기준단가는 **그 자체가 누적 개념**. 배분된 공정의 마스터 단가를 그대로 곱한다 (추가 누적 계산 불필요) |
| 집계 주기 | **월별 기본 + 일별/주별 집계 제공**. 분모(SVP)는 아래 "SVP 이원화" 규칙 적용 |
| SVP 이원화 | SVP는 월 1회 입력. **일별/주별 KPI의 분모는 생산수량 입력 시 함께 입력하는 "생산금액"의 합(추정 SVP)** 을 사용하고, **월별 KPI는 SVP 입력값이 들어오면 그것으로 확정** (미입력 기간은 생산금액 합으로 잠정 표시) |
| 배분기준 형식 | **추천안 채택(§5.1)**: 대상 공정 명시 + 비율 생략 시 균등 분할 (`소결,정형` = 50/50, `소결:70,정형:30` = 명시 비율) |
| 기술 스택 | **사내망 서버 PC + Python venv + 웹앱**. 여러 사용자가 브라우저 접속. 서버 PC에 기존 서버 프로그램이 있으므로 **독립 venv + 전용 포트 5003으로 공존** |
| 불량 입력 화면 | 공정불량과 셋팅불량은 불량유형이 달라 **탭으로 분리된 별도 화면** 구성 |
| Customer Incident | KPI는 **건수(건)**, COPQ 구성요소인 Customer Incident Cost는 **금액(천원)** — 별개 관리 |
| SVP 입력 단위 | **VMS / TM 파트별 전체 금액** (월 1회) |
| 로그인/권한 | **조회자 / 입력자 / 관리자** 3단계 권한 구분. 100EA 검토 승인/수정은 입력자도 가능 |
| COPQ 제외 규칙 | COPQ 계산 시 Scrap Cost에서 **성형공정에 배분된 공정불량·셋팅불량 비용은 제외** (KPI의 Scrap Cost 자체는 전체 포함) |

---

## 1. 시스템 개요

**한 줄 목표**: VMS Part / TM Part 두 그룹의 품질 데이터를 모아 **KPI(불량율·Scrap·COPQ·Claim)를 자동 집계**하고 대시보드로 보여주는 시스템.

핵심 성격:
- **데이터 수집**: 수기 입력 + 정해진 규격의 Excel 업로드가 기본 (1차). 자동 연동은 2차.
- **계산 엔진**: 불량 수량의 공정별 배분 → 누적단가 곱해 비용화 → SVP 분모로 % 산출.
- **조회**: 대시보드(KPI, 일/주/월 전환) + 공정별 집계 리포트.

---

## 2. 요구사항 분석 요약

### 2.1 KPI 항목 (대시보드)

| KPI | 단위 | 집계 방식 | 분모 |
|---|---|---|---|
| Scrap Cost | 천원 | VMS / TM / 통합, **공정별로도 산출** | — (금액) |
| Scrap Quantity | EA | VMS / TM / 통합 | — (수량) |
| COPQ | % (금액은 천원) | VMS / TM / 통합 | SVP (또는 추정 SVP) |
| Customer Incident | 건 | VMS / TM / 통합 | — |
| Warranty | 천원 | VMS / TM / 통합 | — |
| 공정불량율 | % | **VMS / TM 별도만** (통합 안 함) | 생산수량 |
| 셋팅불량율 | % | **VMS / TM 별도만** (통합 안 함) | 생산수량 |

- 기간 축: **일별 / 주별 / 월별** 전환 (대시보드 상단 세그먼트 토글 — 디자인 시안에 이미 반영됨)
- COPQ 세부 = Scrap Cost(성형공정 제외분) + Warranty + 3rd Party Containment + Quality Special Freight + Customer Incident Cost + Unplanned Inspection & Sorting Cost + Variance → 합산액 ÷ SVP × 100
- ⚠ COPQ에 들어가는 Scrap Cost는 **성형공정에 배분된 공정불량·셋팅불량 비용을 제외**한 값이다. KPI 카드의 Scrap Cost는 전체(성형 포함) 금액 그대로 표시하므로 두 값이 다를 수 있다 — 화면에 각주로 표기.
- Claim 항목(Warranty 등)은 월 단위 수기 입력이므로, **일별/주별 화면에서 COPQ는 "월 누계 기준" 표기** 또는 Scrap Cost 중심으로 표시 (구현 시 화면에서 명확히 라벨링)

### 2.2 데이터 소스

| 데이터 | 1차 입력 방식 | 2차 업데이트 (자동화) |
|---|---|---|
| 공정불량 | 직접 입력 화면 + 규격 Excel 업로드 | — |
| 외주소재불량 | Excel 파일 불러오기 | 지정 폴더에서 정해진 시간에 자동 수집 |
| 폐기불량 | Excel 파일 불러오기 | 타 시스템 DB에서 자동 조회 |
| 생산수량 + **생산금액** | 별도 입력 화면 (일 단위) | 전용 PC에서 ERP 자동조작(RPA) 입력 |
| SVP | 별도 입력 화면 (월 1회) | 〃 |
| Claim 7항목 | 별도 수기 입력 화면 | 〃 |

- **공정불량 산정 = 직접입력 DATA + 외주소재불량 + 폐기불량** 3개 소스의 합.
- 외주소재불량 검증 룰: **단일 TM-NO × 단일 불량유형 ≥ 100 EA** 건은 자동 반영하지 않고 **검토/수정 화면**에서 승인 후 반영.

### 2.3 마스터 데이터 3종 (Excel 업로드로 관리)

| 마스터 | 컬럼 | 비고 |
|---|---|---|
| 제품 MASTER | TM-NO, 품명, 공정명 순서, 공정별 기준단가 | 라우팅 + 단가. **단가는 누적 개념으로 입력됨** |
| 공정명 MASTER | 파트구분(VMS/TM), 공정명, COPQ제외여부 | 공정 → 파트 매핑, 성형=Y |
| 불량유형 MASTER | 구분(공정/셋팅), 공정명, 불량명, 배분기준 | 배분기준 형식은 §5.1 |

#### ⚠ TM-NO 날짜 오인식 방지 (전 화면·전 업로드 공통 규칙)

TM-NO가 Excel이나 브라우저에서 날짜로 자동 변환되는 사고를 막기 위해 다음을 **모든** 입력 경로에 적용한다:

1. **시스템 내부**: TM-NO는 DB·API 전 구간에서 **문자열(TEXT) 타입으로만** 취급한다. 숫자·날짜 변환 금지.
2. **Excel 업로드 파서**: TM-NO 열은 반드시 문자열로 읽는다(`dtype=str`). 셀 값이 이미 날짜(datetime)로 변환되어 들어온 경우 자동 반영하지 않고 **업로드 오류로 표시**해 사용자가 원본을 고치게 한다.
3. **업로드 양식**: 모든 양식(마스터·불량입력 등)의 TM-NO 열은 **텍스트 서식(@) 으로 미리 지정**해 배포한다.
4. **화면 입력**: TM-NO 입력란은 text 타입으로만 받고, 저장 전 trim 외의 어떤 변환도 하지 않는다.
5. **Excel 다운로드/내보내기**: TM-NO 열은 텍스트 서식으로 출력한다 (재업로드 시 변질 방지).

---

## 3. 데이터 모델 (안)

### 마스터 테이블
```
제품 (product)          : tm_no(PK), 품명, 파트구분(파생)
제품공정 (product_route): tm_no(FK), 공정순서(seq), 공정명, 기준단가(누적, 천원)
공정 (process)          : 공정명(PK), 파트구분(VMS/TM), COPQ제외여부(기본 N, 성형=Y)
불량유형 (defect_type)  : 불량코드(PK), 구분(공정/셋팅), 불량명, 발생공정, 배분기준(원문)
불량배분규칙 (defect_alloc): 불량코드(FK), 배분대상공정, 배분비율   ← 배분기준 파싱 결과
```

### 트랜잭션 테이블
```
불량입력 (defect_entry)   : 일자, tm_no, 공정명, 불량코드, 수량,
                            소스(직접/공정Excel/외주/폐기), 상태(확정/검토대기)
생산실적 (production)     : 일자, tm_no(또는 파트), 생산수량, 생산금액(천원)  ← 금액이 추정 SVP 재료
SVP (svp)                : 년월, 파트구분(VMS/TM), 금액(천원)               ← 파트별 전체금액, 월 1회
클레임 (claim)            : 년월, 파트구분, 항목(7종), 금액(천원), 비고
인시던트 (incident)       : 일자, 파트구분, 고객명, 내용, 비고, 등록자       ← KPI 건수 집계용
업로드이력 (upload_log)   : 파일명, 종류, 일시, 건수, 결과 — 재업로드/중복 방지
사용자 (user)             : 아이디, 이름, 비밀번호(해시), 권한(조회자/입력자/관리자)
```

### 파생(계산) 테이블
```
불량배분결과 (defect_allocated): 일자, tm_no, 공정명, 불량코드, 배분수량(정수), scrap_cost(천원)
KPI집계 (kpi_summary)         : 기간유형(일/주/월), 기간, 파트구분(VMS/TM/통합), KPI명, 값, 분모유형(SVP확정/추정)
```

> 원본 입력은 보존, 배분·비용·KPI는 재계산 가능한 파생 데이터로 분리. 마스터(단가·배분규칙) 수정 시 해당 기간 재계산.

---

## 4. 화면 구성 (메뉴 트리)

```
📊 대시보드
   └ KPI 7종 카드 + 추이 차트 · [일별|주별|월별] × [통합|VMS|TM] 전환
     (공정불량율·셋팅불량율은 통합 탭에서 숨김, COPQ 분모는 확정/추정 표시)

✏ 데이터 입력
   ├ 불량 입력              : [공정불량 | 셋팅불량] 탭 분리 화면
   │                          각 탭은 해당 구분의 불량유형만 표시, 직접 입력 그리드 + Excel 업로드
   ├ 외주소재불량 가져오기    : Excel 업로드 → 100EA 이상 건 자동 격리
   ├ 외주소재불량 검토       : 격리 건 검토/수정/승인 (승인 시 집계 반영)
   ├ 폐기불량 가져오기       : Excel 업로드
   ├ 생산실적 입력           : 일자별 생산수량 + 생산금액(천원)
   ├ SVP 입력               : 년월 × 파트(VMS/TM), 전체 금액(천원)
   ├ Claim 입력             : 년월 × 파트 × 7항목, 금액(천원)
   └ Customer Incident 관리  : 상단 = 등록 폼 (일자·파트·고객·내용·비고)
                              하단 = 등록 내역 리스트 (기간·파트 필터, 수정/삭제 가능)
                              → 리스트 건수가 KPI Customer Incident(건) 집계 원천

🔐 관리 (관리자 전용)
   └ 사용자 관리            : 계정 등록, 권한(조회자/입력자/관리자) 부여

📈 집계/리포트
   ├ 공정별 불량현황         : 공정불량/셋팅불량 × 공정별 수량·불량율
   ├ 공정별 Scrap Cost
   └ COPQ 상세              : 7개 구성항목 breakdown

⚙ 마스터 관리
   ├ 제품 MASTER  (Excel 업로드 + 조회)
   ├ 공정명 MASTER (Excel 업로드 + 조회)
   └ 불량유형 MASTER (Excel 업로드 + 조회, 배분기준 검증 포함)
```

- 디자인: `docs/design/johnson-electric.md` 토큰 + `example-qms.html` 레이아웃 적용.

---

## 5. 계산 로직 정의

### 5.1 배분기준 형식 (추천안 — 채택)

불량유형 MASTER의 "배분기준" 컬럼 한 칸에 다음 문법으로 기재한다:

```
형식 A (균등 분할):   소결,정형          → 50% / 50%
형식 B (비율 명시):   소결:70,정형:30    → 70% / 30%
형식 C (발생공정 귀속): (빈칸)           → 발생공정에 100%
```

**추천 이유**
1. 마스터 관리자가 Excel 한 컬럼에 쓰는 방식이라 학습 비용이 없다.
2. 대부분의 경우(예시의 소결/정형 50:50)는 공정만 나열하면 끝 — 비율 계산 실수 여지 제거.
3. 특수 케이스만 비율을 명시하면 되고, 빈칸이면 발생공정 귀속이라 기본값도 안전하다.
4. 업로드 시 파싱·검증(공정명 존재 여부, 비율 합 100 확인)해서 오류를 즉시 알려줄 수 있다.

**적용 규칙**
- 배분 대상 공정이 해당 품목의 라우팅(제품공정)에 없으면: 라우팅에 존재하는 대상 공정끼리 비율 재분배. 전부 없으면 발생공정 귀속 + 경고 로그.
- 정수 유지: 최대잉여법 — `floor(N×비율)` 후 잔여분을 소수부 큰 공정부터 +1. (예: 찍힘 40 → 소결 20 / 정형 20)

### 5.2 Scrap Cost (확정)
```
scrap_cost = 배분수량 × 배분된 공정의 마스터 기준단가
```
- 마스터의 공정별 기준단가가 **이미 누적 개념**이므로 그대로 곱한다. 시스템에서 별도 누적 합산을 하지 않는다.
- 공정별/파트별/통합, 그리고 일/주/월 기간별로 합산.

### 5.3 분모(SVP) 이원화 (확정)
```
일별 분모  = 해당 일자의 Σ생산금액              (추정 SVP)
주별 분모  = 해당 주의 Σ생산금액                (추정 SVP)
월별 분모  = SVP 입력값 (입력 전에는 Σ생산금액으로 잠정, 입력되면 확정값으로 대체)
```
- KPI집계에 `분모유형(확정/추정)` 플래그를 저장하고, 대시보드에 "추정" 배지로 표시해 혼동 방지.
- SVP 확정값 입력 시 해당 월의 월별 KPI 자동 재계산.

### 5.4 KPI 산식
```
Scrap Quantity = Σ 불량수량                                      [파트/통합 × 일/주/월]
Scrap Cost     = Σ scrap_cost (천원)                             [파트/통합/공정별 × 일/주/월]
공정불량율      = 공정불량수량 ÷ 생산수량 × 100                    [파트별만, 공정별]
셋팅불량율      = 셋팅불량수량 ÷ 생산수량 × 100                    [파트별만, 공정별]
ScrapCost_COPQ = ScrapCost − (성형공정에 배분된 공정불량·셋팅불량 비용)
COPQ(%)        = (ScrapCost_COPQ + Warranty + 3rdParty + SpecialFreight
                  + CustomerIncidentCost + UnplannedInspection + Variance) ÷ 분모 × 100
Warranty       = Claim 입력값 (천원, 월 단위)
Customer Incident = Incident 등록 건수 집계 (건)
```
- 성형공정 제외는 코드에 "성형"을 하드코딩하지 않고 **공정 마스터의 COPQ제외여부 플래그**로 구현한다 (공정명 변경·제외 공정 추가에 대비). 공정명 마스터 양식에 해당 컬럼을 추가한다.
- COPQ 상세 리포트에 "제외된 성형공정 Scrap Cost" 금액을 별도 표기해 검산 가능하게 한다.

### 5.5 권한 체계
| 권한 | 범위 |
|---|---|
| 조회자 | 대시보드·리포트 조회만 |
| 입력자 | 조회 + 데이터 입력·Excel 업로드 + **외주소재불량 100EA 검토 승인/수정** |
| 관리자 | 전체 + 마스터 관리, 사용자 관리 |

> 100EA 검토 승인/수정은 입력자·관리자 모두 가능 (2026-07-22 확정). 승인 시 처리자 이름을 기록한다.

---

## 6. 기술 구성 (확정)

| 구분 | 확정 내용 | 비고 |
|---|---|---|
| 배포 형태 | 사내망 서버 PC에서 상시 구동, 사용자는 브라우저 접속 | 기존 서버 프로그램과 공존 |
| 런타임 | Python + **venv 가상환경** (기존 프로그램과 의존성 완전 분리) | `venv` 폴더는 커밋 금지 |
| 포트 | **5003 (확정)** | 사용자 접속 주소: `http://<서버IP>:5003` |
| 인증 | 로그인 + 3단계 권한(조회자/입력자/관리자) | 세션 기반, 비밀번호 해시 저장 |
| 백엔드 | FastAPI + Uvicorn | Excel 처리(openpyxl/pandas), 2차 스케줄러·RPA 확장 용이 |
| DB | SQLite (단일 파일) → 데이터 증가 시 PostgreSQL 이관 여지 | 서버 PC에 DBMS 추가 설치 불필요. 정기 백업 스크립트 포함 |
| 프론트 | Jinja2 서버 렌더링 + 경량 JS + Chart.js | 폼·테이블·차트 중심. 디자인 토큰 CSS 적용 |
| 상시 구동 | Windows면 작업 스케줄러(로그온 없이 시작) 또는 NSSM 서비스 등록 | 서버 OS 확인 후 설치 가이드 작성 |

---

## 7. 개발 단계 (로드맵)

### 1차 (수동 입력 기반)
| 단계 | 내용 | 완료 조건 |
|---|---|---|
| M1 | 프로젝트 골격(venv, FastAPI, SQLite, 포트 5003) + 로그인/권한 3단계 + 마스터 3종 Excel 업로드/조회 (배분기준 파싱·검증 포함) | 권한별 접근 제어 동작, 실제 마스터 파일 업로드 → 조회 화면 확인, 잘못된 배분기준이 오류로 표시 |
| M2 | 불량 입력(공정/셋팅 탭 분리, 직접+Excel) / 생산실적(수량+금액) / SVP / Claim / Customer Incident(등록 폼+리스트) 입력 화면 | 샘플 데이터 입력·수정·삭제 동작, 탭별로 해당 구분 불량유형만 표시, Incident 리스트 필터·수정·삭제 동작 |
| M3 | 외주소재불량·폐기불량 Excel 가져오기 + 100EA 검토 화면 | 100EA 이상 건 격리 → 승인 시 반영 |
| M4 | 배분 계산 엔진 + 공정별 집계 (정수 배분 검증) | 예시 케이스(찍힘 40 → 소결20/정형20) 재현 |
| M5 | KPI 집계(일/주/월, 분모 이원화) + 대시보드 (통합/VMS/TM) | 7개 KPI가 수기 검산과 일치, 추정/확정 분모 전환 동작 |
| M6 | 공정별 리포트 (불량현황, Scrap Cost, COPQ 상세) | 검산 일치 |
| M7 | 서버 PC 배포 (전용 포트, 상시 구동, 백업) + 사용 설명 | 타 PC 브라우저에서 접속 확인 |

### 2차 (자동화)
- 외주소재불량: 지정 폴더 정시 자동 수집 (스케줄러)
- 폐기불량: 타 시스템 DB 직접 연동
- 생산실적·SVP·Claim: 전용 PC에서 ERP 자동조작(RPA) 입력

> 각 단계는 독립적으로 검증 후 커밋 (CLAUDE.md 워크플로 준수).

---

## 8. 남은 확인 사항

2026-07-22 답변으로 대부분 확정됨 (§0에 반영). 남은 것은 아래 3건뿐이며, 모두 해당 단계 착수 전까지만 확정되면 된다:

| # | 질문 | 임시 가정 | 필요 시점 |
|---|---|---|---|
| 1 | 생산실적(수량+금액) 입력 단위 — TM-NO별 일 단위가 맞는지? | TM-NO별 일 단위 | M2 |
| 2 | 마스터 기준단가의 단위 — 원? 천원? (양식은 '원'으로 작성됨) | 원 (집계 시 천원 환산) | M4 |
| 3 | 서버 PC의 OS (Windows?) — 상시 구동 등록 방식 결정용 | Windows | M7 |

---

## 9. 참고 — 기존 산출물과의 연결

- `docs/design/johnson-electric.md`: 색·타이포·컴포넌트 토큰 → 모든 화면에 적용
- `docs/design/example-qms.html`: 대시보드 레이아웃 시안 → M5의 출발점. KPI 카드는 §2.1의 7개 항목으로 교체, 기간 토글(일/주/월)은 시안의 세그먼트 그대로 활용
