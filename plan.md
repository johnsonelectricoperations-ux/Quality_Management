# 통합품질관리시스템 구축 계획 (plan.md)

> 근거 문서: `시스템 구성.txt` (2026-07-22)
> 디자인 기준: `docs/design/johnson-electric.md` (존슨 오렌지 #F26100, 엔터프라이즈 대시보드 톤)
> 상태: **분석/계획 단계** — 코딩 착수 전. 아래 "8. 확인 필요 사항"의 답을 받은 후 구현을 시작한다.

---

## 1. 시스템 개요

**한 줄 목표**: VMS Part / TM Part 두 그룹의 품질 데이터를 모아 **KPI(불량율·Scrap·COPQ·Claim)를 자동 집계**하고 대시보드로 보여주는 시스템.

핵심 성격:
- **데이터 수집**: 수기 입력 + 정해진 규격의 Excel 업로드가 기본 (1차). 자동 연동은 2차.
- **계산 엔진**: 불량 수량의 공정별 배분 → 단가 곱해 비용화 → SVP 분모로 % 산출이 핵심 로직.
- **조회**: 대시보드(KPI) + 공정별 집계 리포트.

---

## 2. 요구사항 분석 요약

### 2.1 KPI 항목 (대시보드)

| KPI | 단위 | 집계 방식 | 분모 |
|---|---|---|---|
| Scrap Cost | 천원 | VMS / TM / 통합, **공정별로도 산출** | — (금액) |
| Scrap Quantity | EA | VMS / TM / 통합 | — (수량) |
| COPQ | % (금액은 천원) | VMS / TM / 통합 | SVP |
| Customer Incident | 건(추정) | VMS / TM / 통합 | — |
| Warranty | 천원 | VMS / TM / 통합 | — |
| 공정불량율 | % | **VMS / TM 별도만** (통합 안 함) | 생산수량 |
| 셋팅불량율 | % | **VMS / TM 별도만** (통합 안 함) | 생산수량 |

- COPQ 세부 = Scrap Cost + Warranty + 3rd Party Containment + Quality Special Freight + Customer Incident Cost + Unplanned Inspection & Sorting Cost + Variance → 합산액 ÷ SVP × 100
- 공정불량율/셋팅불량율을 통합하지 않는 이유(추정): 두 파트의 공정 체계가 달라 합산이 무의미하기 때문.

### 2.2 데이터 소스 4종

| 데이터 | 1차 입력 방식 | 2차 업데이트 (자동화) |
|---|---|---|
| 공정불량 | 직접 입력 화면 + 규격 Excel 업로드 | — |
| 외주소재불량 | Excel 파일 불러오기 | 지정 폴더에서 정해진 시간에 자동 수집 |
| 폐기불량 | Excel 파일 불러오기 | 타 시스템 DB에서 자동 조회 |
| 생산수량 / SVP | 별도 입력 화면 | 전용 PC에서 ERP 자동조작(RPA) 입력 |
| Claim 7항목 | 별도 수기 입력 화면 | 전용 PC에서 ERP 자동조작(RPA) 입력 |

- **공정불량 산정 = 직접입력 DATA + 외주소재불량 + 폐기불량** 3개 소스의 합.
- 외주소재불량 검증 룰: **단일 TM-NO × 단일 불량유형 ≥ 100 EA** 건은 자동 반영하지 않고 **검토/수정 화면**에 올린다 (오입력·대량 이슈 필터).

### 2.3 마스터 데이터 3종 (Excel 업로드로 관리)

| 마스터 | 컬럼 | 비고 |
|---|---|---|
| 제품 MASTER | TM-NO, 품명, 공정명 순서, 공정별 기준단가 | 품목별 공정 라우팅 + 단가 |
| 공정명 MASTER | 파트구분(VMS/TM), 공정명 | 공정 → 파트 매핑 |
| 불량유형 MASTER | 구분(공정/셋팅), 공정명, 불량명, 배분기준 | 불량유형별 **복수 공정** 매핑 + 배분 규칙 |

### 2.4 핵심 계산 규칙 — 공정 배분

- 불량유형은 **복수 공정에 배분**될 수 있다.
- 예시: 선별공정에서 "찍힘" 40개 등록, 해당 품목 공정이 성형→소결→정형→선별이면 → **소결 50% / 정형 50%** 배분.
- 제약: **배분 수량이 소수점이 되면 안 됨** → 정수 배분 알고리즘 필요 (예: 최대잉여법 — 비율대로 내림 후 잔여분을 소수부 큰 순서로 +1).
- 공정불량/셋팅불량 모두 **공정별 집계** 필요.

---

## 3. 데이터 모델 (안)

### 마스터 테이블
```
제품 (product)          : tm_no(PK), 품명, 파트구분(파생)
제품공정 (product_route): tm_no(FK), 공정순서(seq), 공정명, 기준단가
공정 (process)          : 공정명(PK), 파트구분(VMS/TM)
불량유형 (defect_type)  : 불량코드(PK), 구분(공정/셋팅), 불량명, 발생공정
불량배분규칙 (defect_alloc): 불량코드(FK), 배분대상공정, 배분비율
```

### 트랜잭션 테이블
```
불량입력 (defect_entry)   : 일자, tm_no, 공정명, 불량코드, 수량, 소스(직접/공정Excel/외주/폐기), 상태(확정/검토대기)
생산수량 (production_qty) : 기간, tm_no(또는 파트), 수량
SVP (svp)                : 기간, 파트구분, 금액(천원)
클레임 (claim)            : 기간, 파트구분, 항목(Warranty 등 7종), 금액(천원), 비고
업로드이력 (upload_log)   : 파일명, 종류, 일시, 건수, 결과 — Excel 재업로드/중복 방지용
```

### 파생(계산) 테이블
```
불량배분결과 (defect_allocated): 기간, tm_no, 공정명, 불량코드, 배분수량(정수), scrap_cost(천원)
KPI집계 (kpi_summary)         : 기간, 파트구분(VMS/TM/통합), KPI명, 값
```

> 원본 입력은 그대로 보존하고, 배분·비용·KPI는 **재계산 가능한 파생 데이터**로 분리 → 마스터(단가·배분규칙) 수정 시 해당 기간 전체 재계산으로 정합성 유지.

---

## 4. 화면 구성 (메뉴 트리)

```
📊 대시보드
   └ KPI 7종 카드 + 추이 차트  · [통합 | VMS | TM] 탭 전환
     (공정불량율·셋팅불량율은 통합 탭에서 숨김)

✏ 데이터 입력
   ├ 공정/셋팅불량 입력      : 직접 입력 그리드 + Excel 업로드
   ├ 외주소재불량 가져오기    : Excel 업로드 → 100EA 이상 건 자동 격리
   ├ 외주소재불량 검토       : 격리 건 목록 → 검토/수정/승인 (승인 시 집계 반영)
   ├ 폐기불량 가져오기       : Excel 업로드
   ├ 생산수량 입력
   ├ SVP 입력
   └ Claim 입력             : 7항목 × 기간 × 파트, 금액(천원)

📈 집계/리포트
   ├ 공정별 불량현황         : 공정불량/셋팅불량 × 공정별 수량·불량율
   ├ 공정별 Scrap Cost
   └ COPQ 상세              : 7개 구성항목 breakdown

⚙ 마스터 관리
   ├ 제품 MASTER  (Excel 업로드 + 조회)
   ├ 공정명 MASTER (Excel 업로드 + 조회)
   └ 불량유형 MASTER (Excel 업로드 + 조회, 배분기준 포함)
```

- 디자인은 `docs/design/johnson-electric.md` 토큰과 `example-qms.html` 레이아웃(사이드바 + 상단바 + KPI 카드 + 데이터 테이블)을 그대로 적용.

---

## 5. 계산 로직 정의

### 5.1 공정 배분 (핵심)
```
입력: 불량 건 (tm_no, 발생공정, 불량코드, 수량 N)
1. 불량유형 MASTER에서 해당 불량코드의 배분기준(대상공정 + 비율) 조회
2. 해당 품목의 공정 라우팅(제품공정)과 교집합 → 실제 배분 대상 공정 확정
3. N × 비율로 배분하되 정수 유지:
   - 각 공정 배분량 = floor(N × 비율)
   - 잔여분 = N − Σfloor → 소수부가 큰 공정부터 1개씩 추가 (최대잉여법)
4. 배분 결과를 defect_allocated에 저장
```

### 5.2 KPI 산식
```
Scrap Quantity = Σ 불량수량 (공정불량 + 셋팅불량 + 폐기)          [파트별/통합]
Scrap Cost     = Σ (공정별 배분수량 × 기준단가) (천원)             [파트별/통합/공정별]
공정불량율      = 공정불량수량 ÷ 생산수량 × 100                    [파트별만, 공정별]
셋팅불량율      = 셋팅불량수량 ÷ 생산수량 × 100                    [파트별만, 공정별]
COPQ(%)        = (ScrapCost + Warranty + 3rdParty + SpecialFreight
                  + CustomerIncidentCost + UnplannedInspection + Variance)
                 ÷ SVP × 100                                     [파트별/통합]
Warranty       = Claim 입력값 (천원)
Customer Incident = 건수 집계 (⚠ 추정 — 8번 질문 참조)
```

---

## 6. 기술 구성 (제안 — 확정은 8번 질문 후)

| 구분 | 제안 | 이유 |
|---|---|---|
| 형태 | 웹 애플리케이션 (사내망) | 여러 사용자가 브라우저로 접근, 설치 불필요 |
| 백엔드 | Python (FastAPI) | Excel 처리(openpyxl/pandas)가 핵심 업무라 Python이 가장 유리. 2차의 폴더 감시·스케줄 수집·RPA도 Python 생태계가 강함 |
| DB | 1차 SQLite → 필요 시 PostgreSQL | 초기 사용자 수·데이터량이 크지 않다면 SQLite로 시작해 운영 부담 최소화 |
| 프론트 | 서버 렌더링(Jinja2) + 경량 JS | 입력 폼·테이블·차트 중심 화면. 대시보드 차트는 Chart.js 수준으로 충분 |
| 2차 자동화 | 폴더 감시(스케줄러) / DB 연동 / ERP RPA | 1차 구조(업로드 파서)를 그대로 재사용하도록 설계 |

> React 등 SPA도 가능하지만, 화면 성격(폼+테이블+차트)과 유지보수 주체를 고려하면 단순한 스택을 권장.

---

## 7. 개발 단계 (로드맵)

### 1차 (수동 입력 기반 — 시스템 구성.txt의 기본 범위)
| 단계 | 내용 | 완료 조건 |
|---|---|---|
| M1 | DB 스키마 + 마스터 3종 Excel 업로드/조회 | 실제 마스터 파일 업로드 → 조회 화면에서 확인 |
| M2 | 불량 입력(직접+Excel) / 생산수량 / SVP / Claim 입력 화면 | 샘플 데이터 입력·수정·삭제 동작 |
| M3 | 외주소재불량·폐기불량 Excel 가져오기 + 100EA 검토 화면 | 100EA 이상 건이 격리되고 승인 시 반영 |
| M4 | 배분 계산 엔진 + 공정별 집계 (정수 배분 검증 포함) | 예시 케이스(찍힘 40 → 소결20/정형20) 재현 |
| M5 | KPI 집계 + 대시보드 (VMS/TM/통합 탭) | 7개 KPI가 수기 검산과 일치 |
| M6 | 공정별 리포트 (불량현황, Scrap Cost, COPQ 상세) | 검산 일치 |

### 2차 (자동화)
- 외주소재불량: 지정 폴더 정시 자동 수집 (스케줄러)
- 폐기불량: 타 시스템 DB 직접 연동
- 생산수량·SVP·Claim: 전용 PC에서 ERP 자동조작(RPA) 입력

> 각 단계는 독립적으로 검증 후 커밋 (CLAUDE.md 워크플로 준수).

---

## 8. 확인 필요 사항 (구현 전 답변 필요)

구성 문서만으로 확정할 수 없는 항목이다. **추측으로 진행하지 않고 확인 후 반영한다.**

1. **집계 주기** — KPI 기준 기간은 월별인가? (입력은 일 단위, 집계·대시보드는 월 단위로 추정)
2. **Scrap Cost 단가 기준** — 불량 배분된 공정의 "공정별 기준단가"를 곱할 때, ① 해당 공정 단가만인지 ② 해당 공정까지의 누적 단가인지?
3. **배분기준의 정확한 규칙** — 예시(성형→소결→정형→선별에서 소결50/정형50)에서 성형·선별이 제외된 근거가 불량유형 MASTER의 "배분기준" 컬럼에 공정과 비율이 직접 명시되는 방식인지? (예: `소결:50, 정형:50`)
4. **셋팅불량 입력 경로** — 셋팅불량도 공정불량과 같은 입력 화면(불량유형 구분으로 분리)을 쓰는지, 별도 화면이 필요한지?
5. **Customer Incident** — KPI의 Customer Incident는 "건수"이고, COPQ의 Customer Incident Cost는 "금액"으로 별개 관리가 맞는지?
6. **SVP·생산수량 입력 단위** — 파트(VMS/TM)별 월 1회 입력인지, 품목(TM-NO)별인지?
7. **사용 환경** — 사용자 수(동시 접속), 서버로 쓸 PC 유무, 사내망 전용 여부?
8. **권한** — 입력자/검토자(외주소재 100EA 승인)/조회자 구분이 필요한지, 로그인 없이 단일 권한인지?

---

## 9. 참고 — 이 계획이 기존 산출물과 연결되는 지점

- `docs/design/johnson-electric.md`: 색·타이포·컴포넌트 토큰 → 모든 화면에 적용
- `docs/design/example-qms.html`: 대시보드 레이아웃 시안 → M5 대시보드의 출발점. 단, KPI 카드 구성은 본 문서 2.1의 7개 항목으로 교체 필요 (현 시안의 합격률·PPM은 예시였음)
